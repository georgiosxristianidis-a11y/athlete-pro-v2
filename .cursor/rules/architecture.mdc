---
description: Архитектура и код-конвенции — указатель на канон .claude/rules/architecture.md.
globs: js/**,routes/**,lib/**,css/**,server.js,sw.js,index.html
alwaysApply: false
---

# Архитектура — канон

**Источник правды: `.claude/rules/architecture.md`.** Открыть его перед правкой кода;
здесь только маршрут, чтобы указатель не разъехался с каноном.

Что там: Store/View, цепочка бэкенда, навигация `Nav.go`, таблица ключевых файлов,
закон четырёх вкладок, Haptic Gate, `esc()`, алиасы миграции планов.

Процессные правила (версия, флаги, PR, миграции) — `CLAUDE.md` § Rules.
