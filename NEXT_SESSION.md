# NEXT SESSION — Athlete Pro

> Читать первым. Это **роутер**, а не свалка состояния: здесь только точка входа и указатели.
> Обновлено 2026-08-13 (THEME-5 закрыта, 1.27.47; CHANGELOG догнан до 1.27.47; HYG-4 закрыта наполовину, заведена BASE-1; очередь извлечения `elite-hud-wow`: GUARD-1, HYG-3, HUD-1 и HUD-2 закрыты, роутер ведёт на HUD-3).

## Правило файла

**Числа и SHA здесь не хранятся** — они протухают от собственного мёржа (корень R4).
Файл дважды поймали на лжи: заявлял VERSION 1.25.19 и «355 тестов» при 1.25.62 в main,
а в тех-заметках рядом лежало «273 теста» и `SW v113`. Добывать командой:

```bash
git fetch origin && git rev-parse --short origin/main   # где main
npm run preflight                                        # старт сессии
npm run inventory                                        # ревизия веток
npm run smoke:prod                                       # доехал ли релиз до прода
```

Актуальная версия — `js/version.js`. Done-история — `CHANGELOG.md`.

## Правила работы

Живут в `CLAUDE.md` (мульти-агентный протокол · закон структуры · Session Protocol ·
анти-хрупкий workflow · влитие через PR в main). Дизайн-спек и инварианты AIR —
`.claude/rules/design.md`, грузится автоматически при работе с `css/**` и `*.view.js`.
Здесь они больше не дублируются: две копии правил = одна из них протухшая.

**Точка интеграции одна: `origin/main`.** Долгоживущий trunk упразднён (O-3) и удалён;
бэкап линии — тег `backup-trunk-6b4f80b`. **Branch-protection на main НЕТ** — проверено
2026-08-04: `gh api repos/.../branches/main` отдаёт `protected: false` (`.../protection`
даёт 403, приватный репо на бесплатном плане её не поддерживает). Единственный барьер —
`.githooks/pre-push` (блокирует любой пуш, чья ЦЕЛЬ — `refs/heads/main`, включая форму
`HEAD:main` с фича-ветки: судит по `remote ref` из stdin, а не по имени HEAD, O-9;
обход `MAIN_PUSH_OK=1`). Обратная сторона того же чтения stdin: пуш из одних
удалений (`git push origin --delete <ветка>`) выходит нулём до всех четырёх гардов —
кода он не отправляет, проверять в нём нечего. Плюс
дисциплина: `gh pr create --base main` → зелёные чеки → `gh pr merge --rebase`.
**`core.hooksPath` теперь относительный (`.githooks`) и резолвится от корня каждого рабочего
дерева** — хуки у ворктри свои, протухший корень их больше молча не выключает (HYG-4 п. 1,
`76cfaf5` + `672592e`). `preflight` краснеет, если путь резолвится вне чекаута. Прежняя
формулировка про абсолютный путь устарела; в `CLAUDE.md` § Multi-Agent Protocol она ещё есть.
**Решение принято 2026-08-06 и переоткрывать его не нужно:** GitHub Pro не берём, публичным
репозиторий не делаем (необратимая публикация всей истории ради одной настройки). Вместо
запрета — видимость: `.github/workflows/main-watchdog.yml` должен заводить issue, если в
`main` приехал коммит с не-зелёным CI или без связанного PR. `npm run preflight` про
недоступную по плану защиту больше не краснеет — вместо неё проверяет, что жив сам хук
(1.27.21).

> **Сторож жив с 1.27.26 и проверен на живых прогонах** (был мёртв с установки в 1.27.23:
> 403 на запросе связанных PR валил job целиком, вместе с обоими сигналами). Проверен обеими
> сторонами: зелёный прогон на чистом мёрже и ручной прогон на заведомом нарушении, который
> реально завёл issue. Последний необёрнутый вызов закрыт в 1.27.28: отказ на ручном пути
> падает через `setFailed` с названным правом, а не необработанной ошибкой.
> Слепое пятно закрыто в 1.27.29 (WATCH-2): третий вход `schedule` раз в час идёт от
> `HEAD main`, а не от события, и заводит issue, если прогонов CI на коммите ноль. Раньше
> такой коммит был невидим по построению — нет прогона, нет `workflow_run`.
> История фазы — `docs/_archive/HANDOFF_main_watchdog.md`.
> **Зелёная галочка на коммите ≠ чеки прошли:** `combined status` бывает `success` от одного
> Vercel при нуле check-runs. Считать: `gh api repos/:owner/:repo/commits/<sha>/check-runs`.

## Куда идти за работой

| Хендофф | Что внутри |
|---|---|
| `docs/handoff/HANDOFF_boot_brand.md` → **SPLASH-1** | **Активная фаза, следующая карточка.** Запуск и знак: три поколения логотипа жили одновременно, сплэш закрывал уже отрисованный шелл. CLEAN-1 (1.27.34) и BRAND-PACK (1.27.35) закрыты кодом; за BRAND-PACK остался полевой чек `about://webapks` за Gio, доезжает через сутки-двое. Дальше SPLASH-1 → MOTION-1, порядок утверждён Gio 2026-08-08. Параллелить нельзя: три из четырёх карточек трогают `index.html`, две — `dashboard.js` |
| `docs/handoff/HANDOFF_panda_core.md` → **HUD-3** | **Активная очередь извлечения. Разбор линии `feature/elite-hud-wow` (Antigravity), 2026-08-11. Линия НЕ вливается** — утечка BYOK-ключа в неаутентифицированный эндпоинт, синк мимо `airgap`, снесённый `SyncManager`, ослабленный гард `air-guard`. Код сохранён тегом `checkpoint-elite-hud-wow`. Извлекается только UI экрана `s-intel`. **Гарды под перенос уже стоят: GUARD-1 закрыта** (`test/perimeter-guard.test.js`, `4ba4d26`, 2026-08-12) **и HYG-3 закрыта** (`890a538`) — брать можно сразу. **HUD-1 закрыта** (1.27.45): база экрана, Air & Glass, error UI. **HUD-2 закрыта** (1.27.46): скелетон в ленте, точки хвостом текста, волна в кнопке отправки, Air Markdown отдельным модулем `js/shared/air-markdown.js` под гардом `test/intel-markdown.test.js`. Следующая — **HUD-3 (worker)**: карточки тренировок, биометрический радар, слайдер тона; там же обе непочиненные дыры — сборка карточки из потока без `esc()` и `SCAN FAILED` в биометрическом оверлее. **Ворктри воркера резать от свежего `origin/main`** — ворктри и под HUD-1, и под HUD-2 приезжали нарезанными от самой отвергнутой линии; с BASE-1 это ловит `npm run preflight` (проверка 3b «донорские линии», гейт `test/preflight-base-guard.test.js`), руками сверять больше не нужно. Дальше HUD-3 → HUD-4 (LEAD, бэкенд `routes/coach.js`). **HUD-1..4 строго последовательны — общий `js/intel.view.js`, параллелить нельзя.** Вне очереди HUD, отдельными заходами: NAV-1 (потеря тапа в быстрой серии) и NAV-2 (`s-intel` мимо конвенции выхода). Решения Gio закрыты грилежом, переоткрывать не нужно |
| `docs/handoff/HANDOFF_intel_readiness.md` → **INTEL-1** | Индекс готовности на экране `s-intel`. **INTEL-2 закрыта** (1.27.49): движок `js/intel.engine.js` считает индекс локально и детерминированно — до неё числа виджета приезжали из ответа LLM и менялись от запроса к запросу, а в `airgap` не появлялись вовсе. Веса (ACWR 0.35 · восстановление 0.30 · монотонность 0.20 · тренд 0.15) и пороги ЦНС (72 ч, 85% 1ПМ) — стартовые, вынесены в константы под калибровку. **Хвост за Gio: выгрузка бэкапа с боевого телефона** — сверить индекс с ощущениями. Порядок дальше: INTEL-1 (session RPE) → INTEL-3 (виджет) → INTEL-4 (кнопки и контракт), переставлять нельзя |
| `docs/handoff/HANDOFF_field_check.md` | **За Gio, один заход.** Карточки с кодом на проде, ждут подписи по DoD-5. Список А — дома до зала, Б — в зале. Перед заходом включить флаги `fab-video` (Профиль → AI) и `drum-window` (консоль) |
| `docs/handoff/HANDOFF_next_cards.md` → **DRUM-TOUCH-1** | **За Gio, один тап.** Барабан веса/повторов на iPhone не крутился (`touch-action:none` на предке при живом скроллере-ребёнке — WebKit-специфика, Blink её не видит). Фикс на проде с 1.27.13 (`pan-y`), но WebKit-половина доказана только выводом — Playwright не даёт touch-drag для WebKit. Закрыть может только полевая проверка: покрутить барабан на телефоне |
| `docs/handoff/HANDOFF_light_theme.md` → **THEME-6** | **🟡 P2, следующая.** THEME-4 (1.27.37, фон листа на токен `--c-sheet`) и THEME-5 (1.27.47, `color-scheme: dark`/`light` — нативные пикеры дат и скроллбары синхронны с темой) закрыты. Осталась **THEME-6**: единственная контраст-проба в проекте (`test/e2e/regressions.spec.js:143`) слепа к теме по построению — жёстко ждёт светлый текст на тёмном фоне, поэтому не может поймать регрессию в светлой теме. Нужна проба, считающая фактический контраст в обеих темах. Кандидат THEME-7 (`--c-text-3` и `--c-accent` на светлых поверхностях, 3.3–4.0:1) ждёт подтверждения Gio по полю |
| `docs/handoff/HANDOFF_gemini_audit_triage.md` → **PERF-1** | Триаж аудита Gemini (2026-08-12), разобран 2026-08-13. **Отчёт прогнан не по `main`, а по ветке `elite-hud-wow`** (439 файлов против 337, база −17 коммитов, в сюите не хватало шести тестов) — три пункта из пяти на ложных премиссах, включая требование ослабить `repo-hygiene.test.js` (гард сработал, а не сломался) и «94 балла Lighthouse лечатся App Shell / Worker / rIC» (все три уже в `main`). Итоговый вердикт отчёта перевёрнут: гардов не хватает, а не слишком много. Семь карточек, между собой по файлам не конфликтуют: **PERF-1** (разведка — сколько дадут бандл и 725 мс Style & Layout; поглотил пункт 5) · **LEAK-1** (две реальные утечки слушателей — `dashboard.js:297`, `claude.view.js:108` — плюс гард-счётчик) · **WEED-1 закрыта** (снесены `cross-env` и `render.yaml`, `DEPLOYMENT.md` переписан с 303 строк шаблона под реальный Vercel-деплой; **🔴 хвост за Gio: сервис на Render всё-таки заводили — удалить его в дашборде и отозвать `ANTHROPIC_API_KEY`; `routes/coach.js` без аутентификации, только rate-limit на IP**) · **SCAF-1** (`.agents/**` вписать в закон H-3, потолок 400 не трогать) · **NOISE-1** (отладочные `console.log` в проде + `no-console`; четыре пустых `[Sync]` — след оборванной правки) · **PII-1** (`piiReplacer` режет по точному ключу, `bodyWeight` проедет) · **HYG-4 — половина закрыта** (`76cfaf5` + `672592e`: прошивка хуков больше не зависит от корня и больше не валит деплой Vercel; корень проверен на `main` 2026-08-13. **Открыт п. 2: ворктри, их стало 35 против 31** — прополка не назначена) · **BASE-1 — 🟡 к обсуждению** (свежий ворктри снова приехал на `8e23fd6` отвергнутой линии; третье срабатывание подряд, развилка из трёх вариантов в хендоффе, рекомендация — гард в `preflight`). **Ветка аудита — донор, не мёрж и не удаление**; `hlc.js`, `routes/sync.js` и 633 строки sync-тестов в очередь HUD не входят |
| `docs/handoff/HANDOFF_next_cards.md` | Стек карточек + открытый бэклог + DATA-SAFETY + решения за Gio |
| `docs/handoff/HANDOFF_design_system.md` | Дизайн-система: иерархия · отступы · движение · отклик |
| `docs/handoff/HANDOFF_gym_grade.md` | Программа GYM-GRADE, DoD из 5 пунктов |
| `docs/handoff/HANDOFF_isl_tail.md` | Островные хвосты, задачи Sonnet |
| `docs/handoff/HANDOFF_orchestration.md` | Роли LEAD/worker/verifier, DoD-лестница, правило прополки веток |
| `docs/handoff/HANDOFF_air_refactor.md` | AIR-хвост |
| `docs/_archive/HANDOFF_main_watchdog.md` | Закрыт целиком — сторож `main`: WATCH-1 (был мёртв с установки, 1.27.26), WATCH-3 (последний необёрнутый вызов, 1.27.28), WATCH-2 (вход по расписанию, 1.27.29) |
| `docs/_archive/HANDOFF_releases_1.25.x.md` | Закрытые разборы релизов (304-заморозка, panda-idle, аудит-консолидация, серия F-*) |
| `docs/_archive/HANDOFF_load_perf.md` | Закрыта — программа LOAD, все 8 карточек взяты (1.27.2–1.27.6): ленивый CSS · modulepreload · логотип статус-бара · сжатие иконок · фильтр прекеша (`.d.ts` + неиспользуемые шрифтовые подмножества) · `/__build` только на localhost/LAN |

## Порядок взятия (утверждён Gio — минимум конфликтов по файлам)

**TYPE-2 закрыта целиком** (7 заходов, последний — `intel.css` + инлайны в `js/`, 1.27.12).
Детали по файлам — в `docs/handoff/HANDOFF_design_system.md`, эта строка их не
дублирует, чтобы не повторить протухание 2026-08-04.

**Ветка CSS-* закрыта целиком: CSS-1 (`*/` в комментарии съедал правило) → CSS-2
(кросс-файловые дубли) → CSS-3 (мёртвый радиус под AIR) → CSS-4 (классы без разметки,
1.27.19).** Приёмы, годные дальше: искать правила **обходом CSSOM**, спорный `!important`
судить по **`git log -S`**, а «класс мёртв?» — тем же `git log -S` отличать born-dead от
осиротевшего. CSS-4 дописала DEAD-1: оверлей AI-коуча (`f6fbd21`) унёс с собой 15 живых
когда-то селекторов `css/claude.css` плюс функцию `buildBodySVG`.

**Вне дизайн-очереди и выше её была THEME-1** (нечитаемые оверлеи в светлой теме) —
корень закрыт THEME-4 в 1.27.37 (замер 16.1:1), нативные контролы закрыты THEME-5 в 1.27.47.
Живой хвост линии — THEME-6 (гард контраста, 🟡 P2, `HANDOFF_light_theme.md`);
приоритета «выше дизайн-очереди» у него уже нет — нечитаемого текста на экране больше нет.

Дизайн-очередь (`HANDOFF_design_system.md` § «Порядок») — **SPACE-1 заходы №1–4 закрыты**
(`profile` · `dashboard` · `workout` · `analytics`, последний 1.27.32); остался хвост
инлайнов в `js/`. Дальше **SCALE-1** (закрывающая: гард шкал `--fs-*/--fw-*` в CI, чтобы сырые
px/веса не вернулись тем же путём, каким ушли; только после того как TYPE-2 и SPACE-1 пройдут
все файлы) → **NAV-BACK-1 + DEAD-1** (одно окно на двоих: общий `base.css`, отдельные коммиты).

> **SCALE-1 до 2026-08-12 звалась GUARD-1** и делила имя с карточкой периметра из
> `HANDOFF_panda_core.md` (закрыта, `4ba4d26`). Переименована дизайновая — та вморожена
> в коммит и в шапку `test/perimeter-guard.test.js`. В старых записях ищи оба имени.

**O-8 закрыта** (`HANDOFF_orchestration.md` § 5a, 1.27.22): протухший `sw.js` теперь красит
`npm test` — `test/sw-cache-name.test.js` пересобирает манифест теми же функциями, что и CLI
(`build-sw.mjs` стал модулем с экспортами), и сверяет три вещи: состав `ASSETS`, дайджест в
`CACHE_NAME` и полное совпадение с тем, что записал бы `build:sw`. Вместе с 1.27.20
(платформо-независимый хеш) пара «протух» / «разъехался по ОС» закрыта целиком.

NAV-BACK-1 не параллелить с SPACE-1/SCALE-1 — общий `base.css`.

**Вне очереди, полевое:** DRUM-TOUCH-1 (барабан на iPhone, фикс на проде с 1.27.13,
ждёт подтверждения телефоном) и ZERO-1 (снят запрет закрывать сет на нуле, уже
на проде, закрыта без хвостов) — обе `docs/handoff/HANDOFF_next_cards.md`.

Дальше по очереди аналитики: **AN-2** (разрез по упражнению — тап открывает историю лифта),
затем AN-3 (время в зале; инфраструктура периода уже есть в `analytics.store.js` `periodRange()`),
OVW-1. После стека — роадмап CRDT foundation (`docs/ROADMAP_elite_athlete-pro.md`).

## Технические заметки

- **Запуск:** `npm run dev` → :3000 (порт 3000 = Gio, 3001 = Claude preview). Телеметрия — `scripts/telemetry-server.mjs --lan`; `server.js` отладочными стабами не подменять.
- **Тесты:** `npm test` = unit+integration; `npx playwright test` — e2e отдельно и **на тёплом сервере** (холодный или зомби на :3000 → флаки goto-таймаутов).
- **SW:** ASSETS и `CACHE_NAME` только через `npm run build:sw` — `CACHE_NAME` авто-бампится контент-хешом манифеста, ручной `vNNN` не нужен. Хеш платформо-независим (текст нормализуется CRLF → LF, бинарники — байт в байт, `scripts/sw-digest.mjs`): повторный прогон на любой ОС обязан давать **пустой дифф**. Не пустой — значит либо кто-то правил `sw.js` руками, либо ассеты поменялись без пересборки. Забыть пересборку больше нельзя молча: `npm test` краснеет (`test/sw-cache-name.test.js`, O-8).
- **lhci гонять ТОЛЬКО из worktree** — корневой чекаут даёт фейковые цифры (кейс perf 61, 2026-07-18).
- **Прод:** Vercel-проект `gio-g7/athlete-pro-v7` (алиас athlete-pro-v7.vercel.app), git-репо athlete-pro-v2, деплой с `main`.
- **Git в worktree:** после убитой по таймауту команды git может висеть (pager держит tty) — `GIT_PAGER=cat` и `</dev/null`.
