# HANDOFF — стек карточек (после Air Cleanup 1-3/5/6)

> Обновлено 2026-07-03. Правило: 1 карточка = 1 сессия, сверху вниз.
> Гейт перед коммитом: `npm test` (214) + `npm run lint` (0 err). Перед рискованным — тег `checkpoint-<date>`.
> База: trunk `claude/csp-soft-delete` @ 4ef45b2 == main == origin (release 1.19.0 задеплоен).

---

## Карточка R — RELEASE 1.19.0 — ✅ ЗАКРЫТА 2026-07-02

- Версии выровнены на 1.19.0 (package.json, js/version.js), SW v84→v85 (version.js в прекэше), package-lock догнал stylelint-деревом.
- Гейт 214/214 + lint 0 err. Коммит `4ef45b2` → FF push trunk + main, Vercel success, прод отдаёт VERSION 1.19.0 и SW v85 (curl-проверено).
- Осталось за Gio: ручной тест «назад» (BUG-NAV) на телефоне.

## Карточка 4b — DB-SPLIT: мелкие сторы — 🚧 сделана в worktree determined-yalow (`ae9ec9a`), ждёт FF-мёржа LEAD

- Metrics/Events/Nutrition/Planned в `js/db/*` (см. память project-state-2026-07-02-dbsplit-4b). При мёрже сверить бамп SW с 4c (v87 у bold-fermi).

## Карточка 4c — DB-SPLIT: Workouts — ✅ `e3b2992` (2026-07-03, ветка bold-fermi)

- Вынесен побайтово в `js/db/workouts.js`, `js/db.js` — фасад, SW v86→v87. Гейт 214/214 + e2e 39/39 + превью-смоук зелёные, LEAD-проверка диффа пройдена.

## Карточка 4d — DB-SPLIT: Backup + финализация

- **ЦЕЛЬ:** вынести Backup (import/export, withMeta-штамп) в `js/db/backup.js`; `js/db.js` остаётся чистым фасадом. build:sw + бамп. Карточка 4 закрыта целиком → отметить в HANDOFF_air_cleanup.md.
- **ГДЕ СТОП:** гейт + смоук export→import в превью.
- **НЕ ТРОГАТЬ:** формат бэкап-JSON (совместимость со старыми экспортами и import-ETL).

## Карточка BUG-DRUM-0 — сет логируется как «0×1» 🔒 LEAD (данные!)

- **СИМПТОМ (скрин Gio 2026-07-03):** в Deadlift среди done-сетов есть «0×1» — вес обнулился при логировании; активные барабаны при этом показывают 0/1. Похоже на новый путь регрессии фантомного нуля (fd99a2e закрывал `display:none`-путь, этот — другой: воспроизводилось при раскрытом rest-таймере в Island).
- **ЦЕЛЬ:** воспроизвести (rest-overlay поверх + быстрый тап по set-check следующего сета?), найти путь, где `flushDrum`/`onSettle` (`js/ui/drum-picker.js:22,104`) читают неинициализированный/скрытый барабан, закрыть гардом + unit-тест на репро.
- **ГДЕ СТОП:** репро-тест падает до фикса, зелёный после; гейт + e2e.
- **НЕ ТРОГАТЬ:** island-set-pulse; API стора.

## Карточка UI-CENTER — вертикальное оцентрование текста в set logger

- **ЦЕЛЬ:** выровнять по вертикали кластер имени упражнения: `.exercise-name` + `.muscle-badge` + `.exercise-meta` (`css/workout.css:150-157`, badge `:301`). Замер: badge сидит на ~3px ниже оси имени (padding 2px 6px при font 8px), meta прижата (margin-top 1px) — блок читается смещённым вверх. Оптическое центрирование сверху-вниз; применить ко ВСЕМ аналогичным блокам set logger (PPL-бейджи, done-состояние `.exercise-meta.done`, свёрнутые карточки).
- **ГДЕ СТОП:** скриншот-сравнение до/после в превью (mobile 375px); гейт.
- **НЕ ТРОГАТЬ:** размеры шрифтов/токены палитры; только выравнивание.

## Карточка GLOW-SL — свечение и просвечивания в set logger 🟩 (CSS-свип по спеку)

- **СИМПТОМЫ (скрин + замеры 2026-07-03):** (а) соседние значения барабанов (`.drum-item` opacity .22) фантомно просвечивают возле чужих строк и ADD SET; (б) тёмный скрим/градиент наезжает на ADD SET при done-сетах; (в) статс-бар просвечивает сквозь раскрытый Island (alpha .85 + blur); (г) зелёное свечение вокруг `.set-check.done`.
- **ЦЕЛЬ:** `mask-image` fade на `.drum-track` вместо голой opacity (обрезать фантомы за пределами колеса), убрать наезд скрима на ADD SET, поднять непрозрачность раскрытого Island до ~0.95.
- **ГДЕ СТОП:** скриншоты до/после; гейт. Ничего из этого не решает перф — это следующая карточка.
- **НЕ ТРОГАТЬ:** island-set-pulse; геометрию барабана (ITEM_H/snap).

## Карточка PERF-DRUM — виртуализация барабанов 🔒 LEAD

- **ФАКТЫ (замеры 2026-07-03, десктоп):** 4 642 из 5 428 DOM-узлов (86%) — `.drum-item` (161 весовых + 50 репс на каждую строку сета); ADD SET = long task 76ms (на телефоне ~×4 = видимый микрофриз); скролл-бёрст = 55ms; `_updateActive` (`js/ui/drum-picker.js:134`) гоняет `querySelectorAll` по всем 161 на каждом тике скролла.
- **ЦЕЛЬ:** за флагом `js/flags.js` (Strangler-Fig) рендерить окно ~9-15 значений вокруг текущего + перестраивать при скролле; `_updateActive` — адресно по индексу, без `querySelectorAll`. DOM экрана воркаута ≤ ~1.5k узлов.
- **ГДЕ СТОП:** long task при ADD SET < 20ms на десктопе; A-4-тест видимости цифр зелёный; гейт + e2e; полевой чек на телефоне за Gio.
- **НЕ ТРОГАТЬ:** flushDrum/onSettle-гарды (fd99a2e), scroll-snap UX, haptic.

## Карточка GLASS-LITE — убрать blur с exercise-карточек 🟩

- **ФАКТЫ:** групповое правило `css/base.css:745-769` вешает `backdrop-filter: blur(20px)` на 11 классов карточек; на экране воркаута 7 таких одновременно — на мобильном GPU backdrop-слои перерисовываются с лагом = мерцание при скролле/кликах.
- **ЦЕЛЬ:** `.exercise-card` (и по решению — остальные списковые карточки) → сплошной `var(--c-surface-deep)` без blur; blur остаётся только на island/nav/модалках.
- **ГДЕ СТОП:** скриншот-сравнение (glass-вид не должен развалиться); гейт.
- **НЕ ТРОГАТЬ:** модалки/Island/nav; токены палитры.

## Карточка Q2 — PII console.log sweep (мелкая, можно делегировать)

- **ЦЕЛЬ:** убрать логирование чувствительного: `routes/integrations.js:69` (длины ключей G=/A=) + grep `console.log` по routes/lib/js на ключи/имена/веса → почистить или перевести на lib/logger.js.
- **ГДЕ СТОП:** grep не находит PII/ключей в логах. Функциональные логи не трогать.
- **НЕ ТРОГАТЬ:** логику; только лог-строки.

## Карточка A-4 — тест видимости drum после Add Set 🟦

- **ЦЕЛЬ:** unit/e2e: после «+ add set» новый ряд имеет `.drum-item--active` с видимым числом (регресс BUG-7/drum-fix fd99a2e).
- **ГДЕ СТОП:** тест падает на ревёрте фикса, зелёный на текущем коде.
- **НЕ ТРОГАТЬ:** сам set-logger.

## Карточка W — прополка правил (регулярная: раз в ~2 недели, после закрытия пакета; следующая ~середина июля 2026)

- **ЦЕЛЬ:** ревизия правил, не кода. По каждому жёсткому правилу (CLAUDE.md § Rules + Session Protocol, NEXT_SESSION § анти-хрупкий workflow, память-feedback): срабатывало за период → живёт; ни разу → вынести из жёстких в заметку или удалить. Обновить протухшие факты (версии, цифры тестов, имена файлов). Слить дубли: правило живёт в одном месте, остальные — ссылки.
- **ГДЕ СТОП:** один docs-коммит. Прополка — ЕДИНСТВЕННОЕ место, где правила меняются; между прополками они неприкосновенны.
- **НЕ ТРОГАТЬ:** код; правила с пометкой жёсткого запрета пользователя (island-set-pulse) — только с Gio.
- Прополка №1 = DOC-SYNC 2026-07-02 (сделана).

---

После стека: роадмап CRDT foundation + полевые DS1 (миграция v3→v4 на живых данных) / DS2 (2-девайс конвергенция) — за Gio с телефонами.
