# HANDOFF — стек карточек (после Air Cleanup 1-3/5/6)

> Обновлено 2026-07-04 (вечер, сессия awesome-davinci: PERF-DRUM закрыта). Правило: 1 карточка = 1 сессия, сверху вниз (исключение: GLASS-LITE + BLACK-DRUM — пара в одной сессии).
> Гейт перед коммитом: `npm test` (229) + `npm run lint` (0 err). Перед рискованным — тег `checkpoint-<date>`.
> База: trunk `claude/csp-soft-delete` @ c39f33f == main == origin (1.19.1 на проде) + ветка `claude/awesome-davinci-3ae27c` (815eec7+f73a6f3, SW v88, НЕ влито). Тег отката: `checkpoint-2026-07-03-drum-0`.
> ЭТА версия файла новее 4a703f7 (gallant-wu) и ab2afe8 (bold-fermi) — при конфликте на rebase брать её.

---

## ✅ Закрыто (детали — CHANGELOG + память)

- **R — RELEASE 1.19.0** (`4ef45b2`, прод SW v85). Остаток за Gio: ручной тест «назад» на телефоне.
- **4c — DB-SPLIT Workouts** (`e3b2992`, bold-fermi, SW v87).
- **BUG-DRUM-0 — сет «0×1»** (релиз 1.19.1 `c39f33f`, на проде). Репро-тесты в `test/drum-picker.test.js`.
- **PERF-DRUM — виртуализация барабанов** (2026-07-04, `815eec7`+`f73a6f3`, awesome-davinci): окно 15 значений за флагом `drum-virtual` (default ON), спейсеры сохраняют scrollTop-математику, `_updateActive` адресно. Замеры: drum-items 4642→750 (-84%), DOM экрана 5479→1696. Все гарды BUG-DRUM-0 гоняются на обоих путях (юниты ×2). Гейт 229/229 + e2e 39/39. Kill switch: `Flags.setFlag('drum-virtual', false)`. Остаток за Gio: полевой чек скролла/фризов на телефоне.

## 🔒 За LEAD (мёрж-очередь, не карточки)

- **4b — DB-SPLIT мелкие сторы** — 🚧 готово в worktree determined-yalow (`ae9ec9a`), ждёт FF-мёржа. При rebase сверить SW-бампы: v87 занят (4c), v88 занят (PERF-DRUM) → им v89.
- Ветки awesome-davinci (PERF-DRUM) и gallant-wu (`4a703f7`, docs) — влить FF после гейта.

---

## Карточка GLASS-LITE — убрать blur с exercise-карточек 🟩 (пара с BLACK-DRUM: одна сессия — СЛЕДУЮЩАЯ, выбор Gio)

- **ФАКТЫ:** групповое правило `css/base.css:745-769` вешает `backdrop-filter: blur(20px)` на 11 классов карточек; на экране воркаута 7 таких одновременно — на мобильном GPU backdrop-слои перерисовываются с лагом = мерцание при скролле/кликах.
- **ЦЕЛЬ:** `.exercise-card` (и по решению — остальные списковые карточки) → сплошной `var(--c-surface-deep)` без blur; blur остаётся только на island/nav/модалках.
- **ГДЕ СТОП:** скриншот-сравнение (glass-вид не должен развалиться); гейт. Полевой чек на OLED Gio — один на пару с BLACK-DRUM.
- **НЕ ТРОГАТЬ:** модалки/Island/nav; токены палитры.

## Карточка BLACK-DRUM — total black барабаны 🟩 (пара с GLASS-LITE, та же сессия)

- **КОНТЕКСТ:** Gio выбрал по мокапу 2026-07-04 **вариант B** — тотальный минимализм, только цифры.
- **ЦЕЛЬ (зафиксировано Gio):** `.drum-wrap` — БЕЗ рамки и подложки вообще (убрать `--c-surface` фон и border); фон = чистый `var(--c-bg)`; `.drum-sel` вместо плашки с рамкой → две горизонтальные hairline-линии (border-top/bottom `var(--c-border-h)`) во всю ширину полосы выбора; соседние `.drum-item` — opacity **0.4** (вместо 0.22 — на чистом чёрном 0.22 исчезает). WCAG: актив #fff на чёрном 21:1 (AAA).
- **ГДЕ СТОП:** скриншот до/после (mobile 375px, тёмная тема); гейт + e2e drum-тест; полевой чек на OLED Gio — общий с GLASS-LITE.
- **НЕ ТРОГАТЬ:** геометрию барабана (ITEM_H 36 / snap / виртуализацию PERF-DRUM), гарды fd99a2e/0ad7626, island-set-pulse; токены не добавлять — только существующие.

## Карточка UI-CENTER — вертикальное оцентрование текста в set logger

- **ЦЕЛЬ:** выровнять по вертикали кластер имени упражнения: `.exercise-name` + `.muscle-badge` + `.exercise-meta` (`css/workout.css:150-157`, badge `:301`). Замер: badge сидит на ~3px ниже оси имени (padding 2px 6px при font 8px), meta прижата (margin-top 1px) — блок читается смещённым вверх. Оптическое центрирование сверху-вниз; применить ко ВСЕМ аналогичным блокам set logger (PPL-бейджи, done-состояние `.exercise-meta.done`, свёрнутые карточки).
- **ГДЕ СТОП:** скриншот-сравнение до/после в превью (mobile 375px); гейт.
- **НЕ ТРОГАТЬ:** размеры шрифтов/токены палитры; только выравнивание.

## Карточка GLOW-SL — свечение и просвечивания в set logger 🟩 (после пары GLASS-LITE+BLACK-DRUM — они меняют фоны)

- **СИМПТОМЫ (скрин + замеры 2026-07-03):** (а) соседние значения барабанов фантомно просвечивают возле чужих строк и ADD SET; (б) тёмный скрим/градиент наезжает на ADD SET при done-сетах; (в) статс-бар просвечивает сквозь раскрытый Island (alpha .85 + blur); (г) зелёное свечение вокруг `.set-check.done`.
- **ЦЕЛЬ:** `mask-image` fade на `.drum-track` вместо голой opacity (обрезать фантомы за пределами колеса), убрать наезд скрима на ADD SET, поднять непрозрачность раскрытого Island до ~0.95. Перепроверить симптом (а) после BLACK-DRUM (opacity 0.4 меняет картину).
- **ГДЕ СТОП:** скриншоты до/после; гейт.
- **НЕ ТРОГАТЬ:** island-set-pulse; геометрию барабана (ITEM_H/snap).

## Карточка 4d — DB-SPLIT: Backup + финализация

- **ЦЕЛЬ:** вынести Backup (import/export, withMeta-штамп) в `js/db/backup.js`; `js/db.js` остаётся чистым фасадом. build:sw + бамп (следующий свободный номер — сверить с 4b!). Карточка 4 закрыта целиком → отметить в HANDOFF_air_cleanup.md.
- **ГДЕ СТОП:** гейт + смоук export→import в превью.
- **НЕ ТРОГАТЬ:** формат бэкап-JSON (совместимость со старыми экспортами и import-ETL).

## Карточка Q2 — PII console.log sweep (мелкая, можно делегировать)

- **ЦЕЛЬ:** убрать логирование чувствительного: `routes/integrations.js:69` (длины ключей G=/A=) + grep `console.log` по routes/lib/js на ключи/имена/веса → почистить или перевести на lib/logger.js.
- **ГДЕ СТОП:** grep не находит PII/ключей в логах. Функциональные логи не трогать.
- **НЕ ТРОГАТЬ:** логику; только лог-строки.

## Карточка A-4 — тест видимости drum после Add Set 🟦

- **ЦЕЛЬ:** unit/e2e: после «+ add set» новый ряд имеет `.drum-item--active` с видимым числом (регресс BUG-7/drum-fix fd99a2e). Гонять с флагом `drum-virtual` в обоих положениях (паттерн — `test/drum-picker.test.js`).
- **ГДЕ СТОП:** тест падает на ревёрте фикса, зелёный на текущем коде.
- **НЕ ТРОГАТЬ:** сам set-logger.

## Карточка W — прополка правил (регулярная: раз в ~2 недели, после закрытия пакета; следующая ~середина июля 2026)

- **ЦЕЛЬ:** ревизия правил, не кода. По каждому жёсткому правилу (CLAUDE.md § Rules + Session Protocol, NEXT_SESSION § анти-хрупкий workflow, память-feedback): срабатывало за период → живёт; ни разу → вынести из жёстких в заметку или удалить. Обновить протухшие факты (версии, цифры тестов, имена файлов). Слить дубли: правило живёт в одном месте, остальные — ссылки.
- **ГДЕ СТОП:** один docs-коммит. Прополка — ЕДИНСТВЕННОЕ место, где правила меняются; между прополками они неприкосновенны.
- **НЕ ТРОГАТЬ:** код; правила с пометкой жёсткого запрета пользователя (island-set-pulse) — только с Gio.
- Прополка №1 = DOC-SYNC 2026-07-02 (сделана).

---

После стека: роадмап CRDT foundation + полевые DS1 (миграция v3→v4 на живых данных) / DS2 (2-девайс конвергенция) — за Gio с телефонами.
