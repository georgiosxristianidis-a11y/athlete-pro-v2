# HANDOFF — стек карточек (после MERGE-QUEUE-3)

> Обновлено 2026-07-07 (релиз 1.21.0 выкачен + старт островной программы). Правило: 1 карточка = 1 сессия, сверху вниз.
> Гейт перед коммитом: `npm test` (245) + `npm run lint` (0 err). Перед рискованным — тег `checkpoint-<date>`.
> База: trunk `claude/csp-soft-delete` = **`0431ca4`** (SW **v96**, запушено origin). **main == trunk == прод == `0431ca4` (релиз 1.21.0)** — Vercel Production success (athlete-pro-v7). Тег `checkpoint-2026-07-07-release-1.21.0`.
> Следующий релиз в main = **1.22.0** (планово — островная программа профилей).
> В worktree нет `node_modules` (резолв из родителя); pre-push зовёт `stylelint` голым именем → падает. Фикс: `npm install` в worktree ИЛИ пуш из главного чекаута. НЕ обходить хук `--no-verify` на main (auto-mode блокирует).
> Проверять принадлежность коммита линии через `git merge-base --is-ancestor <hash> <trunk>`, НЕ по имени ветки — хеши меняются на rebase.

---

## ✅ Закрыто в MERGE-QUEUE-3 (2026-07-06, trunk `f47d3a8`, запушено)

- **Q2 — PII log sweep** (`45d20ad`): длины ключей `routes/integrations.js:69`→`logInfo`; +бонус `js/dashboard.js:868` `console.log('[AI]')` эхоил workout-данные — убран.
- **A-4 — тест drum после Add Set** (`98630d8`, test-only): +6 тестов ×flag. Реальный баг-фикс = `b7bab66` (BUG-7), не `fd99a2e`.
- **GLOW-SL — фантомы set logger** (`86f0a8e`, только `css/workout.css`): mask-image края барабана + `background:transparent` на ADD SET. Симптомы (в)/(г) оказались несуществующими, остров не тронут.
- **AIR-2 — Controls на рецепт AIR** (`6b5c515`): сегмент-контролы `base.css:695-724` Tier-1 flat, active `--c-chrome-t`+hairline.

## ✅ Выкачено в релизе 1.21.0 (2026-07-07, main==trunk `0431ca4`, прод-Vercel success)

- **ISL-REDESIGN** (SW v94): 2 состояния острова вместо 3-цикла (выпилены `_displayMode`/`_cycleMode`/localStorage). Полевой-чек Gio (видео) пройден.
- **AIR-3** (`c538300`, SW v95): снят glass с summ-island + intel-cmd-bar.
- **DHL-CLASSIFY** (`60b1609`, SW v96): дефолт-фолбэк `loadPlan()` → `PPL_GIO_PLAN.weekA/weekB` (4 камеры из коробки) вместо плоского `DEFAULT_PLAN` (оставлен экспортом); `classifyChamber(ex)` — фолбэк для безблочных упр. (heavy▣/shape▥/arms◆/core○) в обеих ветках `buildSession`. Live-add сохраняет `block:'custom'`. +10 тестов. Полевой-чек Gio (десктоп) ок.
- **FOCUS-QUARANTINE** (`f82bc25`): Focus Mode (`_openFocus`, лонг-пресс карточки) вылезал сырым DOM за колонку — **у фичи нет CSS вообще**. Триггер отключён early-return в `_initFocusLongPress` (обратимо 1 строкой). Судьба фичи — решается в островной программе.

---

## 🏝 ОСТРОВНАЯ ПРОГРАММА (утв. Gio 2026-07-07) — цель: остров = DHL-навигация + минимализм/AIR

Решения Gio: остров во время тренировки = **навигатор по камерам (DHL) + минимализм (уйти с глаз, AIR-воздух, не панель)**. Переключение профилей = **настройка** (НЕ цикл по тапу — не воскрешать выпиленный `_cycleMode`). Тап = expand/collapse. Долгий-пресс = экран настроек острова. Custom = **фаза 2**. Целевой релиз программы = **1.22.0**.

- **Карта ISL-SET** (первая, маленькая, самодостаточная): долгий-пресс острова → отдельный экран **Island Settings** (`s-island-settings`); туда же перенести privacy-режим. Тап оставить expand/collapse. ГДЕ СТОП: экран открывается по лонг-прессу, privacy работает оттуда, гейт. НЕ ТРОГАТЬ: island-set-pulse, тап-логику.
- **Карта ISL-PROFILE**: абстракция профиля — одна render-функция острова `render(profile, state)`, ветвление layout внутри, за флагом `js/flags.js`. Профили: **Minimal-DHL** (дефолт: свёрнутый = тонкая полоска с DHL 4-точки + `КАМЕРА N/M`, near-black+hairline, без панели; развёрнутый = крупный DHL-трекер кликабельный + компактный контекст камеры, без пустого центра; отдых = крупная убывающая полоса + next + `+время`) и **Apple-2-state** (текущий ISL-REDESIGN как legacy-опция). Выбор — в Island Settings, персист. ГДЕ СТОП: оба профиля переключаются из настроек, дефолт Minimal-DHL, гейт + полевой чек.
- **Карта ISL-CUSTOM** (фаза 2, опц.): кастом-профиль/конструктор лейаута. Не начинать, пока не подтверждена реальная нужда.

---

## Карточка AIR-3 — summary / intel / athlete-room на рецепт AIR 🔴 (следующая: вектор Gio)

- Полная карточка — `HANDOFF_air_refactor.md` § AIR-3. Продолжение elevation-раскатки после AIR-1 (данные) / AIR-2 (контролы): снять blur/тени с оставшихся Tier-1 поверхностей (summary-карточки, intel, athlete-room), фон `--c-bg-2`+hairline, оставить blur только на Tier-2 (modal/toast).
- **ГДЕ СТОП:** verify-блок + полевой OLED-чек, 1 коммит + SW-бамп (следующий свободный — сверить с v94 ISL-REDESIGN!).
- **НЕ ТРОГАТЬ:** Tier-2 (modal-overlay/sheet/toast blur), island-set-pulse.

## Карточка 4d — DB-SPLIT: Backup + финализация 🟠 (закрывает давнюю карту 4)

- **ЦЕЛЬ:** вынести Backup (import/export, withMeta-штамп) в `js/db/backup.js`; `js/db.js` остаётся чистым фасадом. build:sw + бамп (сверить номер!). Карточка 4 закрыта целиком → отметить в `HANDOFF_air_cleanup.md`.
- **ГДЕ СТОП:** гейт + смоук export→import в превью.
- **НЕ ТРОГАТЬ:** формат бэкап-JSON (совместимость со старыми экспортами и import-ETL).

## Карточка SW-UPDATE-UX — грациозное обновление PWA 🟠 (родилось из вопроса Gio 2026-07-06)

- **ПРОБЛЕМА:** сейчас `sw.js` = `skipWaiting()`+`clients.claim()`, а `js/app.js:339` на `controllerchange` делает безусловный `location.reload()`. Новая версия применяется сама — но может **перезагрузить страницу посреди тренировки** (data-loss/резкий UX). И нет явного «обновление доступно».
- **ЦЕЛЬ:** (1) не релоадить, если идёт активная тренировка (`State.plan?.length` + незакрытые сеты) — отложить до конца/показать тост «Новая версия — применить?» с кнопкой; (2) на `online`-событии дёрнуть `registration.update()`, чтобы оффлайн→онлайн подтягивал фикс; (3) опц. кнопка «Проверить обновление» в профиле. Тост — через существующий `window.Toast`, aria-live уже есть.
- **ГДЕ СТОП:** обновление во время тренировки не рвёт сессию; вне тренировки — тост+кнопка применяют; смоук в превью (эмуляция new SW).
- **НЕ ТРОГАТЬ:** саму precache/fetch-стратегию sw.js; CACHE_NAME-ритуал.

## Карточка AIR-2b — body-stats таб-каскад 🟨 (мелкий хвост AIR-2)

- **ЦЕЛЬ:** `css/body-stats.css:15-40` — второе, более старое правило `.bs-tab.active` (`--c-surface`+box-shadow) грузится после base.css (lazy) и перебивает AIR-2-выравнивание на экране Body Stats. Привести к рецепту AIR (плоский `--c-chrome-t`+hairline).
- **ГДЕ СТОП:** экран Body Stats визуально консистентен с остальными таб-барами; гейт.
- **НЕ ТРОГАТЬ:** логику табов.

## Карточка SET-STALE — чек-боксы не перерисовываются при внешнем апдейте State 🟦 (низкий: один пользователь/одна вкладка в поле)

- **СИМПТОМ:** если State.plan обновился не через клик в этой вкладке (вторая вкладка / resume-гонка), `_updateLiveStats` пересчитывает тоннаж/рельс/чип верно, а `.set-check`/`.set-done-summary` в строках старые (наблюдалось State=19 done при 9 закрашенных в DOM).
- **ЦЕЛЬ:** найти, кто обновляет State без перерисовки строк (sync/storage-listener? resume?), дорисовывать точечно (`renderSetRow` по расхождению, не полный re-render).
- **ГДЕ СТОП:** репро двух вкладок задокументирован; чек-боксы == State.
- **НЕ ТРОГАТЬ:** формулы `_updateLiveStats`; LWW sync.js.

## Карточка W — прополка правил (регулярная, срок ~середина июля 2026 — близко)

- **ЦЕЛЬ:** ревизия правил, не кода. По каждому жёсткому правилу (CLAUDE.md § Rules + Session Protocol, память-feedback): срабатывало → живёт; ни разу → в заметку/удалить. Обновить протухшие факты (версии, цифры тестов 235, SW-номера, имена файлов). Слить дубли.
- **ГДЕ СТОП:** один docs-коммит. Прополка — единственное место, где правила меняются.
- **НЕ ТРОГАТЬ:** код; island-set-pulse — только с Gio.

---

После стека: роадмап CRDT foundation + полевые DS1 (миграция v3→v4 на живых данных) / DS2 (2-девайс конвергенция) — за Gio с телефонами.
