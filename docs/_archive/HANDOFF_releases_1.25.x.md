# АРХИВ — разборы релизов 1.25.x

> Вынесено из `NEXT_SESSION.md` 2026-08-02. Здесь лежат закрытые разборы: причина бага,
> что именно сделали, чем верифицировали. Смотреть при археологии — в оперативный
> хендофф это больше не грузится.

---

## РЕЛИЗ 1.25.5 — ЛЕЧЕНИЕ 304-ЗАМОРОЗКИ (2026-07-24) ✅

Корень полевого бага «телефон показывает 1.24.1 / 1.21 при проде 1.25.4»: Vercel нормализует mtime файлов в константу (окт 2018), а weak ETag Express строится из mtime+size → у любого файла с неизменным размером ETag **совпадает между релизами**. `version.js` всегда 35 байт (`'1.24.1'`→`'1.25.5'`) → возвращающийся клиент вечно получает 304 и крутит старый код. Доказано curl на живом проде: `ETag: W/"23-1668f272800"` — тот же mtime-компонент, что и у всех прочих файлов.

- `8d29651` — `express.static`: `etag:false, lastModified:false`; SPA-fallback `sendFile`: `no-store` + без валидатора.
- `9e162cb` — **обратно валидаторы для медиа**: блэнкетный `etag:false` целил в код, но ронял ассеты в дефолт `public, max-age=0` БЕЗ валидатора → `panda-voice.mp4` (327КБ) качался целиком на каждый промах кеша. Отдельный mount `/assets`: `etag:true, lastModified:false, max-age=1h` (бинарь не меняет размер на месте — его ETag честен).
- `a510d1f` — SW: честный skipWaiting-хендшейк + loop-guard + тап 5× по версии как аварийный форс-апдейт.

Верифай curl на сервере ветки: `/assets/panda-poster.jpg` + `If-None-Match` → **304, 0 байт**; `/js/version.js` + отравленный ETag прода → **200, 35 байт** (заморозка невозможна). SW `v114-2549f128`.

**Лечение уже залипших телефонов** (данные целы): Chrome → История → Очистить → Всё время → галочка ТОЛЬКО «Изображения и другие файлы в кеше» (cookie/данные сайтов НЕ трогать — там IndexedDB с тренировками) → закрыть Chrome и PWA полностью → открыть заново.

**Полевой чек зелёный (2026-07-25, Gio):** телефон обновился до 1.25.5 сам, SW подхватился. Карточка закрыта целиком.

---

## МЁРТВЫЙ ВЕС МЕДИА — panda-idle выпилен (2026-07-26) ✅

`assets/panda-idle.mp4` (1.37 МБ) + `assets/panda-idle.webm` (1.32 МБ) лежали с `b099f73` (v1.0.0) и **ни разу** не были подключены: живой маскот (флаг `fab-video`) крутит `panda-voice.mp4` + `panda-poster.jpg` (`js/shared/panda-video.js`), idle-петли нет ни в js/css/html/json, ни в планах.

- **Порядок обязателен:** `git rm` → `npm run build:sw` → коммит одним куском. Удалять файл из `assets/`, не перегенерив манифест, — значит оставить в `ASSETS` путь, которого нет. Install при этом **не** падает (`precache()` в `sw.js` глотает ошибку каждого `cache.add` по отдельности — сознательно, чтобы один битый ассет не ронял весь SW), поэтому симптом тихий: запись просто отсутствует в кеше и всплывает офлайном.
- **Пустой диф `sw.js` здесь — правильно.** F-7 уже выкинул медиа из прекеша (`MEDIA_RE` в `scripts/build-sw.mjs`), `panda-idle.*` в манифесте не было, а `CACHE_NAME` считается только по записям манифеста. Если после `git rm` медиа-файла манифест всё-таки поехал — значит медиа снова просочилось в `ASSETS`, и это баг (ловится `test/sw-media-budget.test.js`).
- Клиентского трафика удаление не экономит (после F-7 эти файлы никто уже не качал) — экономит 2.7 МБ веса деплоя и снимает ловушку «мёртвый ассет выглядит живым».

---

## ОРКЕСТРАЦИЯ — программа закрыта (2026-07-25) ✅

Разбор трёх инцидентов (потеря влитого PR#9 · прод-фикс провисел невлитым · битый git-email → 500 на create-PR) + карточки — `docs/handoff/HANDOFF_orchestration.md`. Там же DoD-лестница (готово = на проде, не «закоммичено») и роли LEAD/worker/verifier.

**Закрыты все пять корней:** O-1 гард кеш-заголовков · O-2 `npm run smoke:prod` · O-4 `npm run preflight` · O-5 `npm run inventory` (вернул два зелёных потерянных теста + `FABLE5_FINDINGS.md`) · O-3 trunk упразднён · O-6 шапка хендоффа без SHA. Плюс программа гигиены репо: 4492 → 270 отслеживаемых файлов и закон структуры под гардом (`CLAUDE.md` § «Где что лежит»).

O-7 (контент-хеши ассетов, конфликт с no-build) решён паллиативом без билд-шага 2026-08-01.

---

## АУДИТ-КОНСОЛИДАЦИЯ (2026-07-24, релиз 1.25.4) ✅

Линейная ветка `a58ec9` (security `8fed1ae` + SW-автобамп `2654637` + handoff) сведена LEAD, бамп VERSION 1.25.4, релиз через **PR #10** (rebase-merge — main защищён, прямой push отклонён).

**Авто-мёрж вооружён:** правило `Bash(gh pr merge:*)` в `.claude/settings.local.json` (персональный, gitignored) — релиз/handoff-PR вливаются `gh pr merge --auto --rebase` без ручного подтверждения и без поллинга (main-защита = гейт).

**DESIGN.md заархивирован (PR #13):** повтор потерянной работы PR#9 (`e4685e9` откатился при пересборке trunk — НЕ ancestor origin/main, `DESIGN.md` Vantablack снова врал агентам из корня). Cherry-pick от свежего origin/main, merge `8a71516` → `docs/_archive/DESIGN-vantablack-DEPRECATED.md`. Гоча: `user.email` в worktree был битый (без @) → GitHub 500 на POST /pulls.

**Residuals, не закрытые осознанно:**
- stylelint цвето-правило `warning→error` — сначала чистка 84 rgba (Phase E), иначе гейт мгновенно красный.
- `profile.css:546` = 28px — единственный реальный сырой border-radius (токена 28px нет). Остальные «сырые px» из аудита = микро <6px (ниже `--r-xs`), легитимны.
- CORS `credentials:true` (`server.js`) — опц. чистка; origin-allowlist уже строгий, не открытая дыра.

---

## СЕРИЯ F-* — ПОЛЕ 2026-07-26 ✅ (вся закрыта)

F-1 AR-SAVE-CRASH P0 + F-4 UI-TOAST-REDESIGN (1.25.12) · F-7 SW-MEDIA-BUDGET (1.25.13) · F-2 ISL-PIP-NEXT + F-3 AR-DOB-PICKER + F-6 ERR-CLASSIFY-MODULE (1.25.15, PR#39 одним поездом) · F-5 UI-SPLASH (1.25.16). Тест-хвост F-1 (unit на `saveName()` + порядок записи) закрыт `e2339e2`, гард `test/athlete-room-save-name.test.js`.

Перф-инструменты в репо с 1.25.12 (`scripts/profile.mjs` / `graph-audit.mjs` / `compare-bundle.mjs`). Замеры сделаны — **не перемерять**. Главное: на S23 Ultra CPU не проблема (TBT 0, тапы 54-94 мс), проблема — сотовый трафик.

Прочие закрытые с полевым хвостом: BACKUP (1.24.3) · DRUM-PERF-2 (1.24.4, флаг `drum-window`) · FAB-VIDEO (1.25.2, флаг `fab-video`) · P-1 ISL-REST-BTN (1.25.6) · AIR-4 (1.25.8, гард `test/air-guard.test.js`) · AIR-2b (1.25.9) · ISL-REST-NEXT (1.25.7). Неподписанные полевые чеки собраны в `docs/handoff/HANDOFF_field_check.md`.
