# HANDOFF — Настройки ИИ в P.A.N.D.A. Core — ЛИНИЯ ЗАКРЫТА

> Заведён 2026-08-29 решением Gio, закрыт 2026-08-30. Обе карточки влиты, файл переехал
> в архив тем же PR. Детали релизов — `CHANGELOG.md`.

## PC-1 — настройки ИИ переехали в шторку ядра — ЗАКРЫТА (1.27.80)

Закрыта 2026-08-29, три коммита в `main`: `974b6e6`, `a8853d3`, `552e800`. Движок, BYOK-ключ
и тон живут в одной шторке — вход с шестерёнки `s-intel` и строкой из Профиля. Ради чего
заводилась: чат, сводка и биометрия ядра ходят через `aiAuth()`, а не литералом
`engine:'gemini'` — сторожит `test/intel-engine-wiring.test.js`. TTS осознанно остался на
Gemini с подписью в шторке. Хвост (прямое «НЕ ТРОГАТЬ» на маскоте) стал `PC-2`.

## PC-2 — индикатор движка у FAB говорит правду — ЗАКРЫТА (1.27.81)

Закрыта 2026-08-30, PR #285 (Cursor), `7cb1394`. Индикатор судил ключ по локальному BYOK
(`js/claude.view.js:72`) и врал в обе стороны: красный над рабочим серверным Gemini, зелёный
над Claude без ключа. Источник правды теперь `probeAiStatus()`; чистая функция
`fabKeyState(probed, hasLocalKey, engine)` в `js/ai-settings.store.js` различает четыре
состояния, airgap перестал выглядеть поломкой, `.ai-indicator.ready`/`.airgap` получили
реальные правила в `css/base.css`. Смена приватности и движка патчит точку без перезагрузки.
Гард — `test/fab-key-state.test.js`, включая греп «в `claude.view.js` нет чтения `gemini-key`».

Это остаток `F-13` из аудита `LAUNCH-1`; первую половину (маскировку airgap-503) закрыла
`LAUNCH-6`.

**Хвост Gio, в трек запуска:** полевой чек на телефоне — Stats и Профиль, тёмная и светлая
тема, Cloud на шаге 5 онбординга должен перекрасить индикатор без перезагрузки. Светлая тема
на шторках частично мертва (THEME-5/6 в архиве) — не блокер, но фиксировать, что увидел.

**Семантическая мелочь для того, кто вернётся:** класс `ready` в шторке ядра
(`js/ai-settings.view.js`) значит «движок доступен, но не выбран», в FAB — «локальный ключ
есть, пробник живым сервером не подтвердил». Один класс, два смысла; на одном элементе не
встречаются, CSS общий и нейтральный.
