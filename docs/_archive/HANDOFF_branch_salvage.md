# Handoff: невлитые ветки — что спасать (SALV)

> **Заморожен, не отменён.** Линия заведена прополкой 2026-09-13 (36 веток → 20).
> Брать карточку отсюда — решением Gio, не «по дороге»: трек запуска приоритетнее.
> Роутер — `NEXT_SESSION.md`. Правила процесса — `CLAUDE.md`.

## Закрыто разбором — работы нет, ветки под снос

Обе выглядели живыми по возрасту и объёму, но код либо уже в `main`, либо отменён решением.

- **`air-4-sweep-guard` (+3, 48 дн, лок.+origin).** Гард из карточки AIR-4 стоит в `main`
  как `test/air-guard.test.js` — та же проверка `backdrop-filter` вне Tier-2 вайтлиста,
  только шире: в ветке одна половина, в `main` обе (Tier 1 без блюра И Tier 2 обязан его
  нести). Второй кусок ветки — сплэш с растрового `icons/icon-192.png` на инлайн-SVG —
  протух целиком: элемента `#loading` в `index.html` больше нет, сплэш снят
  (он закрывал уже готовый шелл). Остаётся бамп `sw.js` и строки в доках.
  **Вердикт: удалить обе копии.**
- **`claude/profile-hero-spacing-eb9cc7` (+2, 46 дн).** Карточка свёрнута решением Gio
  2026-08-01 в **SPACE-1**; архивный `HANDOFF_profile_hero_spacing-MERGED-INTO-SPACE1.md`
  говорит прямо: «работать по нему отдельно — значит сделать ту же правку дважды».
  **Вердикт: удалить.**

## SALV-1 · PP-4 — ритм hero профиля

**ЦЕЛЬ.** Влить `claude/profile-hero-rhythm-hierarchy-a352a9` (+1, 45 дн): единый
горизонтальный ритм паспорта и `flex-wrap` на `.pp-badge-row`, чтобы бейджи не сдавливали
друг друга. Заодно три сырых значения уходят на шкалу (`4px` → `--sp-0-5`,
`12px` → `--sp-1-5`, `8px` → `--sp-1`) — это закон дизайна, а не вкус.

**ГДЕ СТОП.** Гейт зелёный, скриншот hero до/после в натуральную величину, PR влит.

**ФАЙЛЫ.** `css/profile.css` — 13 строк, один файл. Ребейз дешевле переноса.

**ГЕЙТ.** `npm run build:sw && npm test`; бамп версии обязателен — тронут `css/`.

**НЕ ТРОГАТЬ.** Остальной `css/profile.css`. PP-3 остаётся за Gio, PP-6 — в SALV-4.

## SALV-2 · panda-core — разрезать пачку

**ЦЕЛЬ.** Разобрать `fix/panda-core-stabilization` (+6, 26 дн). Пачка разнородная, и
линия PANDA заморожена (`docs/_archive/HANDOFF_panda_core.md`) — вливать целиком нельзя,
но один коммит в ней к панде отношения не имеет:

1. **RestTimer не останавливался чисто при cancel/finish** — фоновая утечка таймера.
   Это баг активной тренировки, а не косметика маскота. Брать первым и отдельным PR.
2. Мотор настроения панды, нарезка аудио, PR-cheer — заморожено вместе с линией.
3. `scripts/handoff-lifecycle.mjs` (CLI архивации хендоффов + интеграция в preflight) —
   инструмент, живёт своей жизнью, в `main` его нет.
4. Сжатие `CLAUDE.md`/`NEXT_SESSION.md` на 769 токенов — **перепроверить построчно**:
   резка доков теряет правила молча, а оба файла с тех пор ушли далеко вперёд.

**ГДЕ СТОП.** П. 1 влит отдельным PR; по пп. 2-4 в этой карточке записан вердикт Gio.

**ФАЙЛЫ.** `js/workout.view/*` (таймер), `js/panda*`, `scripts/handoff-lifecycle.mjs`.

**НЕ ТРОГАТЬ.** Не вливать пачку одним PR — п. 2 разморозит линию без решения.

## SALV-3 · exercise-history — большая ветка под разбор

**ЦЕЛЬ.** Решить судьбу `claude/exercise-history-main-e57957` (+12, 26 дн, 31 файл,
+3180 строк). Несёт тесты (`test/exercise-history.test.js`, `test/dashboard-boot-motion.test.js`,
дополнения к `intel-readiness` и `session-summary`) и правило `.claude/rules/omni_scenarios.md`.
Верхний коммит — **ревёрт собственной фичи** (ClockSpinner/TimerLinear в RestTimer HUD),
то есть автор сам откатился; остальное не разбиралось.

**ГДЕ СТОП.** По каждому из 12 коммитов вердикт: влить / переложить смысл / выбросить.
База отстала на 26 дней — вливать как есть нельзя, переносить смысл почти наверняка дешевле.

**ФАЙЛЫ.** Сперва `git log --oneline origin/main..claude/exercise-history-main-e57957`,
потом `git show --stat` по каждому. Дифф `test/` читать РАНЬШЕ диффа кода.

**НЕ ТРОГАТЬ.** Ревёрт не «отменять обратно» без разбора, почему автор откатился.

## SALV-4 · четыре одиночки по коммиту

**ЦЕЛЬ.** Быстрый вердикт по четырём origin-веткам, в каждой ровно один коммит:

| Ветка | Возраст | Что внутри |
|---|---|---|
| `claude/profile-targeted-rerender-84a1d6` | 45 дн | PP-6 — точечный ререндер тумблеров настроек вместо полного |
| `claude/measurements-tabs-lag-overflow-f12334` | 37 дн | форма замеров вылезала за экран, вкладки комнаты тормозили (1.27.17) |
| `claude/dither-card-handoff` | 35 дн | только карточка DITHER-1 (дизер-заливка как приём) |
| `vercel/install-and-configure-vercel-w-v170kn` | 22 дн | Vercel Web Analytics; `@vercel/analytics` в `package.json` нет |

**ГДЕ СТОП.** По каждой: жива — PR-ом, протухла — удалить. Не «отдельно потом».

**НЕ ТРОГАТЬ.** Аналитику Vercel не включать без решения по приватности — дефолт airgap
и `docs/THREAT_MODEL.md` старше этой ветки.

## SALV-5 · salvage-db-backup-wip

**ЦЕЛЬ.** Решить судьбу `origin/claude/salvage-db-backup-wip` (+1, 45 дн): снимок
IndexedDB перед миграцией (`js/shared/db-backup.js`, `test/db-migration-guard.test.js`).
Автор сам пометил коммит «подобрано, НЕ готово к вливанию».

**ГДЕ СТОП.** Либо карточка на доведение с гейтом, либо удаление с записью почему.
Тема не косметическая: миграция без снимка — это потеря данных пользователя.

**НЕ ТРОГАТЬ.** Не вливать as-is — автор явно сказал, что не готово.
