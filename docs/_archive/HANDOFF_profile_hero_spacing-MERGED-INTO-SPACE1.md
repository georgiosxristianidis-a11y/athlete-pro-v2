# Handoff: profile-hero-spacing — ЗАКРЫТ СВОРАЧИВАНИЕМ (2026-08-01)

> ⚠️ **Это архив, НЕ инструкция к исполнению.** Решением Gio (2026-08-01) карточка свёрнута в **SPACE-1** (`docs/handoff/HANDOFF_design_system.md`): заход №1 SPACE-1 идёт по `css/profile.css` целиком и лечит описанное ниже системно, а не точечной заплаткой. Уточнять «какой именно элемент» больше не нужно — блокер снят самим фактом сворачивания.
>
> Файл сохранён только ради контекста исходной постановки. Работать по нему отдельно — значит сделать ту же правку дважды.

Date: 2026-07-28

## ЦЕЛЬ
Выровнять air сверху/снизу вокруг одного из элементов паспорта профиля (Gio просит одинаковый отступ сверху/снизу + отдельно отступ ДО «шкалы» — сейчас та слишком прижата к краю).

## СТАТУС
Не начато. Сессия остановилась на уточняющем вопросе — какой именно элемент (кандидаты: `js/profile.view/hexagon-radar.js` — хексагон-радар, или `js/profile.view/lift-bars.js` — полосы по подъёмам). Gio скриншот не приложил, велел разобрать в новой сессии.

## СЛЕДУЮЩИЙ ШАГ
Спросить/уточнить у Gio, какой элемент и (лучше) получить скриншот. Затем найти конкретный CSS-блок в `css/profile.css` (padding/margin вокруг искомого узла) и выровнять top/bottom + добавить отступ до «шкалы» через токены `--sp-*` (не хардкод, PP-4 уже отметила хардкод 4/8/12/24px в hero вместо шкалы).

## ГДЕ СТОП
После визуального совпадения top/bottom air и отступа до шкалы — verify в браузер-превью (computed styles/скриншот), затем обычный git-ритуал (коммит → рекомендация PR).

## НЕ ТРОГАТЬ
PP-4 (три ритма, отдельная карточка в `docs/handoff/HANDOFF_design_system.md`), PP-6 (точечный ре-рендер вместо `screen.innerHTML`) — не смешивать с этой правкой.

## Blockers
Нет скриншота/точного элемента — блокер на уточнение у Gio.
