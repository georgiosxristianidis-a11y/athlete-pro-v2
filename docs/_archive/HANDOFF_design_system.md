# HANDOFF — ЕДИНАЯ ДИЗАЙН-СИСТЕМА (иерархия · отступы · движение · отклик)

> Заведён 2026-07-27. Цель Gio дословно: «единая и чётко работающая иерархия, дизайн-система, система отступов, система анимаций и взаимодействий с пользователем».
> Спек живёт в `CLAUDE.md` § Design — этот файл не дублирует его, а держит очередь работ по приведению кода к спеку.
> Порядок программы: **сначала шкалы (токены) → потом применение по экранам → потом гард в CI.** Наоборот не работает: без шкалы каждый экран изобретает свою.

---

## Диагноз в цифрах (аудит 2026-07-27, факты — не ощущения)

| Система | Состояние | Число |
|---|---|---|
| Отступы | шкала `--sp-*` есть (4/8/12/16/24/32/40/48), но проигрывает хардкоду | **364** хардкод-px в `padding/margin/gap` против **157** через токены |
| ~~Типографика~~ | ~~шкалы нет вообще~~ → шкала `--fs-1..6` введена (TYPE-1, 1.25.32), переведён Профиль | было **284** объявления `font-size`, **24** значения; осталось перевести остальные экраны |
| ~~Насыщенность~~ | ~~9 вариантов~~ → `--fw-md/bold/black` = 600/700/800 | 500/600 неразличимы, 900 у Manrope клэмпится к 800 — упразднены |
| Радиусы | 8 ступеней 6/8/10/14/18/22/24/pill — почти континуум | шкала не читается как шкала |
| Движение | 8 кастомных кривых объявлено — но входы/выходы едут на `--ease-std` | **44** перехода на встроенных `ease/linear`; `--t-slow` объявлен и **не используется нигде** |

Вывод: «иерархия не работает» — не про вкус. Иерархия строится на контрасте, а 55 элементов на 11px и 49 на 10px — это не контраст, это шум. То же с отступами: 70% значений мимо шкалы.

---

## Очередь карточек

### ANIM-1 · Токены движения — входы на ease-out, всё под 300ms ✅ ЗАКРЫТА (1.25.26)
Советы 4 и 6 из [7 practical animation tips](https://emilkowal.ski/ui/7-practical-animation-tips).

- **ЦЕЛЬ:** ввести `--t-enter`/`--t-exit` с правильными кривыми; перевести появления/исчезновения на них; урезать всё, что дольше 300ms и является откликом (не «рисующей» анимацией).
- **Конкретный список:** `overlay-in` (голый `ease` → `--ease-out`), `sheet-in` 0.4s, `toast-in` 0.42s, `fadeSlideUp` 0.4s, `card-in` (`--ease-std` → `--ease-out`), статус-бар `filter/opacity 0.4s ease` (`athlete-room.css:11,22`), `claude.css:187` 0.35s; мёртвый `--t-slow` удалить.
- **ГДЕ СТОП:** `css/base.css`, `css/athlete-room.css`, `css/claude.css`.
- **НЕ ТРОГАТЬ:** «рисующие» анимации (заполнение баров 0.8s в `analytics.css:275`, `athlete-room.css:501`) — это не отклик на действие, правило <300ms к ним не относится. Бесконечные keyframes (`shimmer`, `it-breathe`) — тоже.
- **DoD:** ни один transition/animation отклика не длиннее 300ms; входы не едут на `--ease-std`; ветка `prefers-reduced-motion` перепроверена; `npm test` зелёный.

### ANIM-2 · Единая глубина нажатия ✅ ЗАКРЫТА (1.25.27)
Токены `--press-scale: .97` / `--press-scale-lg: .94` в `base.css`; 40 `:active`-правил идут от них, голых чисел в нажатиях не осталось. Отклик добавлен `.set-check` (без `transition`), `.bs-tab`, `.toast-action`, `.pp-bento-cell` (scale к фону), нажимаемым `.pref-row-icon`. `.fab-close-btn` стартует с 0.92, `.nav-btn:active` переехал со `svg` на кнопку.

**Отклонения от карточки (осознанные):**
- Голый `.pref-row` отклик НЕ получил: это контейнер вокруг тумблера, сам не нажимается (`privacy.view.js:66`). Подсветка обещала бы несуществующий тап. Взамен покрыты реально нажимаемые `button.pref-row-icon` / `.pref-row-icon[data-action]` (напр. экспорт данных, `profile.js:69`) — у них отклика не было вовсе.
- Попутно исправлены 6 битых `transition: transform, opacity <time>` — без длительности у `transform` он анимировался за 0s, то есть новый скейл на `.btn-ghost` / `.week-pill` / `.bs-tab` / `.bs-del-btn` / `.streak-dot` / `.mascot-close-btn` не «пружинил» бы вовсе.
- Снят дубль `.btn-start-workout:active` (два байт-в-байт одинаковых правила, `dashboard.css`).
- `prefers-reduced-motion` не трогали: глобальный блок (`base.css:1218`) обнуляет длительность, отклик остаётся — это и есть правильное поведение.

<details><summary>Исходная карточка</summary>
Советы 1 и 2 оттуда же. Аудит проведён 2026-07-27, список ниже — закрытый, гадать не нужно.

**Диагноз:** 73 селектора `:active`, из них только **15** дают scale. Глубина нажатия у тех, что дают, разъезжается: `.privacy-seg-btn` — 0.97, `.claude-fab` — 0.88 `!important`, `.nav-btn svg` — 0.8. Три разные «силы нажатия» в одном приложении читаются как три разных продукта.

- **ЦЕЛЬ:**
  1. Ввести токен `--press-scale: .97` в `base.css` и свести к нему существующие `:active`-скейлы. Исключения допустимы только для крупных элементов (FAB), и тогда — вторым токеном `--press-scale-lg`, а не числом на месте.
  2. Добавить отклик тач-целям, у которых сейчас нет `:active` вообще (проверено грепом):
     - **`.set-check`** (`workout.css:244`) — 44×44, отметка сета. Главная повторяемая кнопка приложения, десятки нажатий за тренировку — и ноль визуального отклика. Здесь по совету 6 переход должен быть **мгновенным** (без `transition`), иначе на двадцатом сете анимация начнёт раздражать. Haptic Gate уже подключён — scale его дополняет, не дублирует.
     - **`.pref-row`** (`profile.css`) — строки настроек.
     - **`.bs-tab`** (`base.css`, `body-stats.css`) — табы Body Stats / Athlete Room.
     - **`.toast-action`** (`base.css`) — кнопка действия в тосте.
     - **`.pp-bento-cell`** — `:active` есть, но только фон; scale просится.
  3. `.fab-close-btn` стартует с `scale(0.5)` (`claude.css:76`) → 0.92 (совет 2: не анимировать «из ниоткуда»).
  4. `.nav-btn` живёт в inline-`<style>` внутри `index.html:186` (критический путь) — его `:active svg { scale(0.8) }` слишком глубокий и применён к иконке вместо кнопки. Свести к общей глубине.
- **ГДЕ СТОП:** CSS тач-целей + inline-блок навигации в `index.html`.
- **НЕ ТРОГАТЬ:** строки списков и ячейки таблиц (`.set-row`, календарные ячейки) — от scale они «дышат» при скролле. Ползунки и drag-цели — у них своя механика.
- **DoD:** ни одна тач-цель не отвечает «ничем»; все глубины нажатия идут от токена; `.set-check` отвечает мгновенно; `npm test` зелёный; проверка computed styles в превью, как в ANIM-1.

</details>

### TYPE-1 · Шкала типографики — корень иерархии ✅ ЗАКРЫТА (1.25.32), ШКАЛА УТВЕРЖДЕНА GIO
Шкала в `base.css :root` и в `CLAUDE.md` § Design: `--fs-1..6` = 10/13/16/20/26/34 (шаг ≈1.25), `--fw-md/bold/black` = 600/700/800. Пилот — Профиль: 39 объявлений в `css/profile.css` + 8 инлайнов в `js/profile.js` / `js/profile.view/settings.js`, сырых `font-size`/цифровых `font-weight` в них не осталось.

**Что реально изменилось на экране:** тело выросло 14/15 → 16, вторичный текст 11/12 → 13, микро-капс 9/10/11 → 10, hero-число 32 → 34, имя 24 → 26. Проверено в превью на 375px с длинными русскими строками («Восстановление», «Гипертрофия», «Георгиос», 4-значные числа) — переполнений нет, сегменты не переносятся, полоса метрик держит 335px.

**Осознанно НЕ переведены** (живут в `base.css`, общие для всех экранов — тронуть = задеть остальные до вердикта): `.screen-sub` 14px и `.btn-text` 11px/13px. Это первые кандидаты на заход №2.

**Вердикт Gio (2026-07-28): вариант A принят.** Сравнивались две шкалы с одинаковым шагом ≈1.25, разница только в анкере: A = 10/13/16/20/26/34 (база 16px) против B = 9/12/15/19/24/32 (база 15px, на экран влезает ~на строку больше). Взят A — читаемость важнее плотности: экран смотрят в зале, на весу. **Анкер больше не обсуждается** — следующие экраны переводить на A, не переизобретая шкалу.

<details><summary>Исходная карточка</summary>

- **ЦЕЛЬ:** ввести `--fs-*` (5–6 ступеней с честным контрастом, не по 1px) и `--fw-*` (3 варианта: 600/700/800), перевести экраны. Начать с одного экрана-эталона — Профиль, он уже разобран картами PP-*.
- **ГДЕ СТОП:** `css/base.css` (объявление шкалы) + один экран как пилот.
- **НЕ ТРОГАТЬ:** остальные экраны до утверждения шкалы Gio — иначе правка на 284 места без эталона.
- **DoD:** пилотный экран не содержит сырых `font-size`; шкала описана в `CLAUDE.md` § Design.

</details>

### TYPE-2 · Тираж шкалы по экранам ✅ ЗАКРЫТА (1.27.12, заход №7)
Продолжение TYPE-1: анкер закрыт двумя полевыми решениями Gio (TYPE-1 + TYPE-1b), пилот доказал, что шкала не ломает вёрстку на 375px — тираж прошёл по всем экранам.

- **ЦЕЛЬ:** перевести на `--fs-*`/`--fw-*` остальные экраны, по одному файлу за заход.
- **Переведены все экраны:** `profile.css` + `js/profile.view/*` (TYPE-1), `journal.css` (LOG-1, сразу на токенах), `.screen-title`/`.screen-sub` в `base.css` (TYPE-1b), **`base.css` (№1) → `dashboard.css` (№2) → `analytics.css` (№3) → `workout.css` (№4) → `athlete-room.css` (№5) → `privacy`/`summary`/`dynamic-island`/`claude` (№6) → `intel.css` + инлайны в 8 местах `js/` (№7) — все ниже**.
- **Остаток вне карточки, узаконен:** `base.css` — `.ppl-g-total`/`.ppl-g-cap` (SVG viewBox) + 9 `font-weight: 200 800` в `@font-face` (дескрипторы оси шрифта); `js/features/pip.js` — чужой документ (`documentPictureInPicture`), токены не доезжают.

#### Заход №1 — `css/base.css` ✅ (1.25.40)

29 сырых объявлений переведены, сырых `font-size` и цифровых `font-weight` в файле не осталось (кроме двух узаконенных исключений ниже). Что заметно поменялось на экране:

| Правило | Было | Стало | Почему эта ступень |
|---|---|---|---|
| `.section-label` («Recent», «PPL Split», «Top Lifts») | 12/600 | `--fs-1` (10) / `--fw-md` | это uppercase-капс с `letter-spacing: 0.1em`, а `--fs-1` и есть микро-капс. Плюс совпало с уже утверждённым близнецом `.section-label-alt` в Профиле (`profile.css:385`) — раньше два одинаковых по смыслу заголовка секции рендерились разным кеглем |
| `.btn-primary` / `.btn-ghost` / `.ui-input` / `.plan-input` / `.confirm-msg` / `.skip-link` / `.toast` | 14–15px | `--fs-3` (16) | тело и поля ввода — ровно тот же переход 14/15 → 16, что TYPE-1 сделала в Профиле |
| `.ui-stepper-val` | 17/700 | `--fs-3` / `--fw-bold` | значение в степпере |
| `.mini-stepper span` | 14/800 | `--fs-2` / `--fw-black` | «mini»-вариант обязан быть ступенью ниже обычного степпера |
| `.modal-title` | 18/600 | `--fs-4` (19) / `--fw-md` | заголовок секции |
| `.ui-stepper-btn` (глифы −/+) | 22/600 | `--fs-5` (23) / `--fw-md` | ближайшая ступень; кнопка 40×40, глиф влезает |
| `.cp-progress-text` 8px · `.cp-name-stack` / `.it-label` / `.it-progress` 9px · `.plan-meta-label` / `.ppl-g-leg-vol` 11px | 8/9/11 | `--fs-1` (10) | «ниже `--fs-1` не спускаться» — подтягиваем вверх, а не вниз |

**Замер (превью, 375px, русский язык, 60 сидовых тренировок в IDB).** Прошёл по всем пяти экранам — Home · Train · Stats · Профиль · Журнал: `scrollWidth == clientWidth == 375` везде, горизонтального переполнения нет ни на одном. Точечно проверены компоненты, которых нет на экранах по умолчанию: тост (самая длинная реальная строка «PPL | GIO загружен для недель A и B…» — 322×61, две строки, не три), `confirmDialog` (титул 19 · текст 16 · кнопки 16/600 и 16/800), `promptDialog`, UI-кит (`.ui-card-title` / `.ui-field-*` / степперы), `chamber-pill` во всех трёх режимах и `island-tracker`. Легенда PPL-гейджа: колонка 94px под значение 46px — запас есть.

**Два узаконенных исключения (в файле стоит комментарий с объяснением):** `.ppl-g-total` и `.ppl-g-cap` — это `<text>` внутри `viewBox="0 0 200 118"` при ширине до 280px, то есть их `font-size` задан в единицах viewBox, а не в CSS-px (30 рендерится как ~42). Подставить туда `--fs-*` значило бы отдать ступень умноженной на масштаб SVG. Та же система координат и то же исключение, что у подписей радара. `font-weight` от масштаба не зависит — он переведён на токены. Отдельно: девять `font-weight: 200 800` в `@font-face` — это дескрипторы диапазона переменной оси шрифта, а не объявления в правиле; их трогать нечем.

**Хвост, который карточка ждала в `base.css`, а он живёт не там** — ✅ закрыт отдельно (1.25.41). `.btn-text` («All» / «Все» рядом с заголовком секции) объявлен в `css/workout.css:69`, а не в `base.css`, поэтому в заход №1 не попал (ГДЕ СТОП — один файл). Последствие было видно сразу: после перевода `.section-label` на 10px кнопка «Все» (11px, `--fw-black`, акцентный зелёный) стала крупнее заголовка секции, рядом с которым стоит, — перевешивала его размером, весом и цветом сразу. Вылечено одной строкой: `.btn-text` → `--fs-1` / `--fw-black`, у правила стоит комментарий о связи с `.section-label`. Замер: Home «Все» 10/800 против «Recent» 10/600, Train «План» 10/800 против «Программы» 10/600 — один кегль, разница только весом и цветом. Инлайн `font-size: var(--fs-2)` у кнопки DISCONNECT (`profile.view/settings.js`) оставлен: она стоит одна в настройках, а не в строке заголовка.

**Замечено попутно → заведено карточкой DEAD-1** (`HANDOFF_next_cards.md`, 2026-08-01, вместе с остальным мёртвым CSS `base.css`): `.it-label` и `.it-progress` не рендерятся в проде вообще — оба call-site `renderIslandTracker` (`dynamic-island.js:350,364`) не передают ни `label`, ни `progress` (полевой откат 2026-07-08, «в развёрнутом острове было шумно»). Правила живые только в синтетике. Мелкая карточка на удаление мёртвого CSS.

**Гейт:** 418/418 · lint 0 err / 28 warn (счёт warn прежний).

#### Заход №2 — `css/dashboard.css` + инлайны `js/dashboard.js` ✅ (1.25.54)

31 объявление в CSS и 17 инлайнов в `js/dashboard.js` переведены; сырых `font-size`/цифровых `font-weight` в обоих файлах не осталось. Инлайны взяты в тот же заход по прецеденту TYPE-1 (там `profile.css` шёл вместе с `js/profile.view/*`): половина типографики экрана живёт в шаблонах модалки «Итоги недели» и спарклайна, и без них файл был бы чист, а экран — нет.

| Правило | Было | Стало | Почему эта ступень |
|---|---|---|---|
| `.session-title` / `.session-meta` / `.session-vol` (строка Recent) | 13/600 · 11/500 · 13/600 | `--fs-3`/`--fw-bold` · `--fs-1` · `--fs-2`/`--fw-black` | прямой аналог строки Журнала (`journal.css:200` — `.jr-type`/`.jr-date`/`.jr-vol`); два списка одних и тех же тренировок рендерились разным кеглем |
| `.dash-hero-title` · `.empty-dash-title` | 24/800 · 26/800 | `--fs-5` (23) / `--fw-black` | дисплей-строка, та же ступень, что имя в hero Профиля |
| `.dash-cta` · `.btn-start-workout` · `.empty-title` | 14–15/600 | `--fs-3` / `--fw-md` | тот же переход 14/15 → 16, что в `.btn-primary` заходом №1 |
| `.stat-chip-val` | 20/600 | `--fs-4` (19) / `--fw-md` | «значение в полосе метрик» — см. оговорку ниже, на экране пока не видно |
| `.streak-day-label` 9 · `.stat-chip-label` 9 · `.rec-pr-badge` 8 · `.streak-count` 11 · `.stat-chip-unit` 11 · `.session-meta` 11 · `.rec-reason` 11 | 8/9/11 | `--fs-1` (10) | «ниже `--fs-1` не спускаться» — подтягиваем вверх, а не вниз |
| `.rec-old` / `.rec-arrow` / `.rec-new` | 11–14 | `--fs-2` / `--fw-md`, у `.rec-new` `--fw-black` | вся тройка в одной строке; иерархия «старый → новый» держится цветом, весом и glow, а не размером |
| `.weekly-summary-chip .stat-chip-val` | 14 | `--fs-3` | оверрайд обязан быть ступенью ниже обычного `.stat-chip-val` — тот же приём, что `.mini-stepper` в заходе №1 |

**Оговорка по `.stat-chip-val` — перевод сделан, но на экране пока не виден.** Тот же класс объявлен второй раз в `css/analytics.css` (20px/800) и, будучи подключён позже, перебивает `dashboard.css`: замер в превью показывает 20/800, а не 19/600. То есть чипы Дашборда сегодня стилизуются файлом экрана Stats. Само по себе это не типографика, а кросс-файловый дубль — закроется заходом №3, который и так следующий по плану (`analytics.css`); отдельной карточки не требует, но заход №3 обязан начать именно с него, иначе перевод останется мёртвым в двух файлах сразу.

**Замер (превью, 375px, 30 сидовых тренировок).** `scrollWidth == clientWidth == 375`, горизонтального переполнения нет. Стресс русскими строками: «Добрый вечер» + «воскресенье, 31 августа» + CTA «Начать» — hero держит 335px (идентити 157 + кнопка 117 + gap); длинное имя упражнения («Разгибание голени в тренажёре стоя») переносит `.session-title` на две строки, строка Recent растёт 62 → 84px без переполнения — штатная деградация, как в PP-4. `.streak-day-label` на 10px: ячейка 15px в колонке 40px, запас есть. **Скриншот снять не удалось** — превью-панель скрыта, страница не композитит кадры; по той же причине не тикает `rAF`, а `renderRecent`/`renderStreak`/`renderRecommendations` вызываются из вложенных `requestAnimationFrame` (`dashboard.js:734`), поэтому эти три блока в замере вставлены синтетически по шаблонам из кода. Это ловушка стенда, не бага приложения.

**Замечено попутно, не тронуто (кандидат на снос, отдельная мелкая карточка):** вся модалка рекомендаций — `.recommendations-modal`, `.rec-ex-*`, `.ai-notes-*`, `.fatigue-*` — нигде не рендерится: в `js/` нет ни одного call-site. Живая только карточка `.recommendations-card` (`dashboard.js:788`). Плюс `.rec-old`/`.rec-arrow`/`.rec-new` объявлены в файле дважды, и нижний блок перебивает верхний — то есть модальный блок мёртв вдвойне. Переведены оба (DoD — ноль сырых значений в файле), у нижнего стоит комментарий с разбором. Снос — не в типографике.

**Гейт:** 476/476 · lint 0 err / **19** warn (было 28 — девять сняты этим переводом).

#### Заход №3 — `css/analytics.css` + инлайны `js/analytics.view.js` ✅ (1.25.57)

43 объявления в CSS и 5 инлайнов переведены; сырых `font-size`/цифровых `font-weight` в обоих файлах не осталось. Экран Stats нёс самый большой разброс из всех — 9/10/11/12/13/14/16/18/20/22/40 плюс одно значение в `rem`.

| Правило | Было | Стало | Почему эта ступень |
|---|---|---|---|
| `.sh-score` (индекс силы в hero) | 40/800 | `--fs-6` (30) / `--fw-black` | 40px не имел ступени вообще; `--fs-6` и есть «hero-число» |
| `.sc-cur` (текущий вес в карточке кривой) | 22/800 | `--fs-5` (23) / `--fw-black` | дисплей-значение, ступень ниже hero-числа |
| `.sh-stat b` · `#s-stats .stat-chip-val` | 18/800 · 20/800 | `--fs-4` (19) | два «значения в полосе метрик» стояли на разных кеглях, хотя роль одна |
| `.sc-name` · `.seg-btn` · `.cal-pick-btn` · `.orm-val` | 14–16 | `--fs-3` (16) | тело и заголовки строк |
| `.orm-name` · `.ppl-leg-*` · `.cal-pick-date` · `.sh-gain` · `.sc-scrub-tip b` | 12–13 | `--fs-2` (13) | вторичный текст |
| `.cal-day-hdr` 9 · `.cal-num` · `.orm-delta` · `.sc-peak` · `.sc-axis` 10 · `.sh-label` · `.orm-unit` · `.sc-unit` 11 | 9/10/11 | `--fs-1` (10) | «ниже `--fs-1` не спускаться» |

**Дубль `.stat-chip*` снят — заход №2 всё это время не работал.** Классы `.stat-row` / `.stat-chip` / `.stat-chip-val` / `.stat-chip-label` / `.stat-chip-unit` объявлены и в `dashboard.css`, и здесь, а `analytics.css` грузится позже — то есть чипы **Дашборда** рендерились правилами экрана Stats (20px/800), несмотря на перевод в заходе №2. Правила analytics загнаны в скоуп `#s-stats`; замер после: Дашборд 19/600 (своё, из `dashboard.css`), Stats 19/800 (своё). Отличия между экранами остались, но каждый наконец получает свои.

**Замечено попутно, не тронуто (кандидат на снос):** `.stats-segmented-ctrl` / `.seg-btn` и `.ppl-legend` / `.ppl-leg-*` не рендерятся нигде — в `js/` нет ни одного call-site, а у других экранов свои префиксные классы (`privacy-seg-btn`, `week-seg-btn`). Переведены всё равно: DoD требует ноль сырых значений в файле. Просится в ту же карточку на мёртвый CSS, что модалка рекомендаций из захода №2.

**Гейт:** 483/483 · lint 0 err / 19 warn (счёт прежний). Замер в превью на 375px: `scrollWidth == clientWidth` на обоих сегментах Stats и в модалке календаря; проверены hero индекса силы, карточки кривых со скраб-подсказкой, список 1RM, календарь и чипы обоих экранов.
- **Замер остатка (превью, 375px, 2026-07-30)** — сколько размеров мимо шкалы живёт на экране: Дашборд — 11/12/14/20/24 · Stats — 9/11/12/14/18/20/22/40 · Профиль — 11/15 · Журнал — чисто. `base.css` закрыт заходом №1; дальше `dashboard.css`, затем `analytics.css` — на Stats разброс самый большой, включая 40px и 9px. `.btn-text` оказался не в `base.css`, а в `workout.css` — подробности в заходе №1.
#### Заход №5 — `css/athlete-room.css` + инлайны `js/shared/athlete-room.js` ✅ (1.27.10)

30 объявлений в CSS и 6 инлайнов переведены; сырых `font-size`/цифровых `font-weight` в обоих файлах не осталось. Блок PP-3 (вкладка «Сила», `.ar-sc-*`) писался сразу на токенах и не трогался.

| Правило | Было | Стало | Почему эта ступень |
|---|---|---|---|
| `.ar-stat-val` (значение ячейки паспорта) | 28/800 | `--fs-6` (30) / `--fw-black` | hero-число в ячейке, та же роль, что `.pp-bento-val` в Профиле |
| `.ar-avatar-initials` · `.ar-name` | 26/800 · 22/800 | `--fs-5` (23) / `--fw-black` | спек `--fs-5` прямо называет обе роли — «имя в hero, инициалы аватара». Схлопывание двух ступеней в одну здесь задумано: инициалы это графика внутри круга 72px, а не ступень иерархии над именем |
| `.ar-name-input` 17 · `.ar-btn-save`/`.ar-btn-cancel` 14 · `.ar-header-title` 15 · `.ar-pr-name` 14 · инлайн-селект пола 16 | 14–17 | `--fs-3` (16) | тело, поля ввода и кнопки — тот же переход, что `.btn-primary`/`.ui-input` в заходе №1 |
| `.ar-crop-title` (титул модалки кропа) | 16/800 | `--fs-3` / `--fw-black` | 16 уже ровно `--fs-3`; кегль не двигал, модалка узкая (max-width 300) |
| `.ar-pr-val` · первое `.ar-stat-val` | 18/800 · 20/800 | `--fs-4` (19) | «значение в полосе метрик», как `.sh-stat b` в заходе №3 |
| `.ar-streak-mini` 12/**500** · `.ar-ach-status` 12 · кнопки фото 13 | 12–13 | `--fs-2` (13) / `--fw-md` | вторичный текст. Вес 500 упразднён спеком («500 и 600 глазом не различимы») |
| `#athlete-avatar-initials` 11 · `.ar-section-label` 11 · `.ar-editor-label` 11 · `.ar-level-badge` 11 · `.ar-ach-icon` 11 · `.ar-pr-rank`/`.ar-pr-unit` 11 · `.ar-ppl-meta` 11 · `.ar-crop-zoom-label` 11 · `.ar-stat-lbl` 10/11 · `.ar-ach-title` **9** · инлайн-единицы kg/cm/d 12 | 9/10/11/12 | `--fs-1` (10) | «ниже `--fs-1` не спускаться» |

**`.ar-ach-count` — единственное место, где менял не только токен.** Счётчик «3/8» стоит внутри `.ar-section-label` и был 12/600 при заголовке 11/600, то есть приписка перевешивала заголовок размером. Приведён к `--fs-1`/`--fw-black` — ровно приём `.btn-text` рядом с `.section-label` из захода №1: один кегль, разница весом и цветом. У правила стоит комментарий.

**Кросс-классовая коллизия внутри файла, не чинил.** `.ar-stat-val` и `.ar-stat-lbl` объявлены **дважды**: в блоке `.ar-stats` (20/10) и ниже в блоке `.ar-stats-grid` (28/11). Селекторы во втором блоке голые, поэтому побеждают и над живой разметкой — три ячейки паспорта всё это время рендерились 28/11, а не 20/10. Перевёл оба объявления честно (DoD — ноль сырых значений), у второго стоит комментарий с разбором. Чинить специфичность — вне TYPE-2, тот же вердикт, что по двум коллизиям захода №4.

**Замер (превью, 375px, RU, длинное имя «Георгиос Христианидис»).** Переполнения нет нигде: `scrollWidth == clientWidth == 375`, у `.ar-sheet` разница 0 на всех трёх вкладках. Шапка 16/700 · инициалы 23/800 · имя 23/800 в 343px без обрезки · значение ячейки 30/800, единица 10/800 · подпись 10/600 · ячейки 104×73 ровной сеткой · заголовок секции 10/600 · глиф достижения 10/800, подпись 10/600, статус 13/600. Редактор имени: лейбл 10/600, поле 16/600, кнопки 16/600 (142 и 144 в 294 доступных). Модалка кропа проверена синтетической вставкой разметки (живьём требует выбора файла): титул 16/800, лейбл масштаба 10/600, карточка 270×382 в 375. Вкладка «Сила» — скор 30/800, тир 13/800, полосы LIFT-1 живые (трек 155×4, точки 6×6).

**Гейт:** 547/547 · lint 0 err / 18 warn (счёт прежний).

**Замечено попутно → дописано в DEAD-1:** половина `athlete-room.css` не имеет потребителей. Проверено грепом по всему репозиторию с поправкой на склейку классов (урок DEAD-1): среди мёртвых склеек нет, а `ar-stat-editor` — единственный из подозреваемых, у кого потребитель нашёлся. Переведены всё равно, по прецеденту заходов №2 и №3.

- **ГДЕ СТОП:** один CSS-файл за заход (+ инлайны в его `js`-шаблонах, если экран рендерит типографику оттуда). Порядок по частоте показа: `base.css` ✅ → `dashboard.css` ✅ → `analytics.css` ✅ → `workout.css` ✅ → `athlete-room.css` ✅ → `privacy`/`summary`/`dynamic-island`/`claude` ✅ (заход №6, четыре мелких файла разом) → остаток: `intel.css` + инлайны в `js/`.
- **НЕ ТРОГАТЬ:** SVG-подписи радара (`js/.../hexagon-radar.js`) — `font-size` там в единицах viewBox, а не в CSS-пикселях, и от него зависит расчёт клэмпа подписи. Это не нарушение шкалы, а другая система координат.
- **DoD:** в переведённом файле ноль сырых `font-size` и цифровых `font-weight`; `npm test` зелёный; экран проверен в превью на 375px с длинными русскими строками.

#### Заход №4 — `css/workout.css` ✅ (1.27.7)

27 сырых объявлений `font-size` (плюс сопутствующие цифровые `font-weight`) переведены на `--fs-*`/`--fw-*`; в файле не осталось ни одного, кроме двух ранее токенизированных строк (`.btn-text` — хвост из захода №1, `.workout-block-name` — переведён отдельно, полевым решением Gio 2026-08-01). Инлайнов в `js/workout*.js` нет — экран целиком на CSS-классах.

| Правило | Было | Стало | Почему эта ступень |
|---|---|---|---|
| `.live-hero-val` (тоннаж в hero) | 38/800 | `--fs-6` (30) / `--fw-black` | hero-число, та же роль, что `.sh-score` в заходе №3 (40→30) |
| `.type-card-name` (PUSH/PULL/LEGS) | 26/800 | `--fs-5` (23) / `--fw-black` | тот же переход 26→23, что `.dash-hero-title`/`.empty-dash-title` в заходе №2 |
| `.stepper-val` (значение степпера, `!important`) · `.drum-item` (барабан веса/повторов) | 22/800 · 22/600 | `--fs-5` (23) / `--fw-black` · `--fw-md` | тот же переход 22→23, что `.ui-stepper-btn` в заходе №1 и `.sc-cur` в заходе №3 |
| `.program-name` · `.week-pill-val` · `.ex-num` · `.exercise-name` · `.add-ex-search` · `.btn-add-live-ex` · `.core-name` | 14–16 | `--fs-3` (16) | тело, значения строк, поле поиска |
| `.set-done-summary` · `.pill-filter` · `.add-ex-empty` · `.core-empty` | 12–13 | `--fs-2` (13) | вторичный текст |
| `.type-card-meta` 9 · `.program-type` 9 · `.program-dur` 11 · `.week-pill-lbl` 10 · `.live-hero-unit`/`.live-hero-chip` 10–11 · `.exercise-meta` 10 · `.ex-action-btn` 9 · `.set-col-label` 8 · `.set-num` 10 · `.add-set-btn` 11 · `.muscle-badge` 8 | 8/9/10/11 | `--fs-1` (10) | «ниже `--fs-1` не спускаться» |

**Оговорка — две кросс-классовые коллизии, обе не в этом заходе.** `.set-col-label` (кнопка «Weight kg», тумблер bodyweight) делит элемент с `.set-col-btn`, у которого `font: inherit` — шорткод сбрасывает размер/вес обратно к родителю независимо от значения в `.set-col-label`, и так было ещё на сырых px (8/800), порядок правил в файле не менялся. `.exercise-meta` на бейдже «0/3» (счётчик сетов) делит элемент с `.set-col-label`, который в файле объявлен позже — тоже побеждал и на сырых px. Обе коллизии — предсуществующая каскадная механика, DoD (перевод объявленных значений на токены) она не нарушает; чинить специфичность — вне карточки TYPE-2.

**Замер (превью, 375px, live-тренировка PUSH + модалка добавления упражнения + Core).** `scrollWidth == clientWidth == 375`, переполнения нет. Проверены вычисленные стили hero (тоннаж 30/800), карточки типа (26→23/800), степпера (22→23/800 с `!important`), строки упражнения (16/600), кнопки добавления сета/упражнения (10/600 · 16/600), Core-чеклиста (16/600) и модалки поиска (16/600 · 13/700).

**Гейт:** 546/546 · stylelint 0 err.

#### Заход №6 — «остальное»: `privacy.css` · `summary.css` · `dynamic-island.css` · `claude.css` ✅ (1.27.11)

Четыре файла одним заходом, потому что каждый мелкий (8–19 объявлений) и ни один не пересекается с другими картами. **60 сырых объявлений** переведены; сырых `font-size`/цифровых `font-weight` в этих файлах не осталось. Инлайнов в их `js` нет — все четыре экрана целиком на классах (проверено грепом по `js/`: остаточные инлайны живут в `intel.view.js`, `onboarding.js`, `workout.view/*`, `pip.js`, `utils.js`, `index.html` — это следующий заход).

| Правило | Было | Стало | Почему эта ступень |
|---|---|---|---|
| `.passport-num` (число в плитке паспорта данных) | 24/800 | `--fs-5` (23) / `--fw-black` | тот же переход 24→23, что `.type-card-name` в заходе №4 |
| `.summ-title` · `.summ-stat-val` · `.top-lift-val` | 18–20 | `--fs-4` (19) | заголовок секции / значение |
| `.claude-title` 17 · `.coach-rec-type` 16 · `.ai-text` 14 · `.chat-input` 14 · `.island-time` 14 | 14–17 | `--fs-3` (16) | тело, поля ввода — тот же переход, что `.btn-primary`/`.ui-input` в заходе №1 |
| `.privacy-desc` · `.audit-url` · `.coach-msg-text` · `.chat-msg` · `.ai-error` · `.summ-ex-name` · `.summ-pr-name`/`.summ-pr-val` · `.island-name-collapsed` · `.island-ex-name` · `.island-finish-btn` | 12–13 | `--fs-2` (13) | вторичный текст и строки списков |
| `.privacy-seg-btn` 11 · `.claude-sub` 11 · `.legend-*` 10–11 · `.next-ex-*` 11 · `.top-lift-rank` 10 · `.passport-lbl`/`.passport-section-label` 10 · `.audit-meta`/`.audit-status` 10 · `.summ-subtitle`/`.cpill-label`/`.cpill-chip` 10 · `.island-sets-collapsed` 12 · `.island-min-*` 10–11 · `.heat-scale-labels` 9 · `.island-rest-next`/`.island-sublabel` 9 · **`.summ-stat-lbl` 8** · **`.summ-ex-ui-tag` 7** | 7–12 | `--fs-1` (10) | «ниже `--fs-1` не спускаться» — подтягиваем вверх |

**Три решения сверх механической замены.**
1. **`.island.profile-minimal.expanded .island-sublabel` (8px) — переопределение снято целиком.** Оно существовало ради одного состояния и уводило кегль на ступень ниже нижней. Приглушение в этом состоянии и так делает `opacity: 0.75`, которая осталась.
2. **Пары «ключ/значение», разъехавшиеся на 1px, сведены к одному кеглю.** `.passport-row-key`/`.passport-row-val` были 12/11, `.summ-ex-name`/`.summ-ex-meta` — 13/12. Промежуточной ступени в шкале нет, а разница в 1px и не читалась; различают их теперь цвет (`--c-text-2` против `--c-text-3`) и вес. Тот же приём, что `.ar-ach-count` в заходе №5 и `.btn-text` в заходе №1.
3. **Самый крупный подъём — `.summ-ex-ui-tag` 7→10 и `.summ-stat-lbl` 8→10** — оба проверены замером, см. ниже. Тег «UI» инлайновый, он и на 10px занимает 22px и никуда не переносится; подпись «ПОВТОРЫ» — самая длинная из трёх RU-подписей.

**Замер (превью, 375px, RU, реальная разметка через импорт модулей).** Горизонтального переполнения документа нет ни на одном экране (`scrollWidth - 375 == 0` везде).
- **Приватность** — карточка режимов, паспорт данных, журнал доступа. Сегмент-контрол в RU («Облако · Аноним · Без сети»): три кнопки по 108px, `scrollWidth == clientWidth`, переносов нет. Паспорт: число 23/800, подпись 10/600, строка ключ/значение 13.
- **Итог тренировки** — модалка с двумя блоками, PR-секцией и бамбуковым счётом. Критичная подпись **«ПОВТОРЫ»: 47px при 8px → 59px при 10px в ячейке 96px, запас 37px.** Единственное превышение `scrollWidth` во всей модалке — `.summ-pr-name` с `text-overflow: ellipsis`, то есть штатное усечение длинного имени; кегль у него не менялся (13→13), так что дельты нет.
- **Остров** — живая тренировка PUSH, состояния collapsed / expanded / timer-mode. Превышения `scrollWidth` только у элементов с ellipsis (`.island-name-collapsed`, `.island-min-label`), и у обоих кегль не менялся (13→13, 10→10). Пилюля: 120px collapsed, 174px expanded, 290px timer.
- **AI-панель** — единственный экран, который не поднялся живьём: приватность по умолчанию `airgap`, ключа нет, FAB скрыт. Мерил на реальной разметке панели, вставленной в живой документ (та же техника, что кроп-модалка в заходе №5). Все 19 объявлений применились, переполнений сверх ellipsis нет.

**Найдено замером, заведено в DEAD-1, не чинил** (подробности — `HANDOFF_next_cards.md`): `.summ-island-label`/`.summ-chip`/`.summ-island-dot`/`.summ-island-chips` мертвы — шапку блока рисует chamber-pill; `.island-time` мертва иначе и интереснее — разметка есть, таймер её обновляет, но `.island-readout .island-time { display: none; }` гасит её безусловно во всех состояниях острова.

**Гейт:** 547/547 · eslint 0 err · stylelint 0 err / 18 warn (все — сырые цвета, преэкзистинг).

**Осталось по TYPE-2:** `intel.css` (8 сырых размеров / 7 весов) + инлайны в `js/intel.view.js`, `js/onboarding.js`, `js/workout.view/*`, `js/features/pip.js`, `js/shared/utils.js`, `index.html`. Отдельно `base.css:1487-1488` (`.ppl-g-total`/`.ppl-g-cap`) — **не трогать вслепую:** это `<text>` в системе координат viewBox, узаконенное исключение захода №1; там же предупреждение о конфликте с NAV-BACK-1 по файлу.

### SPACE-1 · Шкала отступов ✅ ЗАКРЫТА (1.27.68, заход №6) — ревизия радиусов уехала в SCALE-1

Шесть заходов: `css/profile.css` (1.25.56) · `css/dashboard.css` (1.27.14) · `css/workout.css` (1.27.15) · `css/analytics.css` (1.27.32) · хвост инлайнов `js/` — `intel.view.js` + `onboarding.js` (1.27.67) и остаток из 11 файлов (1.27.68). Сырых `padding`/`margin`/`gap` вне узаконенных исключений не осталось ни в `css/`, ни в `js/`, ни в `index.html`. Разбор каждого захода — в CHANGELOG по версиям.

**Три правила, которые переживают карточку** — нужны SCALE-1, DEAD-1 и любому новому коду:

1. **Ничьи (6 · 7 · 10 · 14 · 20px) решаются направлением, а не округлением.** ВВЕРХ — если элемент нажимается и от роста ничего не переезжает. ВНИЗ — если рост ломает отношение внутри самого правила: лок-ап «число + единица», эллипсис имени на 375px, зазор в многоколоночной сетке (он отнимается у колонки N раз), намеренная асимметрия инсета. Полная формулировка — в шапке `css/workout.css`.
2. **Значения ниже нижней ступени (`--sp-0-5` = 4px) узаконены.** Микро-сдвиги 1–3px служат оптическому выравниванию строки, а не ритму блоков; поднять их до 4px = разорвать лок-ап. У каждого стоит комментарий в коде или в шапке файла.
3. **Клиренс — не ритм.** `72px`/`120px` у `.screen`, `120px` у штампа версии, `44px` под кнопку показа ключа, `var(--side-padding)` = 20px как гуттер экрана — осознанно вне шкалы, с комментариями. Не «дотягивать» до ступени.

**НЕ ТРОГАТЬ:** `--sp-island` (32px, off-grid 8×2.5) и оверрайды `--sp-2`/`--sp-3` в `@media (max-width: 359px)` — ритм там осознанный. Карточка profile-hero-spacing вобрана и закрыта заходом №1: вопрос трёх ритмов снят, было 16 · 32 · 24 между крупными блоками — стал один `--sp-3`.

**Не входило, ждёт своей карточки:** `js/workout.view/handlers.js` (6 объявлений) — файл держал открытый PR#211, брать после его мёржа. Узаконены навсегда: `js/features/pip.js` (чужой документ, токены не доезжают) и `js/ui/receipt.js` (canvas-растр).

**Ревизия радиусов из этой карточки НЕ сделана и переезжает к SCALE-1.** Сокращение 8 ступеней (`--r-xs` … `--r-pill`) до четырёх различимых — это правка таблицы токенов в `base.css`, то есть один заход по всем экранам сразу, а не по файлу. Внутри уже переведённых файлов радиусы и так на токенах.

**Наследство для DEAD-1** (найдено попутно, снос — не работа карточки единиц): `.screen` объявлен дважды с разными числами — инлайн в `index.html` даёт 72/16/120, `base.css:882` даёт 80/20/120, выигрывает второй, то есть первый кадр до загрузки CSS встаёт не туда; `openExercisePickerModal` и `openReplaceExModal` в `js/workout.view/modals.js` — сто строк почти байт-в-байт, каждая правка делается дважды; `js/onboarding.js` несёт собственную копию `dobSelectsHtml` вместо общего хелпера из `shared/utils.js`; осиротевшие правила `.stats-segmented-ctrl` / `.ppl-dist-card` / `.ppl-legend` переведены на шкалу, но не снесены.

**Мелочь, найденная замерами по пути** (каждая — на одну строку работы, ни одна не про отступы): `.muscle-badge` с длинным названием мышцы вылезает за правый край `.exercise-name` и режется его `overflow: hidden` (правый край на 389 при вьюпорте 375); правила `.seg-btn` в `analytics.css` не заскоплены на `#s-stats`, хотя живая разметка `.seg-btn` есть ещё на трёх экранах — тот же класс протечки, что заход №2 чинил у `.stat-chip*`; сетка Wins/Leaks в отчёте Intel переполняет себя на любом инсете (`<li>` с `min-width: auto` плюс длинные русские слова); на дашборде заголовок «TOP LIFTS» и подпись «Estimated 1RM» встают в одну строку внахлёст — это `.section-header` в `base.css`.


### LIFT-1 · Полосы и точки тиров не рендерятся ✅ ЗАКРЫТА (1.27.9) — Gio выбрал A

**Симптом:** в блоке «Силовые показатели» (низ `s-profile` и вкладка «Сила» в Athlete Room) между названием подъёма и весом пусто. Замер в превью на 375px, 4 строки: `.lift-bar-track` 147×**0**, `.lift-bar-fill` 147×**0** (фон инлайном живой), `.lift-dot` **0×0**.

**Корень (не гипотеза — проверено):** `js/profile.view/lift-bars.js` эмитит `.lift-bar-track` > `.lift-bar-fill` и пять `.lift-dot` внутри `.lift-dots-v2`, а CSS-правил для этих трёх классов в репозитории нет **нигде** — удалены коммитом `9529de8` («feat(profile): modern onboarding and athlete room v2», 2026-06-06) вместе со старой разметкой `.lift-row`/`.lift-bar-wrap`. Остались только флекс-контейнеры `.lift-bar-wrap-v2` и `.lift-dots-v2` (`css/profile.css:890`, `:897`), поэтому дети схлопнуты в ноль. Проверка: `grep -rn "lift-bar-track\|lift-bar-fill\|lift-dot\b" css/ js/` — все попадания в JS, ни одного в CSS. Мёртвый хвост того же коммита: `@keyframes bar-grow` (`profile.css:951`) и стаггер `.lift-row:nth-child(*)` (`:942`) — селектор `.lift-row` без `-v2` тоже больше не существует, как и `.lift-row-v2:last-child { border-bottom: none }` при отсутствующем бордере.

**Развилка закрыта: Gio выбрал A** (мокапы «сейчас / A / B» в натуральную величину на 375px, 2026-08-04). Что сделано в 1.27.9 — ниже в описании варианта A плюс два хвоста того же коммита: стаггер строк переведён с несуществующего `.lift-row` на `.lift-row-v2`, удалён `.lift-row-v2:last-child { border-bottom: none }` при отсутствующем бордере. Замер после: трек 147×4, заливка 40% ширины, точки 6×6 в обоих состояниях. Вариант B оставлен в карточке как разобранная альтернатива — если блок когда-нибудь пойдёт под редизайн, список мёртвой разметки уже собран.

**Варианты, как они были показаны:**

- **A — вернуть.** Восстановить правила из `git show 9529de8^:css/profile.css` (строки 584–624) и перевести на текущие токены: трек 4px `--c-bg-3` / `--r-xs`-класса нет для 2px, радиус оставить `2px` как оптический, точки 6px, дырка — `--c-bg-3` + `var(--c-border)`, гэпы через `--sp-*`. `@keyframes bar-grow` и инлайновые `--bar-w`/`--bar-delay` оживают без правки JS. Оговорка по цвету: `TIER_COLOR` в `lift-bars.js` — это `--c-text-1/2/3`, то есть полосы выйдут **монохромными** (серый → белый), а не акцентными; это соответствует палитре (акцент только BRAND), но выглядит скромнее, чем ожидается от «шкалы».
- **B — вычистить.** Убрать из `lift-bars.js` `.lift-bar-track`, `.lift-bar-fill`, `.lift-dots-v2`, `.lift-dot`, вычисление `pct`, `barDelay` и `tierIdx`. `TIER_ORDER` после этого в файле мёртв целиком (его единственные потребители — `pct` и точки), `TIER_COLOR` остаётся ради подписи тира. Строка становится «имя + тир слева, вес + перцентиль справа». Тогда же удалить `@keyframes bar-grow`, `.lift-row:nth-child(*)`, `.lift-row-v2:last-child` и `.lift-dots-v2` из `profile.css`.

**Замечание к SPACE-1:** жалоба Gio «шкала прижата к краю» (заход №1) чинилась на `.pp-lifts-v2` — и это была верная находка, но «шкала» в буквальном смысле уже два месяца была невидимой. Обе карточки трогают один блок, LIFT-1 брать после SPACE-1.

- **ЦЕЛЬ:** блок «Силовые показатели» выглядит одинаково задуманно на обоих экранах — либо со шкалой (A), либо без мёртвой разметки (B).
- **ГДЕ СТОП:** один заход, `css/profile.css` + `js/profile.view/lift-bars.js`. Athlete Room отдельной правки не требует — он переиспользует `renderLiftBars` (`js/shared/athlete-room.js:493`).
- **НЕ ТРОГАТЬ:** `exrxTier` и `strength-engine.js` — расчёт тиров корректен, ломается только отображение. `TIER_COLOR` не переводить на акцент без отдельного решения (правило палитры: акцент — только BRAND).
- **DoD:** замер в превью на 375px даёт ненулевую высоту у полос и точек (A) либо в DOM нет ни одного `.lift-bar-*`/`.lift-dot` (B); `npm test` зелёный; в `css/profile.css` не остаётся селекторов без разметки.

### PP-4 · Ритм и иерархия hero профиля ✅ ЗАКРЫТА (1.25.34)
Частный случай TYPE-1 + SPACE-1, но с конкретным экраном и известным симптомом. Правка целиком в `css/profile.css` — разметка `passport-hero.js` не понадобилась.

**Замер на 375px до правки (длинное русское имя, тир «Средний · 360», «Топ 43%»):**

| Что | Было | Стало |
|---|---|---|
| Левый край hero (аватар) | **28px** | **20px** — как у полосы метрик, бенто и радара |
| Высота пилюли тира | **37px** — текст ломался внутри пилюли на две строки | **23px**, одна строка |
| Полоса пилюль | 158px в 158px доступных — ноль запаса | 190px доступно, пилюли рядом, запас 8px |
| Высота hero | 171px | 157px |

**Что сделано:**
- `padding: var(--sp-3) var(--sp-1)` → `var(--sp-3) 0`. Боковые 8px были третьим ритмом: `.screen` ставит контент на 20px, hero — на 28px. Комментарий у правила объясняет, почему ноль осознанный.
- Хардкоды в шкалу: `margin-bottom` имени `4px` → `--sp-0-5`, мета `12px` → `--sp-1-5`, gap пилюль `8px` → `--sp-1`, padding обеих пилюль `4px 12px` → `var(--sp-0-5) var(--sp-1)`.
- Удушение `.pp-badge-row` лечится двумя правилами: `white-space: nowrap` на пилюлях (пилюля не ломается внутри себя — это и давало 37px) + `flex-wrap: wrap` на строке (не влезли — переносятся целиком). Плюс gap hero `--sp-3` → `--sp-2`: идентити получила 190px вместо 158px.

**Стресс-проверено:** самый длинный тир («Продвинутый · 470» + «Топ 1%») пилюли не влезают в строку и переносятся на вторую — hero растёт до 189px, горизонтального переполнения нет (`scrollWidth == clientWidth`). Это и есть задуманная деградация: ломаться внутри пилюли больше нечему.

**Гейт:** 390/390 · lint 0 err / 28 warn (счёт warn прежний). **Скриншот снять не удалось** — превью-панель скрыта, страница не композитит кадры (известная ловушка, см. разбор TYPE-1); проверка инструментальная, `getBoundingClientRect`. Полевой взгляд — карточка ПОЛЕ-1.

**НЕ тронуто осознанно:** `margin-bottom: 2px` у `.pp-goal-lbl-v2` (ниже нижней ступени шкалы, микро-отступ) и `border-radius: 28px` у `.pp-avatar-ring` — это ревизия радиусов, карточка SPACE-1.

### ПОЛЕ-1 · Один заход на телефоне ✅ ЗАКРЫТА (2026-07-30, ответы Gio)

| Вопрос | Вердикт |
|---|---|
| Движение после ANIM-1/ANIM-2 — не резко ли | **норм**, менять нечего |
| Ре-рендер PP-6 — прыгает ли экран на тумблерах | **прыжка не было** — замер и глаз сошлись |
| Типографика TYPE-1 — читается ли в зале | **«читается хорошо»**, но **«большие буквы кажутся»** → карточка TYPE-1b ниже |

Карточка окупилась ровно тем, ради чего заводилась: два вердикта закрыли месячные хвосты без работы, третий поймал ошибку анкера **до** того, как TYPE-2 растиражировала бы её на 284 объявления. Полевой заход стоит минуту и снимает вопросы, которые из репозитория неразрешимы в принципе.

<details><summary>исходная карточка</summary>

Три вопроса ниже висят месяц не потому, что сложные, а потому, что ни один нельзя закрыть из репозитория. Все три видны на одном экране за одну минуту — поэтому одна карточка, а не три.

- **ЦЕЛЬ:** открыть на телефоне свежий прод и ответить «ок / не ок» по трём пунктам. Версия — та, что на проде в момент захода; сверить в профайл-меню.
- **ЧТО СМОТРЕТЬ:**
  1. **Движение (ANIM-1/ANIM-2, 1.25.26-1.25.27).** Открыть любую шторку/тост и понажимать кнопки: не стало ли резко. Вся отклик-анимация теперь ≤300ms, глубина нажатия одна на всё приложение. Вопрос ровно один: не слишком ли быстро.
  2. **Типографика (TYPE-1, 1.25.32).** Экран Профиль: шкала утверждена по мокапам на десктопе, на живом Android её никто не видел. Смотреть на имя, подписи под числами и капс-пилюли — читается ли в зале, на весу.
  3. **Ре-рендер (PP-6, 1.25.33).** В Профиле подряд подёргать тумблеры настроек: экран не должен прыгать и не должен уезжать скролл. В замерах прыжок 0px, но замер — не глаз.
- **БОНУСОМ, если на проде уже 1.25.34:** hero Профиля выровнен по левому краю с полосой метрик и бенто (PP-4) — раньше был на 8px правее. Вопрос: стало ли ровно.
- **ГДЕ СТОП:** три ответа «ок / не ок» + что резануло. Правки — отдельными карточками, не в этом заходе.
- **ПОЧЕМУ карточкой, а не строчкой в вопросах:** пункты 3 и 4 из «Открытых вопросов к Gio» висят с 27 июля; PP-6 добавил третий. Пока они висят, TYPE-2 (тираж шкалы на все экраны) тиражирует непроверенное в поле решение.

</details>

### TYPE-1b · Правка анкера по полю — верх шкалы опущен ✅ ЗАКРЫТА (1.25.39)
Полевой ответ Gio по 1.25.38: низ шкалы «читается хорошо», но «большие буквы кажутся». Разбор: две реплики не противоречат друг другу — тело (16px) смотрят в зале на весу, и оно вердикт получило; крупным читался верх, где имя 26 и hero-числа 34 занимали половину экрана, неся меньше смысла, чем строки под ними.

**Решение принято мокапом, не числами** (тот же приём, что в TYPE-1): три варианта Профиля в натуральную величину — A (текущий 20/26/34), B (вся шкала вниз, 9/12/15/19/24/32), C (вниз только верх, тело сохранено). Gio выбрал **C**.

| Ступень | Было | Стало |
|---|---|---|
| `--fs-1` … `--fs-3` | 10 / 13 / 16 | **не тронуты** — вердикт «читается хорошо» относился к ним |
| `--fs-4` | 20 | **19** |
| `--fs-5` | 26 | **23** |
| `--fs-6` | 34 | **30** |

**Шаг перестал быть ровным 1.25 — теперь 1.19–1.30, и это осознанно.** Прежнее правило «соседние разведены на ≥25%» переформулировано в `CLAUDE.md` и в комментарии `base.css`: верхние ступени на 375px не нуждаются в прежнем размахе, а различаются они ещё весом и цветом, не только кеглем.

**Заодно переведены `.screen-title` (26px → `--fs-5`) и `.screen-sub` (14px → `--fs-2`)** — те самые два объявления в `base.css`, которые TYPE-1 осознанно не трогала. После понижения верха они стали бы крупнее имени в hero, то есть заголовок экрана перевешивал бы главный элемент. Это первый кусок TYPE-2, взятый вынужденно.

**Замер в превью на 375px** (Профиль, Журнал, Дашборд, Stats): имя 23 · метрика 19 · hero-число 30 · заголовок экрана 23 · подзаголовок 13; горизонтального переполнения нет ни на одном экране; ритм hero (левый край 20px) не поехал, пилюли по-прежнему одной строкой.

### PP-6 · Точечный ре-рендер профиля ✅ ЗАКРЫТА (1.25.33, PR#68)
Тумблеры переведены на скоуп-рефрешеры по образцу `privacy.view.js:_setMode`: `_refreshSettings()` (только `#profile-settings-block`), `_refreshPassport()` (dedup — меняет тренировки, не настройки), `_refreshLangDependent()` (язык правит текст всюду, но на месте). `load()` остался за входом на экран и за импортом бэкапа — тот может переписать всё, редкое тяжёлое действие.

**Заплатка PP-5 снята целиком** — и `min-height: 620px`, и `overflow-anchor: none`. DoD закрыт замером, а не на слово:

| Сборка | Результат на тумблере |
|---|---|
| `main` + заплатка | прыжка нет — заплатка работала |
| `main` **без** заплатки | 1600 → 2552 (**+952px**) — исходный баг |
| PP-6 **без** заплатки | 7 тумблеров подряд, 1600 неизменно, паспорт не пересоздаётся |

**Находка, объясняющая, почему PP-5 лечила лишь наполовину:** она сохраняла и возвращала `scrollTop` у `document.scrollingElement`, а прокручивается `#s-profile` (у `html` стоит `overflow: hidden`). Восстановление было no-op — работал только резерв высоты. Урок общий: перед починкой скролла проверять, какой элемент реально скроллер.

**Осознанное исключение:** ключи AI (`setGeminiKey`/`setAnthropicKey`) блок НЕ перерисовывают — они срабатывают на `blur`, и замена разметки между `mousedown` и `click` съела бы тап, который этот `blur` и вызвал. Им хватает `_patchAiStatus` на месте.

**Замечено попутно → ✅ уже удалён** (чистка PROF-1, 1.25.35; сверено грепом 2026-08-01 — в коде его нет): `toggleReminder` был определён в `js/profile.js` и экспортирован, но ни один элемент UI его не вызывал. Заново не искать.

### PP-3 · Разбор скора ✅ ЗАКРЫТА (1.25.55) — вместе с PCT-1

Выкачено целиком за один заход, как и решил Gio. Вход — обе пилюли в hero профиля («Средний · 313 СКОР» и «Топ 52%»), обе ведут на новую вкладку **«Сила»** в Athlete Room.

**Что на вкладке:**
- скор крупно + тир + шкала 0–500 с рисками на порогах 200/300/380/470 и меткой позиции;
- «До тира «Продвинутый» осталось **+96 кг** в сумме трёх» — считается обратным ходом от порога;
- разложение формулы построчно: `DOTS 307` (445 кг суммы при весе 80) × возраст × рычаги × опыт = `313`. У каждого множителя стоит причина, почему он такой («39 лет — пиковый возраст», «BMI 25.2 — рычаги без надбавки», «2 г. опыта — ещё новичок»); множители ×1.00 приглушены — они есть, но на скор не повлияли;
- **PCT-1 закрыта здесь же:** блок «Откуда «Топ»» — лучшее из четырёх движений (жим 110 кг), сравнение по таблицам ExRx для пола/веса/возраста, **не** с другими пользователями приложения. Именно это в карточке было главным непониманием;
- вклад упражнений — переиспользован `renderLiftBars`, как и планировалось.

**Ячейка DOTS из бенто убрана**, бенто стал `repeat(3, 1fr)` — три ячейки в ряд (серия · тоннаж · часы), и все три теперь про объём работы, а не про силу. Замер на 375px: ячейка 96×92, четырёхзначный тоннаж (`1284`) на `--fs-6` не переполняет, подписи «дней подряд» / «тренировок» влезают.

**Три расхождения, найденные по дороге — чинились как часть карточки, потому что все три давали разные цифры на двух экранах:**

1. **Маппинг «имя упражнения → движение» существовал в двух версиях.** В `profile.view.js` — список регулярок с OHP, в `athlete-room.js` — три самодельных `.match()` без OHP. Сведены в `js/shared/lift-map.js` (`mapOneRMs` + `powerTotal`), заодно добавлены русские названия.
2. **Возраст считался по-разному.** В паспорте — `computeAge` (с учётом дня рождения), в комнате — вычитанием годов. Разный возраст → разный множитель → разный скор у одного атлета. Комната переведена на `computeAge`.
3. **Пороги тиров были скопированы в три места.** Теперь единственный источник — `SCORE_TIERS` + `tierFromScore` в `strength-engine.js`; копии в `passport-hero.js` и `athlete-room.js` удалены.

Плюс сборка контекста комнаты (`_buildContext`) была продублирована в `render()` и `switchTab()` — именно поэтому копии и разъехались. Теперь одна.

**Движок:** `scoreBreakdown()` в `strength-engine.js` — то же вычисление, что `athleteProScore`, но с открытыми множителями. `kgToNext` считается аналитически (DOTS линеен по сумме трёх) и **доводится проверкой по настоящей формуле**: прикидка ошибается на ±1 кг, потому что округляются и DOTS, и итоговый скор — без доводки подпись «осталось +47 кг» приводила к 199 при обещанных 200. Гард — тест, который проверяет, что показанные килограммы реально переводят через порог, а на килограмм меньше — ещё нет.

**Гейт:** 483/483 (7 новых тестов на разбор) · lint 0 err / 6 warn в тронутых файлах (счёт прежний). Проверено в превью на 375px: переполнений нет ни на профиле, ни на вкладке; три состояния — пустое (нет ни одного 1RM), обычное, «Элита» (следующего тира нет, килограммы не обещаются); переключение вкладок работает и когда контекст передан из `render()`, и когда собирается заново.

**Открытый вопрос к Gio (решается картинкой, работа не блокируется).** Слово «СКОР» в пилюле стоит денег: пилюля выросла 105 → 142px, и «Топ 52%» перестал влезать в строку — badge-row переносится, hero растёт 151 → 183px. Это ровно та однострочность, за которую боролась PP-4. Варианты: **A** — как сейчас (слово есть, две строки), **B** — без слова (одна строка, но цифра снова без подписи). Мокап приложен к отчёту сессии.

<details><summary>исходная карточка</summary>

**Решения Gio:** (1) ячейку «DOTS» из бенто **убрать** — тем же заходом, что выкатывает разбор («держать раздельно нет смысла, только путает»); (2) делать **целиком за один заход**, не дробить.

- **Что известно:** число в чипе (напр. 71) = `athleteProScore` = DOTS 69 × 1.02 (опыт <3 лет) × 1.004 (рычаги, BMI<25). Шкала тиров: 200/300/380/470. Рядом в бенто висит «69 DOTS» — производное и итог как два независимых показателя, отсюда «что это за цифра».
- **ЦЕЛЬ:** слово `SCORE` в чипе → тап открывает вкладку «Сила» в Athlete Room (таб-бар уже есть, `athlete-room.js:191`; там же соседняя вкладка с весом/ростом/возрастом, влияющими на скор) → внутри: шкала 0–500 с меткой, разложение формулы, «сколько до следующего тира» в килограммах, вклад упражнений (переиспользовать `renderLiftBars` + `exrxTier`). В том же заходе — снос ячейки DOTS из бенто.
- **ПОЧЕМУ целиком:** данные уже все лежат готовые, дробить нечего; а при разбивке возникнет дыра — DOTS из бенто уже пропал, а полного объяснения скора ещё нет.
- **Hover-подсказка** — только десктопный бонус: на телефоне hover нет. Если делаем — по совету 3: первая с задержкой, соседние мгновенно.
- **СВОДИТСЯ С:** PCT-1 («Top 50%» без объяснения, `HANDOFF_next_cards.md`) — тот же путь в ту же вкладку «Сила», одна работа.
- **ПОРЯДОК:** PP-3 берётся **ДО** SPACE-1. Иначе SPACE-1 причешет отступы в бенто, а PP-3 следом снесёт оттуда ячейку DOTS — часть работы в мусор. Сначала состав экрана, потом ритм по финальному составу.

</details>

#### Заход №7 — остаток TYPE-2: `intel.css` + инлайны в `js/` ✅ ЗАКРЫТ (1.27.12)

95 объявлений переведены тремя коммитами по разведанному плану: `intel.css` (15) + `js/intel.view.js` (28) вместе — один экран; `js/onboarding.js` (27) отдельно; хвост из пяти файлов (`js/workout.view/modals.js` 6, `index.html` 6, `js/workout.view/handlers.js` 4, `js/supabase-check.js` 3, `js/shared/utils.js` 1) одним коммитом. `js/features/pip.js` не тронут — легализован комментарием в коде (ловушка №1 из разведки подтвердилась: чужой документ, токены не резолвятся).

**Замер (превью, 375px).** Главный экран: `scrollWidth == clientWidth`, первый кадр (`.status-logo-text`/`.logo-w1`/`#status-time` из блока в `index.html` до `base.css`) даёт 16/800 и 10/700 после устаканивания — ошибок консоли нет. Intel-экран поднят живьём импортом модуля + довеской ленивого `css/intel.css` (`js/shared/lazy-css.js` не подгружает его без захода на экран) — заголовок 23/800, подзаголовок 10/600, командная строка 16, заголовок логов 10, переполнений 0. Онбординг поднят через `showOnboarding()` — h1 30/800, параграф 16/600, документный `scrollWidth` без переполнения (4px на внутренних `div` — субпиксельное округление flex-паддинга, не текстовый overflow).

**Гоча подтверждена:** `intel.css` и часть инлайнов `js/onboarding.js` рендерятся через ленивую загрузку CSS — при измерении напрямую через `import()` модуля стили не подхватываются, пока явно не довесить `<link>`; итоговые цифры сняты только после этого шага.

**Гейт:** 547/547 · lint 0 err / 18 warn (счёт прежний). TYPE-2 закрыта целиком, следующая — SCALE-1 (в тот момент называлась GUARD-1).

<details>
<summary>Разведка захода №7 (архив — задача выполнена, план ниже уже неактуален как TODO)</summary>

- **ОБЪЁМ (снят 2026-08-05 на `78f8209`):** `css/intel.css` — **15 объявлений** (8 размеров: 26 · 15 · 15 · 11 · 11 · 11 · 10 · 10, 7 весов). Инлайны — **80 вхождений** в восьми местах: `js/intel.view.js` 28 · `js/onboarding.js` 27 · `js/workout.view/modals.js` 6 · `index.html` 6 · `js/features/pip.js` 5 · `js/workout.view/handlers.js` 4 · `js/supabase-check.js` 3 · `js/shared/utils.js` 1.
- **ПОРЯДОК:** `intel.css` + `js/intel.view.js` вместе — один экран. Затем `onboarding.js` отдельным коммитом. Остальные пять — хвостом.

**Три ловушки, подтвердились все три:**
1. **`js/features/pip.js` — исключение, не переводить.** `_setupPiPWindow()` строит `<style>` в чужом документе (`documentPictureInPicture`), `:root` туда не доезжает. Легализовано комментарием в коде.
2. **`index.html` — блок `<style>` до `<link href="css/base.css">`.** Перевод допустим (цвета уже на токенах — прецедент принят), но мерить нужно первый кадр загрузки, а не устоявшийся экран.
3. **Часть инлайнов — в шаблонных строках, не глазами.** `js/supabase-check.js:132`, `js/shared/utils.js:103`.

**Два приёма (шесть+ заходов):** ниже 10px поднимать до `--fs-1`; пары «ключ/значение», разъехавшиеся на 1px, сводить к одному кеглю.

**Гоча окружения:** worktree без `node_modules` → `npm run lint` молча пропускает stylelint. Первое действие в новом worktree — `npm install`.

</details>

### CSS-1 · `*/` в комментарии съедал правило ✅ ЗАКРЫТА (1.27.14)

Заведена и закрыта по жалобе Gio 2026-08-05: «во вкладке «Сила» подпись прижата к шкале — air и иерархия не действуют; иерархия работает нестабильно и не везде, нужен контроль».

**Жалоба была права дважды: дефект настоящий, и он не единственный.** Но причина — не отступы и не вкус. Комментарий-шапка блока PP-3 нёс текст `--fs-*/--fw-*/--sp-*`, где `*/` закрывает комментарий досрочно; хвост комментария парсер прочитал как начало селектора и выбросил следующее правило целиком.

| Файл | Потерянное правило | Что было на экране |
|---|---|---|
| `athlete-room.css:946` | `.ar-sc-head { padding: var(--sp-2) 0 var(--sp-3) }` | зазор «Скор атлета → трек шкалы» **0px** вместо 24 |
| `journal.css:7` | `.jr-header { display: flex; gap: var(--sp-1-5) }` | `display: block`: кнопка «назад» **над** заголовком, шапка 87.6px вместо 51.6 |

Второе место найдено грепом `-\*/` по всем `css/` — то есть «не везде работает» из жалобы объяснилось полностью, третьего такого места в репозитории нет.

**Ответ на «нужен контроль» — `test/css-parse-guard.test.js`.** Гард сверяет, что каждое объявленное правило доезжает до парсера: повторяет разбор браузера (комментарий закрывается на первом `*/`) и сравнивает объявленные селекторы с прелюдиями, которые реально получатся. Плюс второй тест — текст комментария (кириллица) в селекторе. Проверен возвратом бага: оба краснеют. Ловит и незакрытую скобку, и лишнюю `}` — любую потерю правила, не только этот паттерн.

**Урок про гейты, в копилку к «зелёный гейт отвечает только на свой вопрос»:** stylelint на обоих файлах давал 0 errors — мусор перед `{` он разбирает как селектор и не жалуется. То есть дефект вёрстки, живший месяцами, читался глазом как «дизайн-система работает нестабильно», а инструментально не читался никак. Отсюда правило: **симптом «иерархия не действует в одном месте» сперва проверять замером `getComputedStyle`, а не правкой отступов** — если объявленное значение не совпало с вычисленным, чинить нужно не значение.

### CSS-2 · Кросс-файловые дубли селекторов ✅ ЗАКРЫТА (1.27.17)

Продолжение CSS-1: девять селекторов были объявлены в двух файлах сразу, и тот, что грузится позже, молча перебивал более ранний. Прецедент — `.stat-chip-val` (TYPE-2 заход №3), где чипы Дашборда почти год стилизовались файлом экрана Stats.

**Каждый случай разобран замером `getComputedStyle` на 375px до и после — «объявлено X, применяется Y».**

| Селектор | Вердикт | Объявлено | Применялось |
|---|---|---|---|
| `.orm-*` (analytics + base) | **дефект, перенесён в base.css** | на Дашборде — ничего | компонент голый до первого захода в Stats |
| `.week-pill` (base + workout) | **дефект, сведён в workout.css** | workout: полная пилюля | `position:absolute` продолжал ехать из base |
| `.streak-count` (base + dashboard) | **дефект, удалён из base** | dashboard: золото/600 | + янтарный нимб `rgba(255,179,0,.35)` из base |
| `.chat-send-btn` (base + claude) | **дефект, удалён из base** | claude: плоский `--c-secondary` ×3 | фиолетовый градиент base через `!important` |
| `.stat-chip` (base + dashboard) | **дефект в радиусе** | dashboard `--r-m` 14px, analytics `--r-xl` 22px | **18px** — оба мертвы, бьёт AIR Tier 1 |
| `#status-bar` (base + athlete-room) | законный | — | пересечений свойств ноль, три разные зоны |
| `.stepper-val` (base + workout) | законный | — | база даёт шрифт, экран — вес и кегль |
| `.live-hero-val` (base + workout) | законный | — | то же |
| `.stat-chip` анимация (base) | законный | — | база даёт `card-in`, экран — коробку |

**Главная находка — не дубль, а баг, который дубль вскрыл.** Весь компонент `.orm-*` (список 1RM) жил только в `analytics.css`, а рендерится на ДВУХ экранах — Stats и Дашборде (`js/dashboard.js:577`). `analytics.css` грузится лениво, значит **до первого захода в Stats блок 1RM на Дашборде был голым**: `.orm-row` = `display:block` вместо флекса, ни фона, ни паддинга, ни полосы прогресса. Замер на свежей загрузке подтвердил. Не всплывало, потому что на пустой базе блок не рендерится вообще — нужен реальный история тренировок. Компонент переехал в `base.css` (грузится всегда и первым); у Stats значения не сдвинулись ни на пиксель.

**Сканер из карточки был слеп — реальных дублей 20, а не 9.** Регексп ограничен `[^{}\n]{0,120}`, то есть не видит списки селекторов, разбитые переносом строки. Ровно так спрятался блок AIR Tier 1 в `base.css:1081` — одиннадцать селекторов в столбик с `border-radius: var(--r-l) !important`, который глушит радиусы во всех файлах экранов. Рабочая версия — без `\n` в классе и с `;` в разделителях:

```bash
node -e "const fs=require('fs');const p='css/';const strip=s=>s.replace(/\/\*[\s\S]*?\*\//g,' ');const map={};for(const f of fs.readdirSync(p).filter(f=>f.endsWith('.css'))){const src=strip(fs.readFileSync(p+f,'utf8'));for(const m of src.matchAll(/(^|[};])\s*([.#][^{}]*?)\s*\{/g)){if(/@|:root/.test(m[2]))continue;for(const s of m[2].split(',').map(x=>x.trim().replace(/\s+/g,' '))){if(s&&/^[.#]/.test(s))(map[s]=map[s]||new Set()).add(f);}}}console.log(Object.entries(map).filter(([,v])=>v.size>1).map(([k,v])=>k+' -> '+[...v].join(' + ')).join('\n'))"
```

**Приём, который стоит унести дальше: обход CSSOM вместо грепа.** `document.styleSheets` → `cssRules` показывает ВСЕ правила, матчащие селектор, в порядке каскада, с источником, медиа-условием и `!important` — включая инлайн `<style>` в `index.html`, который греп по `css/` не видит в принципе. Именно так нашлись и AIR-группа, и то, что настоящий каркас `#status-bar` лежит в критическом инлайне, а не в `base.css`.

**Осталось 15 дублей — все из групп AIR/Tier в `base.css`, карточка CSS-3.** В работу не брались осознанно: правка одной строки в групповом селекторе задевает восемь файлов экранов сразу, это отдельный PR, а не довесок.

### CSS-3 · Мёртвые `border-radius` под AIR Tier 1 ✅ ЗАКРЫТА (1.27.18) — Gio выбрал A+

**Замер на 375px, пробы в реальных контейнерах экранов (`#s-train` / `#s-stats` / `#s-body` / `#s-profile`), все 13 файлов CSS загружены обходом по экранам:**

| селектор | объявлено в файле экрана | применялось | после правки |
|---|---|---|---|
| `.exercise-card` | `workout.css` → `--r-xl` 22px | **18px** | 18px |
| `.type-card` | `workout.css` → `--r-xxl` 24px | **18px** | 18px |
| `.chart-card` | `analytics.css` → `--r-xl` 22px | **18px** | 18px |
| `.bs-stat-card` | `body-stats.css` → `--r-m` 14px | **18px** | 18px |
| `.profile-card` | `profile.css` → `--r-l` 18px | 18px | 18px |
| `.cal-card` | `analytics.css` → `--r-l` 18px | 18px | 18px |
| `.heatmap-card` | `claude.css` → `--r-l` 18px | 18px | 18px |
| `.coach-card` | `claude.css` → `--r-l` 18px | 18px | 18px |

Контрольное снятие `!important` через CSSOM дало 22 / 24 / 22 / 14 — ровно как объявлено, четыре дубля не шелохнулись.

- **Развилка решена не по вкусу, а по происхождению строки.** `git log -S` даёт единственный коммит `f45b12d` (7 июня, «AI Panda Assistant»): группа была ещё glass-группой, правились `blur`/`saturate`, `!important` приехал попутной строкой в чужом коммите. В `.claude/rules/design.md` радиус не легализован — инварианты AIR перечисляют воздух, hairlines, отступы, safe-area, blur, радиуса там нет. В самом правиле `background` и `border` идут без `!important`: закон запер бы все три.
- **Gio выбрал A+ — снять и мёртвые объявления, и `!important`.** Восемь объявлений в шести файлах экранов удалены, `!important` в `base.css` снят, радиус объявляется теперь ровно в одном месте. Замер после: все восемь по 18px, живой `.type-card` на Train — 18px. Ноль визуальных изменений.
- **Защиты от нового расхождения больше нет — её место занимает SCALE-1.** Радиус имеет смысл добавить в тот же stylelint-блок, что и типографику: правило «`border-radius` в файле экрана для селектора из группы AIR» ловится не глазом.
- Повторный обход CSSOM после правки: по всем 11 классам группы осталось ровно одно правило радиуса — в `base.css`, без `!important`.

### CSS-4 · `.cal-card` / `.heatmap-card` / `.coach-card` — классы без разметки ✅ ЗАКРЫТА (1.27.19)

Найдено попутно в CSS-3. Три класса, два разных диагноза: один дефект вёрстки и два трупа.

**`.cal-card` — дефект, календарь Stats действительно ехал голым.** `js/analytics.view.js:146` объявлял контейнер как `<div id="cal-card">` — **id, а не класс**, а селектора `#cal-card` в CSS нет нигде. Замер на 375px, сид из 12 тренировок (на пустой базе календарь не рисуется — та же слепая зона, что у 1RM в CSS-2):

| свойство | объявлено `analytics.css:6` | было | стало |
|---|---|---|---|
| `background` | `--c-bg-2` | `rgba(0,0,0,0)` | `rgb(12,12,18)` |
| `border` | hairline `--c-border` | `0px none` | `0.67px rgba(255,255,255,.06)` |
| `padding` | `--sp-2` | `0px` | `16px` |
| `border-radius` | AIR Tier 1 `--r-l` | `0px` | `18px` |
| `overflow` | `hidden` | `visible` | `hidden` |

Правка — одно слово в разметке (`class="cal-card"` рядом с id: id держит `getElementById` в `_drawCalendar`, снимать его нельзя).

**`.heatmap-card` и `.coach-card` — осиротевшие, не born-dead. Снесены.** `git log -S` даёт `f6fbd21` (9 июня, «adapt nav for 4-button rule»): коммит выкинул тело `Claude.open()` — весь оверлей AI-коуча — и заменил его редиректом `Nav.go('s-intel')`. До него оба класса жили в разметке `js/claude.view.js:187,189`. То есть фича не потеряна, а переехала в Neural Command Center; CSS остался от старого экрана. Удалены правила в `css/claude.css` и две строки из группы AIR Tier 1 в `base.css`. Обход CSSOM после правки: правил с этими селекторами не осталось ни одного.

**Попутно, в SPACE-1:** `.cal-card` теперь впервые в истории применяет свой `padding: var(--sp-2)` = 16px, а инвариант AIR №3 требует для карточек 24/32 (`--sp-3`/`--sp-4`). Значение не трогал — это работа шкалы отступов, а не разметки, и судить его надо на экране вместе с остальным Stats.

**Попутно, в DEAD-1:** оверлей коуча унёс с собой не два класса, а весь блок. Без потребителей в `js/`+`index.html` остались также `.heatmap-legend`, `.legend-row/-dot/-info/-group/-status/-hours`, `.heat-scale`, `.heat-scale-bar`, `.heat-scale-labels`, `.coach-msg-text`, `.coach-msg-rec`, `.coach-rec-label`, `.coach-rec-type`, `.next-session-card` — плюс уже числившийся там `.chat-send-btn`. Не тронуты осознанно: это сплошной свип файла, а не довесок к карточке на три селектора.

### VIS-1 · Закон токенов держится гейтом, а не совестью ✅ ЗАКРЫТА (2026-08-25)

Спек ссылался на stylelint как на исполнителя запрета сырых цветов, а тот молчал по двум причинам сразу: правило стоит `severity: warning` (`npm run lint` возвращает 0 при любом числе нарушений), а его паттерн якорен на **начало** значения — сырой цвет внутри `box-shadow: 0 4px 12px rgba(...)` или `border: 1px solid rgba(...)` ему не виден. Замер на `f7f6c9b`: в `css/` лежало 43 сырых цвета, stylelint показывал 18 и не красил ничего.

**Сделано:** `test/design-tokens.test.js` — четыре инварианта в `npm test`. Типографика вне `base.css` — ноль (`font-size` с единицей длины, цифровой `font-weight`); цветной hex вне `base.css` — ноль; цветные `rgba` плотнее узаконенных 20% — потолок 14, защёлка бьёт в обе стороны (отрос — регресс, упал — потолок не переставлен); правило в `.stylelintrc.json` не вымыто. Нейтральные `rgba` (r=g=b) и `#000` в `mask-image` из области выведены осознанно: первое — тени, токенов глубины в `base.css` нет; второе — альфа-канал, а не цвет.

**Осталось на потом:** потолок 14 опускать починкой цветов (`dynamic-island.css:41,68` — почти-чёрные фоны мимо `--c-bg`; остальное — акценты плотнее 20%). Инлайн `<style>` в `index.html` объявляет свою копию палитры (24 хекса) и в область не входит — карточка **VIS-2**.

### VIS-1b · Гард слеп к файлу, где случился единственный дизайн-инцидент 🟡 открыта

- **ФАКТ, не гипотеза:** `test/design-tokens.test.js:47,64` — `TOKEN_SOURCE = 'base.css'`, и `cssFiles()` фильтрует его целиком. Все четыре инварианта ходят через эту функцию, обхода нет. Гард физически не может покраснеть на сыром цвете внутри `base.css`.
- **Цена измерена историей:** THEME-4 (контраст ≈1:1 на всех оверлеях в светлой теме) — фон листа сырым `rgba(15, 15, 18, 0.75)` прямо в правиле `.modal-sheet`. `git log -S "rgba(15, 15, 18, 0.75)" -- css/base.css`: внесён `92e29ac`, снят `67e779f` (1.27.37). r=15, g=15, b=18 — не нейтраль, альфа 0.75 > 0.20, то есть по правилу `design-tokens.test.js:146-151` он **был бы** нарушением. VIS-1 его не поймал бы: файл вне области. Единственный задокументированный дизайн-инцидент проекта лежит ровно в слепой зоне гарда, написанного против этого класса ошибок.
- **ЦЕЛЬ:** область гарда расширяется на `css/base.css` **вне блоков объявления токенов** (`:root`, `:root[data-theme='light']` и прочие `--*:`). Там сырое значение — по роли; в правилах компонентов — долг.
- **Объём слепой зоны:** грубым грепом ~25 сырых цветов в `base.css` вне строк с `--`. Это верхняя граница, не число багов — сколько из них протухшие, никто не мерил.
- **Whitelist обязателен, минимум два осознанных исключения:** блок `:root[data-theme='light'] .island` (`base.css:379-391`) — остров тёмный в обеих темах намеренно, это же исключение уже стоит в НЕ ТРОГАТЬ у THEME-4 · чёрный скрим `base.css:743` — узаконен замером в THEME-4. Остальное разбирать поштучно `git log -S`, дорого; допустимо завести отдельный потолок для `base.css` и опускать его так же, защёлкой.
- **DoD:** прогон нового гарда на `304b709` (родитель фикса THEME-4) **краснеет**, на текущем `main` — зелёный. Baseline обязан воспроизводить баг, иначе меряется заплатка.
- **Не догрузка VIS-1, а отдельный заход:** VIS-1 закрыта и её область объявлена в шапке файла; расширение переписывает шапку и требует своего baseline.
- **Открытый вопрос к Gio:** у `RAW_COLOR_CEILING = 14` нет владельца и срока. Тест зелёный при 14 сколько угодно долго, карточка на снижение не назначена. Защёлка без плана погашения перестаёт быть фиксацией долга и становится его узакониванием — новый потолок для `base.css` заводить только вместе с владельцем.
- **⚠️ Конфликт по файлу:** правит `test/design-tokens.test.js` и (whitelist'ом) читает `base.css` — не параллелить с SCALE-1, DEAD-1, NAV-BACK-1.

### VIS-2 · Инлайн `<style>` в `index.html` — вторая копия палитры 🟡 открыта

- **ЦЕЛЬ:** копия токенов в критическом CSS не имеет права разъезжаться с `css/base.css :root`, как разъехался PPL-словарь до DS-1.
- **Факт (2026-08-25):** блок объявляет `--c-bg`, `--c-text-1..3`, `--c-accent`, `--c-intel`, `--c-purple`, `--c-secondary`, `--c-blue`, `--c-red` и светлую тему — 24 хекса. Сверять их с `base.css` сейчас нечем; `test/design-tokens.test.js` смотрит только `css/*.css`.
- **DoD:** тест, который валит расхождение значения токена между инлайном и `base.css`. Не запрет хекса — там он на месте, это точка объявления.

### SCALE-1 · Гард шкал в CI 🟢 закрывающая

> **Половина про типографику закрыта VIS-1 (2026-08-25) — тестом, не линтером.** `font-size`/`font-weight`
> вне `base.css` держатся нулём в `npm test`. Остаётся то, чего VIS-1 не трогает: `padding`/`margin`/`gap`,
> ревизия радиусов и перевод цветового правила stylelint из `warning` в `error` (требует починить 18).
> Второй гард на типографику не заводить — сверяться с `test/design-tokens.test.js`.

> **Переименована из `GUARD-1` 2026-08-12.** Имя столкнулось с закрытой карточкой GUARD-1
> из `HANDOFF_panda_core.md` (периметр отвергнутой линии, `4ba4d26`). Переименована именно эта:
> та вморожена в коммит, PR#184 и шапку `test/perimeter-guard.test.js`, а эта ещё не бралась.
> Новое имя встаёт в один ряд с `SPACE-1` и `TYPE-2` — карточка ровно про шкалы `--fs-*/--fw-*`.

- **ЦЕЛЬ:** stylelint-правило против сырых `font-size` (и позже `padding`/`margin`/`gap`) по образцу уже работающего запрета сырых цветов.
- **ГДЕ ПРАВИТЬ:** `.stylelintrc.json`, блок `declaration-property-value-disallowed-list` — рядом с существующим правилом по цветам (regex по свойству → список запрещённых значений + `severity` + `message` со ссылкой на карточку). Копировать его форму, а не изобретать свою.
- **Когда:** после заход №7 — раньше правило утонет в собственных warning'ах остатка. **Обе половины правила теперь можно ставить одним заходом:** SPACE-1 закрыта (1.27.68), сырых `padding`/`margin`/`gap` вне узаконенных исключений не осталось, так что и шкала отступов встаёт сразу в `error`, а не в warning. Оттуда же переезжает **ревизия радиусов** — 8 ступеней `--r-*` к четырём, правка таблицы токенов в `base.css` одним заходом по всем экранам.
- **DoD:** новое нарушение шкалы = **красный** `npm run lint`. Цвета сейчас стоят на `severity: warning` (18 предсуществующих) — типографику имеет смысл сразу ставить в `error`, потому что после захода №7 нарушений будет ноль и накопить долг неоткуда.
- **Whitelist обязателен, иначе правило красит зелёный код красным.** Три законных исключения, все с комментариями в коде: `base.css:1487-1488` (`.ppl-g-total`/`.ppl-g-cap` — `<text>` в системе координат viewBox) · девять `font-weight: 200 800` в `@font-face` (дескрипторы диапазона оси переменного шрифта, а не объявления) · `js/features/pip.js` (чужой документ, токены не доезжают — но его stylelint и так не видит, правило только на `css/**`).
- **Whitelist для половины про отступы — четыре класса, все с комментариями в коде** (собран SPACE-1, не изобретать заново): значения 1–3px ниже нижней ступени `--sp-0-5` (оптическое выравнивание лок-апов) · клиренсы вне шкалы (`72px`/`120px` у `.screen`, `120px` у штампа версии, `44px` под кнопку в поле ключа) · `var(--side-padding)` = 20px как гуттер экрана · `js/features/pip.js` и `js/ui/receipt.js` (чужой документ и canvas-растр). Плюс `js/workout.view/handlers.js` — 6 объявлений, не переведены, ждут мёржа PR#211: до него правило по `js/` покрасит их.
- **⚠️ Конфликт по файлу:** трогает `base.css` только whitelist'ом, но карточки DEAD-1 и NAV-BACK-1 сидят в нём же — не параллелить.

### BRAND-1 · Сплэш показывал прошлое поколение знака ✅ ЗАКРЫТА (1.27.32) — Gio выбрал PNG из пакета

Аудит 2026-08-07 нашёл **три поколения логотипа в живой эксплуатации одновременно**:

| | что | откуда | где было видно |
|---|---|---|---|
| v1 | неоновый бицепс + вордмарк внутри плитки | 2026-03-16 | только кеши ОС: установленный PWA и favicon-база браузера. В репо и на проде файлов нет |
| v2 | фиолетовая абстрактная форма | 2026-07-30 `e0c2a46` | экран автозагрузки — векторизован инлайном в `index.html` |
| v3 | «A», серебро + голубое свечение | 2026-08-04 `7d89e74` | `icons/*`, манифест, статус-бар, `favicon.ico`, прод |

**Корень:** когда 1.27.1 заменил растровый пакет целиком, инлайновый SVG сплэша не мигрировали. Автозагрузка показывала v2, гасла, и в шапке появлялся v3 — два поколения знака за полсекунды. Файлы были ни при чём: sha256 шести иконок на проде байт-в-байт равен локальным.

**Сделано:** сплэш берёт `icons/icon-192.png` через `rel=preload as=image` — тот же файл, что отдаёт манифест, поэтому знак на первом кадре пиксель-в-пиксель равен иконке на домашнем экране. Внешний бокс `--c-accent-bg` снят: подложка нарисована внутри самого знака, и коробка подкладывала под чёрную плитку зелёный квадрат. Амплитуда `splash-breathe` смягчена 0.65/0.95 → 0.8/0.96: прежняя была настроена под плоский 48px-глиф и на детальном растре читается как мерцание.

**Попутно — выход сплэша стал детерминированным.** `splash-breathe` бесконечная, а загрузка кончается когда кончается: знак срезало на произвольном кадре цикла. Правило `#loading.hidden .splash-mark` снимает анимацию и уводит знак одним и тем же движением (подъём + растворение, `transform`/`opacity`).

**Мина, снятая тем же заходом:** `scripts/build-sw.mjs` сканирует каталог `icons` **с диска**, а в корневом чекауте лежали неотслеживаемые `icons/favicon.png` (34 КБ, удалён из гита в `7d89e74`) и `_temp_icons/`. Запуск `npm run build:sw` из корня вписал бы `favicon.png` в прекеш: мёртвый груз каждому пользователю плюс `CACHE_NAME` с хешем, невоспроизводимым ни в одном worktree → красный `test/sw-cache-name.test.js` у всех остальных агентов. Файлы перенесены в `C:\PROJECTS\_athlete-pro-icons-attic` (не удалены — в гите их нет, восстановить было бы неоткуда). Заодно из репо и прекеша ушли `icons/{push,pull,legs}.svg` — ноль ссылок в коде с момента добавления в 2026-05-20.

### BRAND-2 · `icon-maskable-512` не по спеке маски 🔴 открыта
- **ЦЕЛЬ:** марка ≤56.5% ширины канвы, чтобы круглая маска Android не резала углы плитки и свечение.
- **Замер (2026-08-07):** марка занимает 64% канвы, углы уходят на **112.7%** безопасного радиуса при лимите 80%. Проверять так же — прогнать bbox контента через canvas и посчитать `maxRadius / (size/2)`.
- **DoD:** пересчитанный радиус ≤80%, файл перегенерён, `npm run build:sw` даёт пустой дифф на повторном прогоне.

### BRAND-3 · Пять «почти-чёрных» на один бренд 🟡 открыта
- **ЦЕЛЬ:** один near-black.
- **Что разъехалось:** `#070a19` в «any»-иконках · `#05050f` в maskable · `#0a0a0f` в `manifest.json` (`background_color` И `theme_color`) · `#050507` в `<meta name="theme-color">` и в фоне приложения.
- **Видимое последствие:** на Android сгенерированный PWA-сплэш красится `#0a0a0f`, следом приложение перекрашивает в `#050507` — ступенька на старте. Плюс тинт статус-бара отличается во вкладке и в установленном приложении.

### BRAND-4 · Вордмарк отстал от знака 🟡 открыта, решение за Gio
- «Pro» — `#8b5cf6` со свечением, цвет поколений v1/v2. Знак рядом голубо-серебряный. В одной строке статус-бара два поколения бренда.
- Развилка: подтянуть вордмарк к знаку или знак к вордмарку. Это дизайн-решение, не уборка.

---

## Линия DS — система течёт через JS (аудит 2026-08-20)

Сквозной проход по токенам показал обратное ожидаемому: **CSS чист, протечка в `js/`.** В `css/` вне
`base.css` — 4 сырых hex на 9835 строк, 2 сырых `font-size`, ноль цифровых `font-weight` (все 9 —
дескрипторы `@font-face`), ноль хардкодных разделителей. Спек при этом врал: `intel.css` числился
остатком TYPE-2, фактически там 27 `--fs-*` / 25 `--fw-*` и ноль сырых — строка в `design.md`
поправлена этой линией.

Причина перекоса структурная: `stylelint` смотрит только `css/`, да и то с `severity: warning`.
За пределами его взгляда цвета живут копиями.

### DS-1 · Один источник PPL-цвета для JS ✅ ЗАКРЫТА (1.27.66)

Четыре модуля держали свою копию PPL-хексов, и копии разъехались: `workout.view/render.js` рисовал
Pull как `#00e5ff` и Legs как `#bc13fe` — цвета вне палитры вообще, — а `analytics.view.js` те же
типы как `#00b8d4`/`#8b5cf6`. Один Pull светился разным цианом в тренировке и в аналитике. Вторым
следствием была мёртвая светлая тема: `:root[data-theme='light']` переопределяет `--c-push`/`--c-legs`,
а константы в JS держали неоновые значения жёстко.

Введён `js/shared/ppl-color.js` — `pplColor()` / `pplColorAlpha()` / `isPplType()` / `PPL_TYPES`.
Цвет резолвится из токена через `getComputedStyle`, кэшируется, кэш умирает по `MutationObserver`
на `data-theme`. Hex, а не `var(--c-*)`, потому что вызывающие клеят альфу и кормят цветом
`canvas.fillStyle` — переменная не умеет ни того, ни другого. Вне DOM (node --test) отдаётся
`PPL_FALLBACK`, который тест сверяет с `css/base.css`, разъехаться молча они не могут.

`js/dashboard.js` не тронут — он единственный делал правильно (`var(--c-push)` в inline-стиль,
браузер резолвит сам) и остаётся эталоном для тех мест, где альфа и canvas не нужны.

**Гейт:** 867/867 · eslint 0 err / 7 warn (все преэкзистующие) · `build:sw` перегнан (123 ассета,
digest `791b3602`). **Замер в превью, обе темы:** функция совпадает с токенами (`pull` `#00b8d4` →
`#0284a3`, `legs` `#8b5cf6` → `#6d28d9`); календарь, сетка выбора типа и canvas-бары переезжают на
светлые токены; альфы календаря остались ровно прежними (0.125 / 0.25 против бывших `20`/`40`).
Скриншот снять не удалось — превью-панель скрыта, страница не композитит кадры (известная ловушка,
см. TYPE-1); для цветовой задачи computed-значения доказательнее.

**Гард — `test/ppl-color.test.js`.** Ловит не «любой хекс», а копию PPL-словаря: хекс в окне ±3 строк
от слова push/pull/legs. Иначе гард тащил бы чужие карточки — `#00e676` это одновременно `--c-accent`
(BRAND) и `--c-push` (PPL), и запрет по совпадению хекса краснел на палитре аватаров и на фолбэке
внутри `var()`. Ушедшие `#00e5ff`/`#bc13fe` запрещены безусловно: они вне палитры. Проверено на
регрессе — подсадка копии в `dashboard.js` красит гейт, откат возвращает зелёный.

### DS-2 · Stylelint не краснеет и не видит `js/` 🔴 открыта — берётся следующей

Гард цветов существует, но не гейт: `severity: "warning"` плюс `ignoreFiles: ["css/base.css"]`.
Отсюда 7 живых хардкод-rgba в бордерах, три из которых выше потолка 20% из `design.md`
(`analytics.css:287` — 0.30 и цвет `rgba(232,132,140)` вообще не из палитры, `profile.css:508` — 0.3,
`dashboard.css:445` — 0.25). Главное же — `js/` вне его взгляда, из-за чего DS-1 и стала возможной.

- **ЦЕЛЬ:** поднять правило до `error` и распространить проверку сырых цветов на `js/`.
- **ГДЕ СТОП:** `.stylelintrc` + конфиг eslint; разбор 7 существующих rgba — тем же заходом, иначе
  гейт покраснеет на преэкзистинге.
- **НЕ ТРОГАТЬ:** легитимные исключения, список в DS-3 — им нужен whitelist, а не перевод.
- **DoD:** `npm test` зелёный при `error`; новая сырая rgba в CSS и новый сырой цвет в JS краснят
  гейт (проверить регрессом, как в DS-1).

### DS-3 · Параллельная палитра и остатки сырых цветов 🟡 открыта

Разнородный хвост, каждый пункт мелкий, но вместе они и есть «система течёт»:

1. **Палитра аватаров вне BRAND/SEMANTIC** — `js/shared/athlete-room.js:33-56` держит 15 цветов
   происхождения Tailwind (Indigo, Emerald, Pink, Rose) плюс шкалу тиров Untrained→Elite. Спек
   говорит: декоративные акценты — только `--c-accent`/`--c-secondary`. Тот же массив скопирован
   в `js/profile.view.js:69` — две копии разъедутся при первой правке, как разъехался PPL.
2. **Текст на canvas темы не замечает** — `analytics.view.js` метка недели `#a1a1aa` и значение
   `#ffffff`. Замер в светлой теме это подтвердил: бары переехали на токены, подписи остались
   тёмнотемными. Нужен резолвер токенов текста, родственный `ppl-color.js`.
3. **`GOLD = '#D4AF37'`** дублируется в `analytics.strength-curves.js` и `chamber-pill.js` при живом
   `--c-gold`, который в светлой теме `#8a6d1f`. Ровно болезнь DS-1, только для achievement-цвета.
4. **`onboarding.js:253`** передаёт карточке приватности `#8b5cf6`, тогда как соседняя строка для
   той же роли использует `var(--c-blue)`.
5. **SVG-текст мимо шкалы** — `base.css:1675-1676`: `font-size: 30px` (это ровно `--fs-6`) и `11px`
   (ступени нет — между `--fs-1` и `--fs-2`). Оговорка захода №1 TYPE-2 про систему координат
   viewBox остаётся в силе для второго, но первый — просто незаменённый токен.

**Легитимны, трогать не надо** (это whitelist для DS-2): `js/plate-calc.js` — цвета блинов по
стандарту IWF, то есть данные, а не декор; `js/ui/receipt.js` — canvas-растр чека; `theme.js:21`
`META_COLOR` — `meta theme-color`, переменную туда не передать; `js/features/pip.js` — чужой
документ `documentPictureInPicture`, токены не доезжают (то же исключение, что узаконила TYPE-2).

---

## Порядок и зависимости

```
ANIM-1 ✅ ──► ANIM-2 ✅                         (движение закрыто целиком: 1.25.26 + 1.25.27)
ПОЛЕ-1 ✅ ──► TYPE-1 ✅ ──► TYPE-1b ✅ ──► TYPE-2 (base ✅ · dashboard ✅ · analytics …) ──┐
PP-6  ✅ ──► PP-4 ✅ ──► PP-3 ✅ (+ PCT-1 ✅) ─────────────────────────────────────────────┴─► SPACE-1 ──► SCALE-1
NAV-BACK-1 — отдельным окном (общий base.css, не параллелить)
```

**Следующая по очереди — SPACE-1:** состав профиля теперь финальный (бенто из трёх ячеек), ритм можно выравнивать по нему. Параллельно свободна TYPE-2 заход №3 (`analytics.css`) — файлы не пересекаются.

**Порядок утверждён Gio 2026-08-01, по критерию «минимум конфликтов по файлам»:**

1. **TYPE-2** (`dashboard.css` → `analytics.css`) — единственная работа, которая ни с чем не дерётся.
2. **PP-3** (профиль + Athlete Room, ячейка DOTS уходит) — ✅ выкачена в 1.25.55 вместе с PCT-1.
3. **SPACE-1** (`profile.css`, вбирает бывшую profile-hero-spacing) — строго ПОСЛЕ PP-3: та меняет состав бенто, и ритм выравнивается по финальному составу, иначе часть работы в мусор.
4. **NAV-BACK-1** (`HANDOFF_next_cards.md`) — отдельным окном, трогает `base.css` и все подэкраны разом.
5. **SCALE-1** — закрывающая, только после TYPE-2 и SPACE-1.

Анкер закрыт: ПОЛЕ-1 дала полевой вердикт, TYPE-1b по нему опустила верх шкалы. Тиражировать теперь можно — решение проверено на телефоне, а не только по мокапам.

## Разбор сессии 2026-07-29 (PP-6 + гард дрейфа)

**Выкачено:** 1.25.33 (PP-6, PR#68). Плюс инфраструктурное: гард дрейфа базы (PR#67) и актуализация очереди (PR#66).

**Главный урок сессии — не про профиль, а про процесс.** PP-6 сначала пришла готовой веткой от другого агента: подход верный, браузерная проверка настоящая, «pre-push SAST/SCA прошёл», «node --test 211/211 зелёные». Всё правда — и всё по коду от 28 июня, на 233 коммита позади. Влитие откатило бы ISL-SET, TYPE-1, PP-5 и ещё три карточки.

Три сигнала были прямо в отчёте и каждый читался как хорошая новость:

- **211/211 зелёные** — при 385 тестах в main. Ветка не содержала половины сюиты (15 тест-файлов из 33). Зелёный на неполной сюите — не зелёный.
- **«Заплатки PP-5 нет, она на параллельной ветке»** — PP-5 была в main с 1.25.25. Аномалия замечена верно и закрыта гипотезой вместо `git log origin/main -- <файл>`.
- **`_refreshPrivacy()`** — рефрешер для блока, которого в профиле нет с ISL-SET.

Корень: гейты отвечали честно, но ни один не отвечал на вопрос «этот код про сегодняшний main?». SCA про уязвимости, SAST про стиль, тесты про свою сюиту. Поставлен `scripts/check-branch-drift.mjs` первым шагом в `.githooks/pre-push` — считает пересечение (твои файлы ∩ изменённые в main), а не отставание. Подробности и правила — в `CLAUDE.md` § Multi-Agent Protocol.

**Что при этом сработало правильно:** подход из старой ветки переиспользован целиком — плохой была только база. Переложить идею на свежий файл вышло дешевле, чем разбирать конфликт в 90 из 126 строк.

**Ловушка замера:** первый baseline-прогон показал, что на `main` прыжка нет — и это едва не закрыло карточку как несуществующую. Прыжка не было потому, что работала заплатка PP-5. Правильный эксперимент — снимать заплатку в обеих сборках; тогда `main` даёт +952px, а PP-6 ноль. Мораль: меряя «стало ли лучше», сначала убедись, что baseline воспроизводит баг.

## Разбор сессии 2026-07-28 (TYPE-1)

**Выкачено:** 1.25.32 — шкала `--fs-*`/`--fw-*` + Профиль как пилот (PR#65, чеки `test`/`e2e`/Vercel зелёные).

**Что сработало и стоит повторять:**

- **Развилку решать картинкой, а не числами.** Первый заход был текстом («6 ступеней, шаг 1.25, база 16px») — ответ Gio: «покажи превью варианты, так сложно понять». После трёх мокапов Профиля в натуральную величину (было / A / B) вердикт пришёл одной строкой. Мокапы **не масштабировать** `transform: scale()` — 10px превращается в нечитаемые 5px и сравнение теряет смысл.
- **Аудит перед правкой окупился.** «9 весов» оказались меньше, чем кажется: 500 и 600 глазом не различимы, а 900 у Manrope упирается в верх переменной оси (200–800) и клэмпится к 800 — три объявления давали один рендер. Без этого замера шкала весов вышла бы из пяти ступеней вместо трёх.

**Ловушка сессии:** превью-панель скрыта → страница не композитит кадры, скриншот снять нельзя. Проверку переполнений делали замерами `getBoundingClientRect`/`scrollWidth` через `javascript_tool`. Это работает, но означает: **визуальную проверку на телефоне никто не отменял**, инструментальная её не заменяет.

**Хвост карточки:** полевой смоук 1.25.32 на Android Gio — единственное, что нельзя проверить из репозитория.

## Разбор сессии 2026-07-27 (что произошло и на что не наступать)

**Выкачено:** 1.25.21 (long-press индикатора приватности) · 1.25.22 PP-1 · 1.25.23 PP-2 · 1.25.24 дубль `.pp-bento` · 1.25.25 PP-5 · 1.25.26 ANIM-1. Прод-смоук зелёный на 1.25.25, для 1.25.26 — прогнать после выкатки.

**Ловушки, стоившие времени:**

- **Ветка была на 215 коммитов позади main.** Фикс long-press писался по коду, которого в main уже нет: `PrivacyRapid` и экран `s-privacy` упразднены, приватность переехала в `s-island-settings`. Правило подтвердилось на практике: `git fetch && git log --oneline origin/main -5` и сверка ключевых символов **до** первой правки, а не после.
- **Прод-смоук дал ложный красный** — worktree сидел на влитой ветке 1.25.21, прод уже был 1.25.25. Смоук сверяет локальный `version.js` с продом, поэтому протухший чекаут выглядит как непроехавший релиз. Перед смоуком — перевести дерево на свежий `origin/main`.
- **Память врала про PP-1** («закрыт, не влит») — на деле влит через PR#52. Статусы карточек перечитывать по `git log origin/main`, не по записям.
- **Превью-вкладка не композитит кадры,** когда панель скрыта: `requestAnimationFrame` не тикает, `setTimeout` клэмпится до 1000ms, плавный скролл и скриншоты не работают. Замеры делать через `MutationObserver` (микрозадачи не тротлятся) и `scrollIntoView` без `behavior:'smooth'`.

**Открытые вопросы к Gio** (без них PP-3 стоит):
1. PP-3: убирать ли ячейку «DOTS» из бенто после появления разбора скора?
2. PP-3: делать разбор целиком или сперва `SCORE` + шкала?
3. ANIM-1 в поле: не стало ли слишком резко — `--t-enter` крутится одной строкой.
4. TYPE-1 в поле: шкала утверждена по мокапам, но на живом Android не смотрели.

Вопросы 3 и 4 (плюс появившийся третий — ре-рендер PP-6) собраны в карточку **ПОЛЕ-1** выше: один заход на телефоне закрывает все.

**Аномалия, не разобранная в эту сессию:** над островом видна красная точка (`.island-dot.offline`), хотя в airgap она обязана быть серой (`--c-chrome`, `sync-dot.js`). Либо режим не airgap, либо `navigator.onLine`/sync врёт. Диагностика, отдельная карточка.

## Инварианты программы

- Vanilla JS, GPU-only анимации (`transform`/`opacity`), Spring Physics из `js/shared/spring.js` — правки движения не отменяют этих правил.
- Анимируемый `blur` (совет 7 статьи) — точечно и никогда в списках: полевое устройство Gio — средний Android.
- `prefers-reduced-motion` обработан в 5 местах — при каждом заходе по движению проверять, что правка не прошла мимо этой ветки.
- Цвета — только токены `css/base.css :root` (см. `CLAUDE.md` § Design). Эта программа добавляет к цвету три недостающие шкалы: размер, отступ, движение.
