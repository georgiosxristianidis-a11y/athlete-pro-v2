# HANDOFF — GYM-GRADE: финишная программа Athlete Pro

> Утв. Gio 2026-07-10. Цель программы: приложение = эстетическое удовольствие + плавность + стабильность в зале (личный стандарт, n=1). НЕ рост, НЕ маркетинг — это осознанно отложено до достижения DoD.
> Ресурс: Gio 1-2 ч/день → 1 карточка = 1-2 сессии. Порядок сверху вниз, приоритет = что сильнее бьёт по опыту в зале.
> Гейт каждой карточки: `npm test` (273) + `npm run lint` (0 err). Карточки, уже описанные в `HANDOFF_next_cards.md`, здесь НЕ дублируются — только ссылка и место в очереди.
> Прополото 2026-07-17: закрытые пункты очереди (FIELD-ISL → 1.23.0, MERGE-D → L2, SW-UPDATE-UX → 1.24.1, P-2 L3+L4 → 1.24.2) убраны — история в CHANGELOG/журнале.

---

## Definition of Done (гейт всей программы)

1. **Серия 10** — десять тренировок подряд без нового полевого бага (журнал внизу файла). **Сейчас 0/10** (обнулено 2026-07-26: полевая сессия дала баги F-1/F-2).
2. **AIR-чистота** — все экраны соответствуют AIR-инвариантам (`NEXT_SESSION.md` § Философия AIR), ноль визуальных отклонений. Остаток: AIR-2b + полевой OLED-чек AIR-4 (код AIR-4 ✅ 1.25.8, гард `test/air-guard.test.js`).
3. **Плавный барабан** — открытие сет-логгера и прокрутка веса без джанка на телефоне Gio. Остаток: DRUM-PERF-2.
4. **Обновление не рвёт тренировку** — ✅ SW-UPDATE-UX выкачен в 1.24.1.
5. **История неубиваема** — экспорт в 1 тап на видном месте + периодическое напоминание; миграция IDB проверена на живых данных (DS1). Код ✅ (BACKUP `22f7638`); остаток: полевой DS1.

Программа закрыта = все 5 пунктов зелёные. После этого — решение о следующем векторе (solar-спека уже готовится параллельно без кода).

---

## Очередь карточек

### 1. BACKUP — 4d + видимый экспорт — ✅ `22f7638` (2026-07-18)
Код закрыт (см. `HANDOFF_next_cards.md` § 4d). Остаток DoD-5 — полевой DS1 (миграция v3→v4 на живых данных, за Gio) + полевой чек экспорта/напоминалки.

### 2. DRUM-PERF-2 — плавность барабана без виртуализации — 🔶 код ✅ `0bd5d88` (2026-07-18), ждёт полевой чек
Флаг **`drum-window`** (дефолт OFF): окно ±20 значений со спейсерами, rebuild ТОЛЬКО в покое (scrollend/seek/heal), никогда в scroll-тике — анти-паттерн drum-virtual. DOM тренировки 6090 → 1956 узлов (превью-замер). Контракт scrollTop↔index — `test/drum-contract.test.js` (запинен ДО правок, `d8eb775`); все BUG-DRUM-0/BUG-7 гарды гоняются на всех 3 путях. Живой превью-чек: билд/скролл/коммит/рецентр/подсветка ✅, State weight сходится.
**Остаток (ГДЕ СТОП):** полевой чек Gio — `Flags.setFlag('drum-window', true)` на телефоне; вес корректен на всех упражнениях, скролл плавный. Особенность: один флик ограничен ±50 кг (край окна), дальше — второй флик после рецентра. drum-virtual не тронут, OFF.

### 3. AIR-4 + AIR-2b — добить DoD-2
AIR-4 — ✅ код в 1.25.8 (`HANDOFF_air_refactor.md` § AIR-4, «Сделано»), остаток — полевой OLED-чек. AIR-2b — в `HANDOFF_next_cards.md`, CSS/docs.

### 4. POLISH-LOOP — ритуал полевого полиша (постоянный, до конца программы)
**ЦЕЛЬ:** после каждой тренировки Gio кидает заметки-раздражения (голосом/текстом, без формата) → LEAD превращает в микро-карточки сюда, closable за 1 сессию. SET-STALE из `HANDOFF_next_cards.md` брать, если проявится в журнале.
**ГДЕ СТОП:** ритуал живёт до DoD-1 (серия 10). **НЕ ТРОГАТЬ:** ничего заранее — карточки только из реальных наблюдений.

#### Микро-карточки полиша (из полевых заметок Gio)

**P-1 · ISL-REST-BTN — визуально легче rest-кнопки (заметка 2026-07-11)**
**ЦЕЛЬ:** кнопки Rest-HUD (`+15с`/skip, `css/dynamic-island.css` `.island-rest-btn` 32×30) ощущаются тяжёлыми/«приклеенными». Визуал ужать до ~28×26 + иконки 13px, но зону нажатия сохранить ≥40px невидимым pseudo-element (`::after` с отрицательным inset) — в зале потные пальцы, тач-таргет не жертвуем. Само поведение «кнопки живут в острове только во время отдыха» — by design (ISL-REDESIGN), не трогать. Смежное: ISL-REST-NEXT (`HANDOFF_isl_tail.md`) трогает тот же rest-HUD — кандидат на одну сессию.
**ГДЕ СТОП:** полевой чек Gio — легче на глаз, попадание не ухудшилось. **НЕ ТРОГАТЬ:** timer-mode логику показа/скрытия, island-set-pulse.

---

#### Полевая сессия 2026-07-26 (прод 1.25.11) — карточки F-1..F-7

> Разбор по коду уже сделан — в карточках корни, а не «посмотреть». F-1 и F-3 правят один файл (`js/shared/athlete-room.js`) — не параллелить.
> Заметка Gio про «оранжевые цифры на самсунге» — НЕ баг: скрин из другого проекта (Work Tracker Pro), амбер там его палитра; Samsung цвета не подменяет (режим Vivid лишь повышает насыщенность). Заказ по тому скрину = F-4.
>
> **Статус на 2026-07-26:** F-1 и F-4 закрыты кодом в релизе 1.25.12, F-7 — в 1.25.13. **Не брать заново.** Свободная очередь: **F-6 → F-2 → F-3 → F-5**.
> Статус карточек проверять ancestry (`git merge-base --is-ancestor <sha> origin/main`), а не по совпадению хеша — на rebase-мёрже хеши мутируют.

**F-1 · AR-SAVE-CRASH — падение при сохранении имени/DOB — 🔴 P0 · 🔵 Sonnet — ✅ код в 1.25.12**
**КОРЕНЬ (найден):** `js/shared/athlete-room.js:475` — `await import('./profile.store.js')`. Модуль лежит в `js/profile.store.js`, а относительный путь из `js/shared/` резолвится в несуществующий `js/shared/profile.store.js` → reject → `unhandledrejection` (`js/app.js:273`) → тост «Something went wrong», а классификатор `js/shared/errors-ui.js:31` добавляет ложное «Нет соединения. Проверьте интернет.» (скрин поля). Имя при этом частично записано (`DB.Settings.set('athlete-name')`, строка 471 проходит), а `dob`/`sex`/`name` в профиль — нет: рассинхрон аватара и паспорта.
**ЦЕЛЬ:** удалить динамический импорт (строки 474-475) — `updateProfile` уже импортирован статически (`athlete-room.js:4`). Порядок записи сделать согласованным: сначала `updateProfile`, затем `DB.Settings.set('athlete-name')`.
**ТЕСТ-ГАРД:** unit в `test/` — `saveName()` с замоканными DB/`updateProfile` не бросает и пишет все три поля; плюс статический гард класса регрессии: в `js/shared/*.js` нет `import('./…')` на модуль, живущий в `js/`.
**ГДЕ СТОП:** смена имени/даты/пола в Athlete Room без тостов ошибки, паспорт обновлён; гейт `npm test` + `npm run lint`.
**НЕ ТРОГАТЬ:** схему профиля. `errors-ui.js` в рамках F-1 не трогать — но он НЕ невиновен, см. F-6.
**СДЕЛАНО (1.25.12):** динамический импорт удалён, `updateProfile` берётся из статического импорта. Верификация: 0 ошибок консоли, `window.AthleteRoom` жив, `npm test` 308/308. **Осталось из карточки:** тест-гард (unit на `saveName()` + статический гард `import('./…')` в `js/shared/*` на модуль из `js/`) и согласование порядка записи — вынести в отдельный заход.

**F-2 · ISL-PIP-NEXT — два «next», один врёт — 🟠 P1 · 🟠 LEAD**
**КОРЕНЬ (найден):** `js/rest-timer.js:59` шлёт в PiP частичный стейт `{time, name}`, а `PiP.drawFrame` мержит его в `_lastState` (`js/features/pip.js:96`) — `nextName`/`sets` остаются от кадра ДО закрытия подхода. Одновременно `js/shared/dynamic-island.js:387` намеренно не пушит в PiP, пока `_timerActive` (чтобы не затирать кадр «ОТДЫХ…»). Итог: остров считает `nextEx` честно (`dynamic-island.js:328`), PiP показывает прошлое «NEXT» = текущее упражнение; плюс окно PiP физически ложится поверх пилюли острова.
**ЦЕЛЬ (решение Gio — синхронизировать):** `nextName`/`sets` — один источник правды: RestTimer передаёт полный актуальный стейт (либо остров публикует `nextName` в PiP и во время отдыха, не трогая `time`/`name` кадра «ОТДЫХ…»). Оба «далее» обязаны совпадать в любой момент.
**ГДЕ СТОП:** репро (закрыть последний подход упражнения → отдых → свернуть приложение) даёт одинаковый текст в PiP и острове; полевой чек Gio.
**НЕ ТРОГАТЬ:** `island-set-pulse` (жёсткий запрет); механику RestTimer и отсутствие числового таймера в rest-HUD; `_timerActive`-защиту кадра «ОТДЫХ…» — она чинила мигание, не откатывать.

**F-3 · AR-DOB-PICKER — системный календарь вместо нашего — 🟠 P1 · 🔵 Sonnet**
**КОРЕНЬ:** `js/shared/athlete-room.js:359` — `<input type="date">`. Нативный пикер Android рисуется в локали системы (русский при английском UI) и мимо дизайн-системы. Такой же `type="date"` есть в `js/body-stats.js:154,170` (дата замера) — оценить попутно, по умолчанию не трогать.
**ЦЕЛЬ:** заменить на уже существующий паттерн онбординга — три `<select>` год/месяц/день с локализованными месяцами через `isRu()` (`js/onboarding.js:160-205`, обработчик `_obSetDob` там же:314). Стили — токены (`--c-bg-3`, `--c-border`, `--r-*`), высота 52px как в онбординге; при дословном дубле вынести в общий хелпер.
**ПОПУТНО (тот же экран):** селект пола `athlete-room.js:362-365` хардкодит `Male/Female` мимо `isRu()` — локализовать.
**ГДЕ СТОП:** в англ. и рус. версии пикер наш и на языке UI; сохранённый `dob` формата `YYYY-MM-DD` совпадает со старым (`computeAge`, `js/profile.store.js:129`, не трогаем); гейт.
**НЕ ТРОГАТЬ:** ключи `profile.dob`/`profile.sex`, онбординг.

**F-6 · ERR-CLASSIFY-MODULE — «нет соединения» вместо бага в коде — 🟠 P1 · 🔵 Sonnet**
**КОРЕНЬ (найден при разборе F-1):** `js/shared/errors-ui.js:30` — ветка `s.includes('failed to fetch')` ловит текст Chrome `TypeError: Failed to fetch dynamically imported module: <url>` и отдаёт «Нет соединения. Проверьте интернет.». То есть **любой** провалившийся ленивый импорт — опечатка в пути, 404 модуля, оборванный чанк — рапортуется пользователю как проблема со связью. F-1 убирает один такой случай, класс ошибок остаётся. Цена: в поле идёшь чинить интернет вместо кода — ровно тот сценарий, что стоил нам разбора 304-заморозки (симптом уводит от корня).
**ЦЕЛЬ:** до сетевой ветки добавить проверку на `dynamically imported module` / `error loading dynamically imported` → отдавать generic, не «нет соединения».
**ТЕСТ-ГАРД:** unit на классификатор — строка ошибки модуля Chrome → generic; `Failed to fetch` от обычного `fetch()` → по-прежнему «нет соединения»; `!navigator.onLine` → «нет соединения».
**ГДЕ СТОП:** оба кейса разведены тестом; гейт `npm test` + `npm run lint`.
**НЕ ТРОГАТЬ:** остальные ветки классификатора (timeout / 429 / auth / 5xx) и сигнатуру `toUserMessage(err, fallback)`.

**F-4 · UI-TOAST-REDESIGN — тосты по референсу Gio — 🟡 P2 · 🟠 LEAD — ✅ код в 1.25.12**
**ЧТО ЕСТЬ:** `css/base.css:647-721` (`#toast-wrap` снизу над навбаром; карточка 12/16px, `--r-l`; иконка 18px «голая»; статус — только тонкая рамка) + разметка `js/shell.js:88-90,142-165`.
**ЦЕЛЬ (решение Gio — полный редизайн):** иконка в мягком круглом чипе статусного цвета (не голый штрих), крупнее радиус и внутренние отступы, читаемее текст, стек — сверху. Цвета только через токены (`--c-accent`/`--c-red`/`--c-amber`), рамки — `var(--c-border)`/`--c-border-h` (закон hairlines), фон — Tier 3 floating glass (blur здесь легитимен). Контраст мерить по РЕАЛЬНОМУ композиту подложки (метод свипа контраста 1.25.10), не по токену фона.
**ГДЕ СТОП:** success/error/info и тост с кнопкой-действием (`toast-action`, живёт в SW-апдейте `js/app.js:371,405`) единообразны; тост не перекрывает поля редактора Athlete Room (скрин поля); гейт + `test/air-guard.test.js` зелёный.
**НЕ ТРОГАТЬ:** API `Toast.show(msg, type, duration, {action})` и дедуп по `_toastKey` — только разметка/CSS.
**СДЕЛАНО (1.25.12):** чип 34px несёт семантический цвет (тонировка рамки убрана — цвет живёт в одном месте), `--r-xxl`, текст 14px, ширина по содержимому, стекло через `color-mix` от `var(--c-bg-2)`, иконка ошибки — alert-triangle. Стек сверху, но **под** статус-баром (`top: safe-top + 55px`): верх экрана — территория Dynamic Island, тост не смеет перекрывать rest-таймер посреди подхода (проверено геометрией: toastTop 63 / statusBottom 55). Верификация: `npm test` 308/308, stylelint 0 предупреждений в секции, 0 pageerror, браузерный чек 412×915 (одиночный / стек трёх типов / двухстрочный / кнопка действия).

**F-5 · UI-SPLASH — минималистичная загрузка — 🟡 P2 · 🟠 LEAD**
**ЧТО ЕСТЬ:** `#loading` в `index.html` (стиль `index.html:202-208`) — растровый `icons/icon-192.png` в пульсирующем боксе; на телефоне читается как «огромный favicon с пикселями». Гасится в `js/app.js:104`.
**ЦЕЛЬ:** инлайн-SVG (лого-марк/вордмарк) вместо растра + лёгкая плавная анимация строго GPU-only (`transform`/`opacity`, `--ease-*` / spring из `js/shared/spring.js`), фон — токен near-black, без blur (Tier 1). Уважать `prefers-reduced-motion`.
**ГДЕ СТОП:** на телефоне Gio сплеш чистый и незаметный, без вспышки и сдвига при переходе в приложение; критический путь не вырос (сплеш остаётся инлайн в `index.html`, без доп. запросов).
**НЕ ТРОГАТЬ:** `icons/*` (иконки PWA), момент снятия сплеша (`js/app.js:104`); эмодзи запрещены — только SVG.

---

#### Перф-профиль 2026-07-26 (S23 Ultra, LTE в зале) — карточка F-7 + факты

> Инструменты в репо (`scripts/profile.mjs`, `scripts/graph-audit.mjs`, `scripts/compare-bundle.mjs`, релиз 1.25.12). **Замеры ниже уже сделаны — не перемерять, брать как вход.**

**F-7 · SW-MEDIA-BUDGET — 3.11 MB видео в прекеше — 🟠 P1 · 🟠 LEAD — ✅ код в 1.25.13**
**КОРЕНЬ (измерен):** SW прекеширует на первом визите **4.15 MB / 119 записей**, из них **3.11 MB медиа**: `assets/panda-idle.mp4` (1376 KB), `panda-idle.webm` (1292 KB), `panda-voice.mp4` (324 KB) — список `sw.js` ASSETS. На LTE 6 Mbps это ~6 с фоновой сотовой закачки, на забитом 1.5 Mbps — ~22 с. В перф-графики (FCP/LCP/TBT) не попадает вообще: грузится в фоне после `load`. Для сценария «зал, LTE» — **самая дорогая статья во всём приложении**.
**ЦЕЛЬ:** видео из precache в runtime-cache (кешировать при первом реальном воспроизведении); в precache оставить только `panda-poster.jpg`. Попутно оценить пережатие: 1376 KB mp4 на декоративную петлю — в 3-5 раз больше нужного; если ужать до ~400 KB, вопрос с precache может отпасть сам.
**ГДЕ СТОП:** панда играет офлайн после первого просмотра; прекеш ≤ 1.5 MB; гейт + полевой чек офлайна на телефоне Gio.
**НЕ ТРОГАТЬ:** ASSETS править **только** через `npm run build:sw` (не руками), затем бампить `CACHE_NAME`. Флаг `fab-video` и механику постер-кадра.
**СМЕЖНОЕ:** тест-гард на бюджет прекеша (≤1.5 MB) — писать в том же заходе, иначе 3 MB заедут обратно молча.
**СДЕЛАНО (1.25.13):** `scripts/build-sw.mjs` исключает медиа из ASSETS (`MEDIA_RE`), `sw.js` получил `mediaCacheFirst` — видео кешируется на первом реальном воспроизведении. Гоча, которую пришлось лечить: `<video>` просит `Range: bytes=0-`, сервер отвечает 206, а `maybeCache` берёт только 200 → без починки видео не кешировалось бы вообще; 206 на весь файл перемаркируется в 200 и кладётся в кеш (частичный — не кладётся). Прекеш **4.15 MB / 119 → 1.24 MB / 116 записей** (−2.91 MB), `panda-poster.jpg` остался. Гард — `test/sw-media-budget.test.js` (бюджет по реальным размерам файлов + запрет медиа в ASSETS + синхронность `MEDIA_RE` build↔sw + 206→200). Верификация в браузере: после первого проигрывания в кеше лежит ровно 327830 байт со статусом 200; **сервер остановлен — панда играет** (readyState 4, duration 10.005). Гейт 346/346, lint 0 err, SW `v118-1da15e45`.
**Остаток F-7:** (1) полевой чек офлайна за Gio; (2) `assets/panda-idle.mp4` + `panda-idle.webm` (2.6 MB) — мёртвый вес с `b099f73` (v1.0.0), в коде не упомянуты нигде; лежат в репо, но больше не качаются клиентом — удаление отдельным заходом; (3) пережатие `panda-voice.mp4` (328 KB) — по желанию, уже не критично.

**Факты замеров (вход для будущих карточек, перемерять не надо):**
- **CPU не проблема.** S23 Ultra (Snapdragon 8 Gen 2, троттлинг 1x): **TBT = 0**, тапы экранов 54-94 мс при пороге INP 200. Всё, что находится на троттлинге 4x (`updateClock` 49 мс, `_renderVolumeChart` 80 мс, PiP `_draw` 22 мс) — артефакт эмуляции слабого телефона, на реальном устройстве не видно. Карточки на это не заводить.
- **PiP заводится на буте.** `dynamic-island.js:218` зовёт `PiP.init()` безусловно — canvas 512×256 + `captureStream(5)` + играющий `<video>` в DOM, даже если PiP не открывали. CPU-цена копеечная, но это живой 5fps медиапайплайн при телефоне на батарее. Лечить в одном заходе с **F-2** (та же `pip.js`), отдельной карточки не надо.
- **Бандлер (esbuild, замерено A/B).** LTE норма: весь JS 4197 → 3731 мс (**−466**). LTE забитый: 6573 → 5356 мс (**−1217**). Запросов 48 → 29, трафик 200 → 135 KB. Бонус — content-hash в именах чанков = иммунитет к 304-заморозке. **Цена:** ломает `Vanilla JS, no build step`, трогает сборку + `sw.js` + Vercel разом. Решение отложено — брать только осознанно и отдельным PR, не в довесок.
- **Мёртвый вес:** JS 54% загруженного не исполняется, CSS 90% правил не применяется (190 KB). Механически чистить нельзя — coverage видит только снятый сценарий. Риск не окупается, в очередь не ставить.
- **Здоровое, не трогать:** утечек нет (3 цикла обхода: heap −0.8 MB, listeners −48), скролл 60fps (p95 17.7 мс, 0-1 пропуск), повторный визит FCP 136 мс / 64 из 64 из SW-кеша, CLS 0.
- **Дыра в измерениях:** всё намерено в headless-хроме на машине LEAD. RUM (`web-vitals` → свой эндпоинт, с уважением к airgap-режиму) в проекте нет — что происходит в зале на самом деле, не знает никто. Кандидат в бэклог, не в очередь.

### 5. W — прополка правил — 🔶 docs-часть ✅ 2026-07-17
Хендоффы и NEXT_SESSION.md прополоты и синхронизированы. Остаток (с Gio): ревизия правил CLAUDE.md — карточка в `HANDOFF_next_cards.md`.

---

## Журнал полевых тренировок (счётчик DoD-1)

| Дата | Версия | Баги / раздражения | Серия |
|------|--------|--------------------|-------|
| 2026-07-11 | 1.23.0 | багов нет; раздражение: rest-кнопки тяжёлые → карточка P-1 | 1 |
| 2026-07-15 | 1.24.1 | багов нет; лонг-пресс → Island Settings работает; аудит idle-острова → карточки L3 (решено: нейтральная точка) + L4 GESTURE-UNIFY | 2 |
| 2026-07-17 | 1.24.1→1.24.2 | багов нет; полевой чек L4 зелёный (long-press → настройки из любого режима) → релиз 1.24.2 (CHROME-LAW + L3 + L4, SW v108) | 3 |
| 2026-07-26 | 1.25.11 | 5 находок → F-1 краш Athlete Room (P0) · F-2 два «next» (PiP vs остров) · F-3 системный календарь на DOB · F-4 стиль тостов · F-5 пиксельный сплеш. Разбор F-1 дал шестую: F-6 классификатор ошибок выдаёт «нет соединения» на любой упавший ленивый импорт | **0** (серия обнулена: F-1/F-2 — настоящие баги) |
