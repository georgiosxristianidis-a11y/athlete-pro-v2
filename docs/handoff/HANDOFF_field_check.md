# HANDOFF — полевой чек-лист (за Gio)

> Заведён 2026-08-01. Здесь собраны ВСЕ карточки, где код на проде, гейт зелёный, но DoD-ступень 5 (полевое подтверждение) не пройдена.
> Правило: пока пункт не подписан, карточка не закрыта — сколько бы ни было зелёных тестов.
> Формат ответа от Gio — свободный («3 ок, 5 — сплеш моргает»). LEAD разносит по карточкам и хендоффам сам.
> Жизненный цикл: подписанный пункт вычёркивается отсюда и помечается в родном хендоффе. Все подписаны → файл в `docs/_archive/`.

---

## ПЕРЕД ЗАХОДОМ — включить два флага

Профиль → AI → тумблер **«Живой маскот»** (`fab-video`).
Барабан — через консоль: `Flags.setFlag('drum-window', true)`.

Оба сейчас OFF: код на проде с 1.25.2 / 1.24.4, но пользователь его не видит. При вердикте «ок» — дефолт ON следующим релизом. Обратно выключается той же строкой, без отката Git.

---

## СПИСОК А — дома, ~5 минут, до зала

Прогонять **до** тренировки: если что-то всплывёт, оно не смажет тренировку и не обнулит серию DoD-1 зря.

| # | Что сделать | Что должно быть | Карточка |
|---|---|---|---|
| A1 | **Профиль → DATA → экспорт.** Открыть скачанный файл | файл открывается, тренировки на месте | BACKUP / DS1 (`HANDOFF_gym_grade.md`) |
| A2 | Закрыть приложение полностью, открыть заново | сплеш чистый, без вспышки и сдвига при входе | F-5 UI-SPLASH |
| A3 | Открыть панду (маскот), затем **выключить сеть** и открыть снова | панда играет офлайн | F-7 SW-MEDIA-BUDGET |
| A4 | Дашборд → «Все» рядом с Recent → полистать журнал, поискать упражнение | подгрузка по прокрутке плавная, клавиатура не сбивает фокус в поиске | LOG-1 (`HANDOFF_next_cards.md`) |
| A5 | На плохой сети (или с троттлингом) открыть Профиль | открывается мгновенно, не ждёт сеть | профиль 1.25.19 |

**A1 идёт первым не случайно** — он одновременно и чек, и страховка перед всем остальным.

---

## СПИСОК Б — в зале, по ходу обычной тренировки

Ничего специально делать не надо, кроме отмеченного. Это побочный эффект тренировки, а не тест-сессия (правило POLISH-LOOP).

| # | Что смотреть | Что должно быть | Карточка |
|---|---|---|---|
| Б1 | **Крутить вес в барабане** (флаг включён) | плавно, вес верный на всех упражнениях. Особенность: один флик = ±50 кг, дальше второй флик после рецентра | DRUM-PERF-2 |
| Б2 | Кнопки `+15с` / skip во время отдыха | легче на глаз, попадание потными пальцами **не хуже** прежнего | P-1 ISL-REST-BTN |
| Б3 | Закрыть последний подход упражнения → отдых → **свернуть приложение** | «далее: X» в окне PiP и в острове показывают **одно и то же** | F-2 ISL-PIP-NEXT |
| Б4 | Пилюля острова во время отдыха, длинные русские имена упражнений | «далее: X» не ломает пилюлю, обрезается многоточием | ISL-REST-NEXT |
| Б5 | **В темноте, на минимальной яркости:** Analytics · Профиль-privacy · сводка тренировки · вкладки Замеров | ровные плоские поверхности, нет грязных пятен, ореолов и «светящихся» плашек | AIR-4 + AIR-2b (закрывают DoD-2) |
| Б6 | Маскот-панда (флаг включён) | звук/синхрон/уши/постер в порядке, батарея не проседает заметно | fab-video |

---

## ЗАОДНО — одна диагностика

**Точка над островом.** Если она **красная** — посмотреть, какой режим приватности стоит в этот момент (Профиль → приватность).

- Красная при **cloud/anon** и лежащей сети → **не баг**, так задумано, вычёркиваем.
- Красная при **airgap** → настоящий баг, заводим карточку с репро.

Почему так: `deriveDotState` (`js/shared/sync-dot.js:28`) первым же условием отдаёт `airgap` → серый, до сетевых веток дело не доходит. Значит красная в airgap возможна только если режим на самом деле другой, либо `_updateNetworkStatus` не вызвался после смены режима. Аномалия висит с 2026-07-27 неразобранной.

---

## Что этот заход закрывает

- **11 карточек** переходят из «код готов» в «закрыто по DoD».
- **DoD-2** (AIR-чистота) закрывается целиком — Б5 её последний хвост.
- **DoD-5** (история неубиваема) — A1 её последний хвост.
- **DoD-1** (серия 10 тренировок без нового бага) — сейчас **0/10**, этот заход открывает счёт.
- Два флага получают вердикт и становятся дефолтом ON.
