# HANDOFF — сторож main

> Узкий файл под одну фазу: довести `.github/workflows/main-watchdog.yml` до живого состояния.
> Живёт, пока есть невзятая карточка. Все взяты и влиты → переезд в `docs/_archive/`.

## Где сейчас правда

**Сторож жив с 1.27.26 и проверен на живых прогонах — WATCH-1 закрыта.** Ниже история
болезни; актуальная задача одна, WATCH-2.

**Было:** поставлен в 1.27.23, за всё время отработал один раз и упал:
единственный прогон — `failure` со `Unhandled error: HttpError: Resource not accessible by
integration`. `permissions:` не содержал `pull-requests: read`, а `github-script` зовёт
`GET /repos/{owner}/{repo}/commits/{sha}/pulls`. Всё, что не перечислено в явном блоке
`permissions:`, получает `none` — отсюда 403.

Убивает не один сигнал, а оба: необработанная ошибка валит job целиком, вместе с проверкой
зелёности CI, которая идёт **до** запроса PR. То есть дыра, ради которой сторож делался,
всё это время стояла открытой и выглядела закрытой.

**Стало:** фикс влит PR#159 (1.27.26). Проверять состояние ancestry, а не эту строку:

```bash
git fetch origin && git log origin/main --oneline -- .github/workflows/main-watchdog.yml
```

## Что в ветке

- `pull-requests: read` (связанные PR) + `actions: read` (прогон CI по `run_id`).
- Каждый вызов API обёрнут. 403/404 на `/pulls` деградирует в issue «сторож не смог
  проверить происхождение коммита» с отдельным заголовком — ослепший сторож не обвинитель.
  Отказ дедупа → предупреждение и issue всё равно. Отказ `getCommit` → тело issue со
  ссылкой, собранной из SHA. Падаем намеренно ровно в одном месте: не вышло завести issue,
  там сигналу больше некуда деться.
- Дедуп переехал с `search.issuesAndPullRequests` на `issues.listForRepo`: поисковый индекс
  отстаёт на минуты, на быстрой серии коммитов сторож завёл бы второй issue на тот же SHA.
- `workflow_dispatch` с входом `run_id` — единственный способ проверить сторожа, не пушнув
  в `main` напрямую.
- Baseline воспроизведён на моках Octokit, 14 сценариев: старый скрипт на 403/404 падает
  тем же `HttpError`, новый — заводит issue и живёт.

## WATCH-1 — ЗАКРЫТА 2026-08-07, проверена обеими сторонами

Не рассуждением: обе половины подтверждены логами живых прогонов.

**Чистый мёрж — сторож зелёный.** Прогон `31176132054` (`workflow_run`, `success`) на
мёрж-коммите `27227de`:

```
main 27227de: CI зелёный, PR #159 — чисто.
```

Это и есть доказательство, что 403 ушёл: строка печатается только после успешного
`listPullRequestsAssociatedWithCommit`.

**Нарушение — сторож заводит issue.** Прогон `31178164276` (`workflow_dispatch`, `success`):

```
Ручной прогон сторожа на CI #29770664500: ветка main, итог success, коммит 4ad7117.
##[warning]Заведён issue .../issues/160
```

Issue #160 — «Сторож main: 4ad7117 приехал мимо ворот», находка «нет связанного смёрженного
PR». `4ad7117` (20 июля) — настоящий прямой пуш в `main`: CI на нём зелёный, PR нет. Найден
перебором ~180 прогонов CI на `main`; позже 27 июля нарушений нет, свежим не заменить.
Issue оставлен как артефакт проверки, закрыть после взгляда Gio.

Способ прогнать сторожа руками, не пушнув в `main`, остаётся рабочим инструментом:

```bash
gh workflow run main-watchdog.yml --ref main -f run_id=<ID прогона CI>
```

## WATCH-1-GATE — ЗАКРЫТА 2026-08-07 (1.27.27)

Проверка выше была одноразовой: 14 сценариев на моках прогнали и не закоммитили. Регресс
не сторожился ничем — следующий забытый `permissions:` воскресил бы тот же баг, и заметить
его снова можно было бы только вручную.

`test/main-watchdog.test.js` — гейт вокруг воркфлоу, сам воркфлоу не тронут. Два класса:

- **Поведение.** Скрипт достаётся из `script: |` разбором по отступам (без `js-yaml` — он
  транзитивный, и по нему только что был high), исполняется через `new Function` с моками
  Octokit. На автоматическом пути ни один отказ API не роняет сторожа и не отменяет issue.
- **Права.** Статическая сверка «зовёшь эндпоинт → имей право». Моки не знают про
  `GITHUB_TOKEN`, `permissions:` не знает про код — соединяет только этот файл.

**Список эндпоинтов снимается с самого скрипта** прогоном-разведчиком: новый вызов API
попадает под гард сам, без правки теста.

Baseline воспроизводится командой, ребейз назад не нужен:

```bash
git show d9c248b:.github/workflows/main-watchdog.yml > /tmp/old.yml
WATCHDOG_WORKFLOW=/tmp/old.yml node --test test/main-watchdog.test.js   # 11 из 18 красных
```

Оба обязательных пункта краснеют: `отказ listPullRequestsAssociatedWithCommit уронил
сторожа` и `в permissions нет pull-requests: read`. На сегодняшнем `main` — 18 из 18.

### Находка гейта, не закрытая здесь → WATCH-3

`actions.getWorkflowRun` — единственный вызов, оставшийся без `try/catch` (строка 66).
403 на нём убивает ручной прогон необработанной ошибкой, тем же способом, что и баг 1.27.23.

Тяжесть низкая и потому оставлено: ручной прогон смотрит тот, кто его запустил, — сигнал
не теряется в тишине, в отличие от автоматического пути. Правка воркфлоу вне границ этой
карточки, файл проверен живыми прогонами. Тест фиксирует поведение **как есть** (отказ
обязан быть виден: `setFailed` или выброс), а не как хотелось бы: ужесточать контракт —
задача правки воркфлоу, и гейт её не подменяет.

Чинится четырьмя строками — обернуть в `try/catch` → `core.setFailed`. После этого в тесте
`ручной прогон: отказ getWorkflowRun не проходит молча` можно ужесточить до `assert.equal
(r.threw, null)`.

## Карточка WATCH-2 — слепое пятно: CI не запускался ВООБЩЕ

Найдено 2026-08-06 на живом инциденте, в текущем фиксе не закрыто.

Сторож висит на `workflow_run` от CI. Если CI на коммите не запускался вовсе — события нет,
сторож не просыпается, и коммит въезжает в `main` абсолютно молча. Ровно это и произошло:
в окно аварии два PR слиты в `main` с **нулём check-runs** (`gh api
repos/:owner/:repo/commits/<sha>/check-runs` → `total_count: 0`).

Хуже того, `combined status` на этих коммитах — `success`, потому что единственный контекст
там от Vercel. **Зелёная галочка в UI ≠ чеки прошли**: считать `check-runs`, а не смотреть
на цвет.

Что нужно: второй вход, не зависящий от того, добежал ли CI. Например `schedule` (раз в
час) или `push` на `main`, шаг «у HEAD `main` есть хоть один завершённый check-run от CI» →
иначе issue. Держать в том же файле и тем же принципом «не падать»: отказ API — жалоба,
а не красный прогон.

## Аномалии 2026-08-06, проговорены вслух намеренно

- **PR #157 закрыт в 19:27 UTC не автором карточки.** Закрыла сессия гейта, с явного
  согласия Gio и по названной причине: #157 нёс версию 1.27.24, а её к тому моменту занял
  влитый #155 — влиться в том виде он уже не мог, линию всё равно пришлось бы ребейзить и
  перенумеровывать. Ветка `claude/watchdog-alive` при этом не тронута.
  Работа не пропала, но переоткрыть PR не вышло: ребейз осиротил его head-коммит, GitHub на
  `reopen` отдаёт «Could not open the pull request». Пришлось заводить #159. Вывод на
  будущее остаётся в силе: закрытый чужой ветке PR **сначала переоткрыть, потом ребейзить**
  — обратный порядок необратим.
- **Версии 1.27.24 и 1.27.25 заняли** мёржи #155 и #156, пока ветка ждала; ветка ушла в
  1.27.26.
- Оба мёржа уехали в `main` без единого прогона CI (см. WATCH-2). Гейт на них не гонялся
  вообще.
- **Конфликтные маркеры пережили `npm run build:sw`.** Генератор переписывает только блок
  `ASSETS` и строку `CACHE_NAME`, остального файла не касается — `<<<<<<<` спокойно доехал
  до коммита. Поймал гейт (`npm test` и линт), но дешевле проверять самому после любого
  ребейза: `grep -rn "^<<<<<<<" --include="*.js" .`
- **SCA покраснел в CI на зелёном pre-push:** `js-yaml` 4.3.0, high (GHSA-5p4m-2wfm-xmqj),
  транзитив через `stylelint → cosmiconfig`. Адвизори опубликовано между прогонами. Лечение
  штатное — `npm audit fix --package-lock-only`, влито тем же PR.
