# HANDOFF — Трек запуска

> Заведён 2026-08-25. Один трек вместо семнадцати параллельных линий.
> Цель трека: довести приложение до состояния, в котором ссылку можно дать чужому человеку.
> Порядок карточек фиксирован и отсортирован по убыванию риска — не переставлять без разбора.
> Две линии: **LAUNCH-1..10** — продукт до раздачи ссылки; **AGENT-1..9** — координация двух
> инструментов (+ `FLOW-5`, `HYG-5`/`HYG-6` из разбора 27.08 и 10.09, см. § «Разбор работы системы»).
> `AGENT-1/2/3/4/5/6/7/8`, `FLOW-5` и `HYG-6` закрыты, открыты `AGENT-9`, `HYG-5`;
> `LAUNCH-1` закрыта 2026-08-25, `LAUNCH-2`, `LAUNCH-3A` и `LAUNCH-5A` — 2026-08-26 (релиз
> 1.27.76), `LAUNCH-3B` — 2026-08-27 (релиз 1.27.77). `LAUNCH-6` закрыта: код 2026-08-27
> (PR #271, `0ca5fb7`), ключ на Vercel проверен живым запросом 2026-08-30. `LAUNCH-5B` закрыта 2026-08-28
> (PR #272, `9601e51`). `LAUNCH-9` пункты 3–4 закрыты 2026-08-28
> (PR #273, `440c302`). `LAUNCH-4` закрыта 2026-08-28
> (PR #274, `3722ba2`). `LAUNCH-7` закрыта 2026-08-28
> (PR #275, `21ae48d`). `LAUNCH-8` закрыта 2026-08-28
> (PR #276, `a9b0d5b`). `LAUNCH-10` — этим PR. `MOTION-FIX` заведена 2026-09-11 в § «После
> LAUNCH-10» — трек не блокирует, до его закрытия не берётся.
> База: только `origin/main`. Ритуал ветки — скилл `agent-brief`. «Влито» = ancestry в `origin/main`.

## Почему трек

Пять месяцев работа шла по семнадцати линиям сразу, ни одна из которых не отвечала на вопрос
«можно ли этим пользоваться постороннему». Аудит 2026-08-25 нашёл пять дыр, каждая из которых
в одиночку блокирует раздачу ссылки. Трек их закрывает и на этом заканчивается.

Остальные семнадцать линий заморожены в `docs/_archive/` — это не отмена, а пауза: карточки
живы, вернуться к ним можно после `LAUNCH-10`. Роли LEAD/worker/verifier и DoD-лестница —
`docs/_archive/HANDOFF_orchestration.md` и `docs/_archive/HANDOFF_gym_grade.md`.

## Что вскрылось (проверено 2026-08-25)

| Находка                                                       | Чем проверено                                                     | Карточка |
| ------------------------------------------------------------- | ----------------------------------------------------------------- | -------- |
| ИИ-тренер на проде не отвечает — серверные ключи не настроены | живой запрос к проду                                              | LAUNCH-6 |
| Первый экран нового пользователя английский                   | аудит: дефолт `locale.store.js` + `dashboard.js`                  | LAUNCH-5 |
| Тесты никогда не гонялись в движке Safari                     | `playwright.config.js` — один проект `chromium`/Pixel 7           | LAUNCH-7 |
| Пять обновлений библиотек висят месяц                         | PR #1–#5, все от 2026-07-26; `npm outdated` = 13 пакетов          | LAUNCH-2 |
| Юридических документов нет, сайт открыт поисковикам           | `git ls-files` — только `privacy.*` (экран настроек, не документ) | LAUNCH-3 |

Уточнение по последней строке: экран приватности в приложении есть и работает. Отсутствует
именно текст — политика, условия, согласие на обработку. `LAUNCH-3` пишет документы и
связывает их с уже существующим экраном, а не строит экран заново.

---

## LAUNCH-1 — аудит пути новичка — ЗАКРЫТА

Закрыта 2026-08-25 вторым заходом, с кликами. Разбор — в § «Находки аудита»: слои первого
запуска, онбординга, оверлеев, ИИ, повторного входа и офлайна, десять новых дыр (F-6…F-15),
одна прошлая находка опровергнута (F-5), две поправлены (F-2, F-4). Правок кода ноль.

Главный урок постановки: **панель браузера внутри приложения не годится для интерактивного
аудита** — пока она скрыта, страница не композитит кадры, `requestAnimationFrame` не тикает,
скриншот падает по таймауту. Именно это оборвало первый заход. Инструмент второго —
Playwright поверх того же `npm run dev`, профиль Pixel 7, `locale: ru-RU`, чистый
`userDataDir` на каждый прогон. Работа для `LAUNCH-9` — там же, приоритетами.

---

## LAUNCH-2 — уборка фронта — ЗАКРЫТА

Закрыта 2026-08-26. Семнадцать линий заморожены в `docs/_archive/` — в `docs/handoff/`
остался один этот трек; `NEXT_SESSION.md` переписан на указатель в него. Закон структуры в
`CLAUDE.md` знает про заморозку; чтобы влезть в потолок 3000 токенов (было 2992 — восемь
токенов запаса), сжаты «Ритуал агента» и дубль формата брифа, правила сохранены.

**Семь заявок разобраны.** Все пять от Dependabot просили версии **ниже сегодняшних**
(`@playwright/test` 1.62.0 при 1.62.1, `eslint` 10.8.0 при 10.9.1) — вливать их значило
получить второй круг обновлений. Закрыты; вместо них один бамп dev-зависимостей до текущих,
`playwright` и `@playwright/test` парой — это же снимает мину `TOOL-PW-1` из стэша. #227
закрыт как перекрытый PRECACHE-1: бут-замыкание построено на `modulepreload`, в `index.html`
их 50. #243 (Vercel Web Analytics) тогда оставлен открытым, решение — за `LAUNCH-4`: включать
сбор данных до политики и согласия (`LAUNCH-3`) нельзя. Закрыт 2026-09-10 разбором HYG-6.

**Рабочие копии: 14 → 10.** Судил содержимым, а не SHA — хеши мутируют на ребейзе, и три
копии, «не влитые» по ancestry, оказались влитыми патчем. Снято четыре чистых и влитых.
Остальное — ниже, хвостами.

### Хвосты LAUNCH-2 (не входили в ГДЕ СТОП)

- **Прод-зависимости не тронуты** — `@google/genai` 2.16→2.19, `@supabase/supabase-js`
  2.102→2.112, `helmet` 8.2→8.3, `express-rate-limit` 8.5→8.6 (плюс мажоры `dotenv` 17 и
  `express` 5, которые сами по себе карточка). Отдельным заходом, не попутно с dev-тулингом.
- **`@anthropic-ai/sdk` 0.51 → 0.121** — карточка **после `LAUNCH-6`**: прыжок трогает код
  ИИ-тренера, а ИИ на проде сейчас и так молчит; бампать оба слоя сразу — не узнать, какой
  сломался.
- **Две влитые копии держат незакоммиченный WIP**, обе живые и ценные:
  `agitated-wescoff` — `css/workout.css`, барабан (фон `#000`, `opacity` 0.22→0.12, активный
  `#fff`); **как есть не влить** — сырые hex валят `test/design-tokens.test.js`, нужен перевод
  на токены. `watchdog-perms` — `main-watchdog.yml`: `pull-requests: read` (без него
  `/commits/{sha}/pulls` отдаёт 403), отказ API больше не считается уликой прямого пуша, дубли
  issue ищутся списком вместо search API. Второе стоит своей карточки — сторож, который врёт
  на сбое API, учит закрывать его issue не глядя.
- **Корневой чекаут — живая сессия** (`claude/cards-theme6-vis1b`), пункт «корневой чекаут
  чист» в постановке протух. Работать из своего worktree; ветка в корне меняется под руками.
- **Фоновый шум подтверждён:** в ряде копий `git status` даёт ~4190 удалений — сторонний
  репозиторий из `5f87246`, в `origin/main` его нет. Реальные правки считать без него.

---

## LAUNCH-3 — юридический минимум, сразу двуязычный

Разрезана надвое по файловому набору, как `LAUNCH-5`: тексты живут вне `js/`, связка с
приложением — внутри. Половина A закрыта 2026-08-26 (1.27.76), половина B — 2026-08-27
(1.27.77). Разрез был виден из поля ФАЙЛЫ до начала работы — то, ради чего заведён `AGENT-3`.

### LAUNCH-3A — документы и прямые ссылки — ЗАКРЫТА (1.27.76)

Закрыта 2026-08-26, PR #261 (Cursor). Шесть страниц по `/legal/<doc>.<lang>.html` — политика,
условия, согласие × `ru`/`en`; тексты сверены с кодом (`js/claude.store.js`, `/api/coach`,
`js/usage.js`), не списаны с шаблона. Язык — отдельным файлом, а не чтением состояния:
`getLang()` вне шелла недоступен. `legal/**` добавлен в `includeFiles` — без этого прод отдаёт
шелл вместо документа, и локальный зелёный ничего не доказывает. Прекеш не вырос: `build-sw.mjs`
каталог `legal/` не сканирует. Гард — `test/legal-docs.test.js`. Проверено на проде:
`/legal/privacy.ru.html` отдаёт 200 на 1.27.76.

**Осталось за Gio:** юридическая пригодность текстов. Карточка дала черновик и связки.

### LAUNCH-3B — связка с приложением — ЗАКРЫТА (1.27.77)

Закрыта 2026-08-27, PR #269 (Cursor). В карточке приватности три ссылки на
`/legal/<doc>.<lang>.html` по `getLang()`; отдельная вкладка (`rel="noopener"`), иначе уход
из PWA рвёт сессию. Наряд совпал с диффом, `sw.js` пересобран командой, драйвер `AGENT-5`
снял конфликт по `CACHE_NAME`. Бамп 1.27.77 — в этом же PR.

---

## LAUNCH-4 — статистика — ЗАКРЫТА (PR #274, 3722ba2)

Закрыта 2026-08-28. USAGE-1 на проде уже зелёная (`/_vercel/insights/script.js` 200).
Флаг `usage-stats` ON; скрипт только за воротами `js/usage.js`. PR #243 закрыт (2026-09-10) —
безусловный тег обходит DNT/GPC, тумблер и airgap. Дашборд только у Vercel.

---

## LAUNCH-5 — русский язык целиком

Аудит `LAUNCH-1` разрезал карточку надвое: механизм языка в проекте есть и работает, выпал из
него один файл. Половины берутся раздельно, наборы файлов не пересекаются.

### LAUNCH-5A — дом через словарь — ЗАКРЫТА (1.27.76)

Закрыта 2026-08-26, PR #255 (Cursor). `s-home` целиком через `t()` — пустое состояние, тосты,
`title`. Паритет ключей `en`/`ru` сторожит `test/locale-parity.test.js`; попутно убраны шесть
мёртвых `metrics.*`, из-за которых англоязычный новичок видел на экране сырой ключ.

**Поправка наряда, которую стоит помнить:** в выданном тексте стоял `prettier --check .` — это
красно по всему репо (226 файлов старше prettier, в CI проверка снята намеренно,
`.github/workflows/ci.yml:72`). Читать как «prettier только по своим файлам»; общий reformat —
отдельное решение, не попутное.

**Мёрж стоил ребейза:** ветка конфликтовала с `main` по `CACHE_NAME` в `sw.js` на каждом из
четырёх коммитов — обе стороны генерируемые. Разрешение: дайджест `main`, довести ребейз,
затем `npm run build:sw` пересчитывает его от финального дерева.

### LAUNCH-5B — остальные слои на t() — ЗАКРЫТА (PR #272)

Закрыта 2026-08-28, PR #272 (`9601e51`). Дефолт новичка — локаль устройства.
Онбординг, активная тренировка, профиль и хвосты аудита — через `t()`.
Переключатель EN/RU на первом экране онбординга. Гард — `test/locale-init.test.js`.

---

## LAUNCH-6 — ИИ-тренер: ключ и инструкция — ЗАКРЫТА (PR #271, 0ca5fb7)

Код влит 2026-08-27: airgap 503 не маскируется под «нет ключа» (`js/shared/ai-status.js`),
вход на доме (F-12), инструкция ключей — `docs/DEPLOYMENT.md`. Ancestry: `0ca5fb7` на
`origin/main`.

**Хвост Gio закрыт 2026-08-30.** Живой запрос к проду: `GET /api/ai-status` → `200`,
`{"gemini":true,"anthropic":false}`. Серверный Google-ключ на Vercel стоит, облачный коуч
в режиме Cloud отвечает без BYOK. `ANTHROPIC_API_KEY` не выставлен сознательно — движок
Claude остаётся выбираемым и работает только со своим ключом пользователя.

**Что этим же замером вскрылось и в карточку не входило:** индикатор ключа у FAB судил
наличие ключа по локальному BYOK и серверного не видел — красный «нет ключа» над работающим
Gemini, зелёный над неработающим Claude. Это остаток `F-13`, половину которого закрыла
`LAUNCH-6`. Ушло в `PC-2` и **закрыто 2026-08-30** (PR #285, `7cb1394`, 1.27.81): источник
правды — `probeAiStatus()`, гард `test/fab-key-state.test.js`. Линия PANDA Core закрыта
целиком — `docs/_archive/HANDOFF_panda_core_settings.md`.

---

## LAUNCH-7 — движок Safari и живой iPhone — ЗАКРЫТА (PR #275, 21ae48d)

Закрыта 2026-08-28. Проект `webkit` (iPhone 13) рядом с `chromium` (Pixel 7);
`PiP.init` пропускает видео без `captureStream`. Гард — `test/playwright-webkit.test.js`.
**Осталось за Gio:** живой iPhone (safe-area, адресная строка, фон, DRUM-TOUCH-1) —
Playwright WebKit на десктопе это не Safari.

---

## LAUNCH-8 — резервная копия — ЗАКРЫТА (PR #276, a9b0d5b)

Закрыта 2026-08-28. JSON v1 включает `plans` и allowlist `local` (план, тема; без
`ap-device-id`). Восстановление на втором чистом профиле — unit wipe + e2e два
контекста. Старый файл без `plans`/`local` импортируется. Гард —
`test/backup.test.js`, `test/e2e/backup-restore.spec.js`.

---

## LAUNCH-9 — дыры, найденные аудитом

**ЦЕЛЬ.** Закрыть то, что принесла `LAUNCH-1`. Список ниже — из § «Находки аудита», порядок
по убыванию цены для чужого человека, не по трудоёмкости.

**ГДЕ СТОП.** Пункты 1–6 закрыты и проверены тем же способом, что и найдены: чистый профиль,
прогон по слоям, факт в консоли и на скриншоте. Пункт 7 закрыт либо осознанно отложен
строкой с причиной.

1. **F-15 · дом на английском.** Вынесена в `LAUNCH-5A` — здесь не брать.
2. **F-12 · ИИ недостижим у новичка.** Закрыта вместе с `LAUNCH-6` (вход на доме).
3. **F-6 · онбординг не переживает закрытие вкладки.** Закрыта PR #273 (`440c302`):
   черновик в `ap-onboarding-draft`. Гард — `test/onboarding-draft.test.js`.
4. **F-9 · «Fast Skip» выдумывает профиль.** Закрыта тем же PR: экран заглушек,
   профиль пишется только после подтверждения. Тот же гард.
5. **F-10 · Athlete Room не закрывается системным «назад»** — закрыта (1.27.82):
   реестр оверлеев в `Nav` кладёт отдельную запись в history, Back и Escape снимают
   верхний перекрывающий слой, экран под ним не меняется. Гарды —
   `test/overlay-back.test.js` и три e2e-кейса в `test/e2e/regressions.spec.js`.
   NAV-BACK-1 (единый шеврон) по-прежнему в архиве дизайна, сюда не входила.
6. **F-11 + остаток F-5 · доступность** — закрыта (1.27.84): `set-check`, барабаны и
   ручка перестановки, степперы отдыха и глаз ключа несут `aria-label`; онбординг —
   `role=dialog` `aria-modal`, шелл и FAB — `inert`. Гард — `test/a11y-f11.test.js`.
7. **F-7/F-8 · молчаливые дефолты** — закрыта (1.27.87): пол и приватность
   не пресетятся в `blankOnboardingData`; шаг 5 не держит `_navButtons(true)`.
   Fast Skip по-прежнему показывает заглушки и ждёт подтверждения. Гард —
   `test/onboarding-defaults.test.js`.
   **Второй вход добит 12.09 (1.27.100).** `_obQuickStart` перетирал уже
   выбранный пол заглушкой `'m'`: кнопка стоит на шаге 1, но `ob:prev`
   возвращает туда с заполненного шага 3. Пока Navy читала легаси-ключ, цена
   была нулевой; после `615e73b` (`resolveSex` берёт `profile.sex`) женщина
   после Quick Start снова получала мужскую формулу. Урок для закрытых
   карточек: гард ловил только тот путь, которым баг нашли.

**НЕ ТРОГАТЬ.** Всё, что аудит не назвал. Карточка не повод для общей уборки кода. F-3
(`<html lang>`) и F-13 (`/api/ai-status` 503) отдать в `SEO`/`LAUNCH-6`, здесь не чинить.

---

## LAUNCH-10 — чеклист готовности и раздача ссылки

**ЦЕЛЬ.** Пройти чеклист целиком и отдать ссылку живому человеку.

**ГДЕ СТОП.** Все карточки трека влиты (ancestry в `origin/main`), релиз доехал до прода,
чеклист пройден на живом устройстве, ссылка отдана.

**НЕ ТРОГАТЬ.** Ничего нового в этот заход. Карточка проверяет, а не строит.

### Этим PR

Чеклист с честным статусом — `docs/LAUNCH_CHECKLIST.md` (таблица пунктов, ancestry,
блокеры). **Ссылка для раздачи:** https://athlete-pro-v7.vercel.app (`docs/DEPLOYMENT.md`).

**Статус на 2026-08-30:** авто-пункты 2, 3, 5, 6, 7 и CI п. 4 — PASS; п. 1 — авто PASS,
живой телефон не гонялся. Ключи Vercel выставлены, `PC-2` влита — **кодовых блокеров
не осталось**. **Трек не закрыт** и ждёт только поля: живой iPhone, прогон новичка на чистом
телефоне, взгляд на индикатор FAB глазами. Все три — Gio, одним заходом.

---

## BOOT-TRIM — бут-граф без Athlete Room, Integrity и панды — ЗАКРЫТА (PR #321)

Закрыта 2026-09-10. Комната и Integrity — по тапу/idle, панда — по empty-state/overrun;
`modulepreload` подрезан. Два ревью-факта, оба стоили правки поверх исходной карточки:

- **Пустой Home ждал чанк панды.** `_buildEmptyState()` стал `async` и `await`-ил
  `import()` до записи разметки: при `fab-video` (ON) первый заход держал пустой `s-home`
  до сети, а reject импорта ронял весь `Dashboard.load()`. Теперь разметка синхронная,
  `<video>` уезжает без `src`, адреса и mood приезжают вторым кадром в `_hydrateMascot()`
  с `catch` (чанк не доехал — Home рабочий, кеш промиса сброшен под следующую попытку).
- **Прелоад — не хинт, а сборка прекеша.** По `<link rel=modulepreload>` в `index.html`
  `scripts/build-sw.mjs` собирает install-фазу. `strength-engine.js` и `profile.store.js`
  сняли как «транзитивы Athlete Room», а они приходят статикой через
  `claude.store.js` ← `dashboard.js` — то есть остаются в первом кадре и уехали в
  `ASSETS_WARM`. Холодный офлайн-старт сразу после апдейта падал на пустом кеше.
  Прелоад вернули; тем же ходом закрыт унаследованный из `main` долг того же класса —
  `shared/ai-engine.js` и `usage.js`. Install-фаза 0.602 → 0.647 MiB при потолке 0.75 (F-7).

**Гарды — `test/boot-graph.test.js`:** замыкание статических `import` от entry-скриптов
`index.html` обязано целиком лежать в `ASSETS` (без allowlist); `_buildEmptyState` обязан
остаться синхронным, `_hydrateMascot` — нести `catch`.

**Урок общего действия:** «модуль не на критическом пути» — утверждение о графе, а не о
намерении. Проверяется обходом транзитивных статических `import` от entry, не чтением
списка прелоадов: `claude.store.js` тянул за собой два модуля, которых никто не ждал.

---

## После LAUNCH-10

> Секция для работы, которая не блокирует раздачу ссылки и сознательно ждёт закрытия трека.

### MOTION-FIX — разбор моторики 1.27.82 — НЕ БРАТЬ ДО ЗАКРЫТИЯ LAUNCH-10

**ЦЕЛЬ.** Закрыть находки постфактум-аудита коммита `399311c` (`feat(motion): compositor-only
springs for switch, tabs, shake, stepper`, 1.27.82). Четыре эффекта уехали одним пакетом и до
поля — вопреки собственному порядку внедрения того же плана: «одна карточка на P1, отдельная
на P2, P3/P4 только после полевой проверки, не пакетом». Из четырёх чисто сделан один.

**ГДЕ СТОП.** Пилюля в модалке плана едет от предыдущей кнопки, а не от левого края, и не
дёргается при наборе в поиске. `npm test` зелёный с новым гардом на конечные `transition`.
Полевой прогон прошёл, вердикт по `.bump` записан строкой.

**НЕ ТРОГАТЬ.** Rest-бар и `update()` во время отдыха. `.set-check` — без transition,
мгновенный scale + haptic. `island-set-pulse`. `Spring.animate` на дашборд-барах.
`.switch-thumb` — единственная часть, сделанная ровно по плану (один литерал,
`0.25s var(--ease-std)` → `0.26s var(--ease-spring)` на `transform`), претензий нет.

#### Что вскрылось

| Находка                                                                                                                                                                                                                                                                              | Где                                                      | Решение                                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Глайд пилюли фиктивный. `render()` перезаписывает весь оверлей на клик вкладки, клик недели и **каждый символ поиска**; пилюля каждый раз новый элемент и едет от `translateX(0)`. A→B едет, B→A щёлкает; при наборе дёргается от левого края на символ                              | `js/workout.view/modals.js:161`, `:244-257`              | Дефект — полный re-render, не пилюля. Частичный рендер списка, шапка переживает клик                                                                                                                                                            |
| `initSegPill` не идемпотентен: повторный вызов на живом хосте пришьёт второй `<span class="seg-pill">`, второй click-listener и второй `ResizeObserver`, старый не `disconnect`. Сейчас не стреляет только потому, что хост каждый раз новый — то есть починка выше эту мину взводит | `js/ui/seg-pill.js:19-47`                                | Идемпотентность + снять RO с кнопок (наблюдать хост), `disconnect` прежнего                                                                                                                                                                     |
| `void valEl.offsetWidth` — forced sync layout на каждый тап степпера, Train live. Прямо запрещено формулировкой плана «без `offsetHeight`-reflow»                                                                                                                                    | `js/workout.view/handlers.js:90`                         | `el.getAnimations().forEach(a => a.cancel())`. `js/shared/field-shake.js:11` **остаётся** — холодный путь, цена нулевая                                                                                                                         |
| Закон «не анимировать `left`/`width`» не сторожит никто: `test/css-infinite-composite.test.js` по собственной шапке берёт только `@keyframes` под `animation: … infinite`, конечные переходы не в счёт                                                                               | `test/css-infinite-composite.test.js`                    | Новый гард по образцу PERF-2 на конечные `transition` с layout-свойствами. Замороженная инвентаризация — три адреса: `css/athlete-room.css:1012`, `css/base.css:2323`, `css/intel.css:402`                                                      |
| Раскатка P2 планировалась на `.stats-subnav` и `.bs-sex-segment` — у обоих нет ни одного производителя в `js`/`html`, правила в `css/base.css:1327` красят то, чего нет                                                                                                              | `css/base.css:1327`                                      | Снести оба из CSS                                                                                                                                                                                                                               |
| `.privacy-segment` — единственный живой непокрытый сегмент; в продукте два разных поведения одного контрола                                                                                                                                                                          | `js/privacy.view.js:55`, `js/island-settings.view.js:44` | Подключить. Не одна строка: `display: grid` + `padding: 4px` требуют своего `--seg-pill-inset`, радиус кнопок `--r-s` против `--r-xs` у пилюли, три акцента режимов = три модификатора, `:active { scale }` на кнопке без такого же на подложке |
| Старые `.week-seg-btn.active` с сырыми `rgba(0,230,118,…)` живы как фолбэк под `background: transparent` от `.seg-pill-host`                                                                                                                                                         | `css/base.css:1340-1348`                                 | Снести или перевести на токены — сырьё против design-закона                                                                                                                                                                                     |
| Закон кадра, которым судились все четыре эффекта, в репозитории не записан нигде: `.claude/rules/design.md` (39 строк) не содержит ни `transform`, ни `ease-`, ни слова о моторике. Живёт в тексте промпта, то есть дрейфует вместе с чекаутом                                       | `.claude/rules/design.md`                                | Секция Motion, 8-10 строк: compositor-only, отклик ≤300ms, `prefers-reduced-motion` глобально, reflow-ретриггер только вне горячего пути, **хост анимации состояния не пересоздаётся рендером**                                                 |

#### Решения, принятые сознательно

- **`seg-pill` остаётся** — глайд признан частью языка продукта; схлопывание до CSS-подсветки
  отклонено.
- **Третья ветка в `if`, не опции.** `syncSegPill` выбирает модификатор через
  `host.classList.contains('week-segment')` / `'plan-tabs'` — модуль хардкодит имена своих
  клиентов. Обобщение через `opts.pillClass` отклонено. Долг записан: после третьей ветки
  модуль знает четыре имени экранов и красит три — `.bs-tab-bar` проваливается мимо всех
  веток и остаётся нейтральным.
- **Поле — один прогон после фиксов**, сценарий: сессия 20+ сетов, тапы степпера, отдых.
  Судит критерий отката P4 из плана: «плывёт на 20-м сете — снимаем, haptic остаётся».
  Риск порядка назван: если поле скажет «снимаем», правка reflow в `_bumpStepperVal` окажется
  выброшенной. Пять строк, не ноль.
- **Гард едет вместе с фиксами.** Он зелёный сразу (инвентаризация покрывает три известных
  адреса) и мог бы уйти раньше — вариант «только гард сейчас» отклонён. До взятия карточки
  регресс на `left`/`width` ловится только глазами.
- **E2E на seg-pill не заводится.** Гард из строки выше не ловит «пилюля не смонтировалась»,
  и это принято: модалка плана дорога в прогоне, а Playwright тут уже цеплялся к чужому
  серверу на `:3000`.

#### Смежное — вскрылось 2026-09-11, в аудит `399311c` не входило

`a2c94a2` (4 сентября) закрыл двойной драйвер на `.orm-bar-fill`: CSS `transition` по тому же
`transform`, который гонит `Spring.animate`, перезапускался каждый кадр пружины. Это **третий**
случай одного класса — 1.27.89 (шторка метрик), 1.27.90 (confirm), теперь 1RM-бары — и каждый
закрыт точечным гардом на один селектор (`test/orm-bar-motion.test.js`).

Отсюда задача, которой нет ни в одной карточке: **общий гард на dual-driver** — CSS `transition`
по свойству, которое на том же селекторе гонит `Spring.animate` из `js/`. Делать одним заходом
с гардом на конечные `transition` из таблицы выше, иначе в `test/` ляжет два похожих теста,
каждый про свой селектор.

Инвентаризация трёх адресов перепроверена на свежем `origin/main` 2026-09-11 и не изменилась:
`css/athlete-room.css:1012`, `css/base.css:2324` (сместилась на строку), `css/intel.css:402`.

**Долг, к карточке не относящийся.** Комментарий над `.switch-wrap:active` обещает
«кругляш растягивается в капсулу», а `width` не стоит в `transition` ни в одной ревизии —
ширина щёлкает мгновенно. Пред-существующее, `399311c` тут ни при чём.

### EX-RU-1 — показ nameRu — ЗАКРЫТА (1.27.113)

При `lang=ru` пикер (добавление и замена) и карточка активной тренировки показывают
`nameRu`; поиск в пикере ловит кириллицу. Идентификатор не тронут — `ex.name` и
`dataset.name` остаются английскими в плане, истории и 1RM, так что кейс 0 кг от
2026-07-08 не повторяется. Резолв в `js/shared/exercise-label.js` (чистый, без стора):
exact → `DB`→`dumbbell` → двунаправленный substring → таблица оверрайдов для сидовых
имён без пересечения с библиотекой. Гард — `test/exercise-label.test.js`: каждый лифт
`PPL_GIO_PLAN`/`PPL_HYBRID_PLAN` обязан дать непустой `nameRu`, плюс source-гард, что
пикер пишет в план `ex.name`, а не подпись.

**Хвост за Gio (поле):** Safari RU — поиск кириллицей, старт Push на русском,
переключение на EN возвращает английские подписи с теми же весами. Журнал, остров,
сводка, аналитика, дом, intel остались английскими — это следующая карточка.
---

## Линия VOICE — озвучка тренера

### VOICE-2 — живая волна на озвучке — ЗАКРЫТА (1.27.93)

Карточка лежала только в закрытом PR #294, код — в невлитом `f7fdb95`. Взято из истории и
переписано, а не перенесено: оригинал нёс две мины.

- **Мина имени.** `.intel-wave-bar` в `main` занят волной кнопки отправки. Глобальный
  `querySelectorAll('.intel-wave-bar')` из `f7fdb95` угонял её столбики и оставлял на них
  инлайновый `transform` после конца реплики. Своё имя — `.intel-voice-wave-bar`.
- **Мина звука.** `createMediaElementSource` уводит звук из `<audio>` в граф: не поднялся
  контекст — голоса нет вовсе. Теперь элемент не трогаем, пока `ctx.state !== 'running'`, а
  волна в этом случае идёт ключевыми кадрами. Бросок при сборке графа возвращает источник
  прямо в `destination`, а не в тишину.
- **Что осталось за бортом сознательно.** Пресеты тембра и скорости из того же `f7fdb95`
  (`routes/coach.js`, настройки) — это другая карточка: голос, а не его визуализация.
  Пульсация маскота оттуда же мертва — класса `.panda-sprite` в `main` нет; живой кандидат —
  реальная реактивность `.talk-wave` у реплики панды, но это линия PANDA-C.
- **Файлы:** `js/intel.voice-wave.js` (новый) · `js/intel.view.js` (слот + все выходы
  `speakText`) · `css/intel.css` · `test/voice-wave.test.js`.
- **Полевой чек за Gio:** iPhone, «Озвучить» на карточке ответа и «СВОДКА» — столбики обязаны
  ходить по голосу, а не ровным циклом; звук не должен пропадать ни разу.

#### Как проверить

- **Без ключа и без квоты** — спека `test/e2e/voice-wave.spec.js`: отчёт и ответ TTS
  подменяются на уровне сети, звук синтетический (тон с паузами, иначе «столбики двигаются»
  прошло бы и на ключевых кадрах отката). Гонять на своём порту, иначе Playwright
  переиспользует чужой `:3000` и проверит чужой код:

  ```bash
  PORT=3456 node server.js &
  E2E_BASE=http://localhost:3456 npx playwright test test/e2e/voice-wave.spec.js --project=chromium
  ```

  WebKit спека пропускает намеренно: автоплей blob-аудио после сетевого `await` там
  недетерминирован, движок Safari закрывается полем, а не флаком в CI.

- **Живьём, если ИИ отвечает ошибкой** — сперва два переключателя, а не разбор волны:
  приватность **Cloud** и ИИ включён (дефолт установки airgap рубит запрос до сети), движок
  **Gemini** (Anthropic на бэкенде без ключа — карточка AI-1, там 500 на любом действии;
  прод-зонд `/api/ai-status` отвечает `{"gemini":true,"anthropic":false}`). Ошибку видно в
  ленте STREAMING_LOGS строкой `[ERROR]`.
- **Волна не появится вовсе, если озвучка не доехала** — она стартует только после успешного
  ответа TTS. `[ERROR] Voice synthesis failed` в логе = смотреть ключ и квоту, а не визуал.
- **Отличить реактивность от отката:** по голосу столбики проседают в паузах между словами;
  ровная синусоида без провалов = Web Audio не поднялся, идёт запасной путь.

---

## Линия AGENT — координация двух инструментов

> Заведена 2026-08-25 после того, как Claude и Cursor одновременно писали в корневой чекаут:
> `NEXT_SESSION.md` правился, пока его читали как стабильный. Линия не про качество моделей,
> а про то, чтобы два премиальных инструмента не мешали друг другу.

### Роли

**Claude — оркестратор.** Карточки, архитектура, гарды, приёмка, мёржи. Пишет в worktree.
**Cursor — исполнитель.** Реализация внутри файлового набора карточки. Тоже worktree:
корневой чекаут не отдаётся никому и остаётся чистым зеркалом `main`.
**Gemini — снят с пути записи.** Остаётся на двух работах, где он объективно сильнее: полевой
чек по скриншотам с живого телефона и сплошное чтение репо, где объём контекста важнее
точности. Его выход — отчёт в карточку, не коммит.

### Что сюда сознательно не вошло

Контракты изоляции и приёмки не заводятся: они уже закон — `CLAUDE.md:43` (worktree),
`CLAUDE.md:44-45` (PR-only, «влито» = ancestry). Переписать их третьей копией значит нарушить
собственное правило «две копии = одна протухшая». Правило существует два месяца, а невлитых
веток 79 — дыра не в формулировке.

Гард на сырые hex в правилах Cursor тоже не заводится: настоящая утечка идёт через `js/`, где
stylelint слеп и стоит на `"severity": "warning"` (`.stylelintrc.json:13`). Это открытая
карточка DS-2, а не новая находка.

---

## AGENT-1 — подпись инструмента в коммите — ЗАКРЫТА

Закрыта 2026-08-25 этим же PR. Постановка была наполовину мимо: трейлер Claude уже писал
(207 из 246 коммитов за 30 дней) и `rebase-merge` его не теряет — `8c1a9cc` несёт
`Co-Authored-By: Claude Opus 5`. Сделано: строка Cursor в `.cursor/rules/000-core.mdc`
и счётчик `npm run scorecard` (`scripts/agent-scorecard.mjs`).

Главная поправка — **ключ соединения не SHA, а номер PR.** Гейт живёт на PR, а `--rebase
--delete-branch` переписывает SHA и сносит ветку, на которой CI гонялся. Счётчик берёт
прогоны из Actions API, группирует по PR и падает назад на карту SHA→PR, когда GitHub
оставляет `pull_requests[]` пустым — без этого запаса в выборку попадает 8 PR вместо 40.

Первый замер: 40 влитых PR, окно `2026-06-24…2026-08-25`, claude 34/37 зелёных с первого
раза, `unsigned` 3. Долю читать вместе с причиной падения — красный первый прогон в этом
репо чаще означает дрейф базы, чем плохой код.

---

## AGENT-2 — один свод правил на два инструмента — ЗАКРЫТА

Закрыта 2026-08-25, PR#250, `8c1a9cc`. `.cursorrules` не устарел, а противоречил закону:
слал в убитый `DESIGN.md`, велел звать несуществующий `bsEsc()`, предписывал сырые hex
вместо токенов и нёс хвост в UTF-16LE внутри UTF-8. Отправлен в
`docs/_archive/cursorrules-2026-08-25.md`, вместо него `.cursor/rules/*.mdc` — указатель
на `.claude/rules/*`, не копия. Сторож — `test/agent-rules-parity.test.js`: каждая ссылка
на файл существует, каждая названная функция находится грепом.

---

## AGENT-3 — наряд с файловым набором и гейтом — ЗАКРЫТА

Закрыта 2026-08-26. Шаблон в скилле `agent-brief` несёт ФАЙЛЫ (globs, открытые на запись) и
ГЕЙТ (цепочка команд, а не одна: правка `js/`/`css/` обязана начинаться с `npm run build:sw`,
иначе `test/sw-cache-name.test.js` краснеет и выглядит посторонней поломкой). Там же строка о
том, что набор сверяется с открытыми PR **до** выдачи, а не на пуше.

Обкатано на двух нарядах. `LAUNCH-5A` (25.08) подтвердил предсказание карточки: `LAUNCH-9`
пункт 1 и `LAUNCH-5` оказались одной работой по `js/dashboard.js`, и это увидели при выдаче.
`LAUNCH-3` (26.08) — поле ФАЙЛЫ разрезало карточку надвое: тексты вне `js/` ушли Cursor сразу,
связка по `js/locale.store.js` встала ждать `PR #255`. Оба раза столкновение стоило минуты
чтения вместо переделки.

Автоматизации нет и не заводится: `scripts/check-branch-drift.mjs` остаётся источником правды
на пуше, наряд даёт ранний сигнал, а не второго сторожа.

---

## AGENT-4 — гард на подпись — ЗАКРЫТА (замером, 2026-09-08)

Закрыта 2026-09-08 замером, без хука. Окно после `AGENT-1` (с 2026-08-25): влитые PR с
прогоном CI в выборке `--limit 100` несут узнаваемый трейлер; новых `unsigned` нет.
Шесть `unsigned` в полной выборке (#195–197, #218, #221–222) все старше 25.08 — история
не переписана. Гард `commit-msg` + дубль в CI не заводятся: строка не росла.

---

## Разбор работы системы — 2026-08-27

> Двое суток двухинструментной работы (25–26.08): 15 влитых PR, из них 3 от Cursor.
> Замеры разбора: `npm run scorecard --limit 40`, `git worktree list`, `preflight`.
> Что сработало и остаётся: разрез карточки по файловому набору (`AGENT-3`), правила
> указателем вместо копии (`AGENT-2`), ключ метрики по номеру PR (`AGENT-1` — данные
> подтвердили, `pull_requests[]` пуст в 39 прогонах из 40), Gemini вне пути записи.

Карточки ниже — из найденных дыр, по убыванию отдачи. Ни одна не блокирует `LAUNCH-*`.

### FLOW-5 — preflight на main — ЗАКРЫТА

Закрыта 2026-08-27 этим же PR. `preflight` печатал `[OK] сессия на main` по имени ветки и
не считал отставание вовсе (`scripts/preflight.mjs:144`). Корневой чекаут стоит на `main`
почти всегда, и правила читаются именно из него: 27.08 корень отставал на девять коммитов,
старт сессии был зелёный, а `agent-brief` прочитался в версии без полей ФАЙЛЫ/ГЕЙТ — то есть
закрытая `AGENT-3` выглядела открытой. Теперь на `main` считается расстояние до `origin/main`:
WARN с числом и `git pull --ff-only`. Гард — `test/preflight-main-freshness.test.js`.

**Хвост, сознательно не взятый:** локальные коммиты впереди `origin/main` (`ahead`) карточка
не судит. Это другая мина — прямой коммит в `main` — и ловит её защита ветки, а не старт сессии.

### AGENT-5 — генерируемое вне наряда — ЗАКРЫТА

Закрыта 2026-08-27. Постановка была наполовину мимо: «исполнитель не коммитит `sw.js`»
неисполнимо — `test/sw-cache-name.test.js` (карточка O-8) требует, чтобы закоммиченный `sw.js`
совпадал с деревом, иначе PR красный. Правильная цель не «не коммитить», а «не разрешать
конфликт руками».

Сделано: `.gitattributes` объявляет `sw.js merge=ours`, драйвер заводит postinstall
(`scripts/fix-hooks-path.mjs` — конфиг в репозиторий не коммитится, а установку делает каждый
чекаут). При rebase «ours» — это upstream, то есть версия базы; свою сторону добивает
`npm run build:sw`, считая дайджест от финального дерева. Ровно то разрешение, которое в
`LAUNCH-5A` делали вручную четыре раза подряд. Забыл пересобрать — краснеет O-8.

Гард — `test/sw-merge-driver.test.js`, поведенческий: две ветки правят `sw.js`, ребейз обязан
пройти сам; baseline без правила встаёт конфликтом (иначе тест мерил бы погоду).

Правила: `ФАЙЛЫ` наряда генерируемое не берут (скилл `agent-brief`), в своде Cursor `sw.js`
назван генерируемым. **Попутная мина того же файла:** гейт исполнителя предписывал
`npm run format` = `prettier --write .` — 226 чужих файлов в диффе и конфликт с каждой открытой
веткой. Заменено на prettier по своим файлам; это же было корнем каскада в `PR #255`.

### AGENT-6 — счётчик чинить, метрику сменить — ЗАКРЫТА

Закрыта 2026-08-27, `3573389` (Cursor). Дефолтный вызов больше не падает: список PR берётся
без поля `commits`, коммиты догружаются по одному PR — потолок нодов GraphQL не достигается.
Появилась графа **«LEAD поверх»**, ошибки `gh` печатают настоящую причину. Гард —
`test/agent-scorecard.test.js`.

Первый замер на живой команде (окно 07.08–27.08, 100 PR): claude 90 PR / 92% зелёных с
первого раза, cursor 4 PR / 100%, `unsigned` 6. **«LEAD поверх» = 2** — оба на PR Cursor
(#255, #256), по одному коммиту приёмки. То есть цена приёмки, ради которой карточка
заводилась, теперь видна числом, а не догадкой.

**Разблокировала `AGENT-4`:** команда из её постановки запускается, `unsigned` = 6 на окне.

### AGENT-7 — гейт наряда проверяется до выдачи — ЗАКРЫТА

Закрыта 2026-09-08. Скилл `agent-brief` требует `# verified @ <sha>` у поля ГЕЙТ;
механика — `npm run brief:gate` (`scripts/check-brief-gate.mjs`). Известная мина
`prettier --check .` (LAUNCH-5A / `98e281a`) отвергается до запуска. Гард —
`test/brief-gate.test.js`.

### AGENT-8 — префикс ветки называет инструмент — ЗАКРЫТА

Закрыта 2026-09-15. Имя ветки сверяется с трейлером `Co-Authored-By:` —
`scripts/check-branch-prefix.mjs` (`npm run prefix:check`), два слоя: pre-push (судит по
целевой стороне рефспека, как Main Block) и шаг в обязательном job `test`. Владельца
назначает ПЕРВЫЙ подписанный коммит — та же формула, что у `scorecard` (AGENT-6), поэтому
бамп версии от LEAD поверх готовой работы Cursor имя не меняет. Ветка без известного
трейлера (`dependabot/*`) не блокируется: это графа `unsigned`, вопрос AGENT-4. Гард —
`test/branch-prefix.test.js` (18 кейсов, 11 на проход). Обход: `PREFIX_OK=1`.

**Хвост закрыт 15.09:** все четыре названные ветки (`ai-1-ui-lockout`, `arch-a1-complete-session`,
`arch-a2-integrity-check`, `a11-scorecard-close`) мертвы — первая влита PR #362 до включения
гарда, остальные три оказались влитыми по patch-id и сняты прополкой HYG-5. Оставшиеся пять
живых без префикса — в `HYG-7`. Ветку открытого PR переименовать нельзя, не потеряв PR, —
такой PR вливается ДО гарда либо проходит с `PREFIX_OK=1`.

### AGENT-9 — ночной автономный прогон Cursor

**ЦЕЛЬ.** Ночь производит N веток с зелёным гейтом и **ноль пушей**; утром LEAD принимает
и вливает. Конструкция разобрана 2026-09-10, в репо не заведена.

Опорный факт, он же переворачивал приоритет: ночной режим уже наполовину работал —
**21 открытый PR, из них 14 с веток `cursor/*`**, черновики шли подряд по вечерам
(#308…#323, ~20:00). Узкое место было не в запуске агента, а в приёмке: работа копилась
непринятой, и добавить ей темпа значило увеличить очередь, а не выпуск.

**Блокер снят 2026-09-12: `HYG-6` закрыта, очередь 14 → 0.** Разбор дал и мерку темпа для
прогона: приёмка четырнадцати черновиков стоила четырёх мёржей, четырёх бампов версии и
одного ручного разрешения конфликта. Ночь, производящая больше четырёх веток за раз,
снова создаст осадок — не потому что агент плох, а потому что `strict` на `main`
сериализует мёржи, и пропускная способность приёмки конечна.

Проектное решение (детали разбора — в сессии 10.09, здесь скелет):

- `cursor-agent` CLI, headless `-p --force`, **не** Background Agents: облачный агент пушит
  ветки в публичный репо без приёмки.
- Права — `.cursor/cli.json`: `deny` на `Shell(rm)`, `Shell(gh)`, `Read(.env*)`, `Write(sw.js)`
  (генерируется), `Write(scripts/**)`, `Write(.github/**)`. Deny побеждает allow.
- `Shell(git)` разрешён целиком, поэтому пуш режет хук `beforeShellExecution`
  (`.cursor/hooks.json`, exit 2 = deny): `git push`, `gh pr`, `--no-verify`, `reset --hard`.
- Карточка = worktree от свежего `origin/main` = один вызов; наряд из скилла `agent-brief`
  файлом, гейт после прогона, вердикт в `summary.txt` — утром читается он, не диффы.
- Мины: общий `package-lock.json` (бамп версии ночью не делает никто — это работа LEAD при
  мёрже), `prettier --check .` красен исторически и в вердикт не входит, трейлер
  `Co-Authored-By: Cursor` дублировать в наряд — в headless правила читаются слабее,
  без него работа уезжает в `unsigned` у `npm run scorecard`.
- Машина не должна засыпать: `powercfg /change standby-timeout-ac 0`; сон обрывает лог
  на середине карточки и читается как «агент застрял».
- **`prettier` только по своим правкам** — обязательная строка наряда, найдена разбором HYG-6:
  Cursor-ветки гоняют форматирование по всему файлу. #318 заявлен точечным фиксом, а весит
  +495/−113, из них львиная доля — переформатирование `js/analytics.store.js`; то же в #298 и
  #308. Ревью тонет в шуме, а `git log -S` по такому файлу после мёржа врёт. Ночь без этой
  строки произведёт тринадцать таких же диффов за раз.

**Права и гард заведены** (PR #325/#327/#329, добиты #352): `.cursor/cli.json` (allow/deny,
deny побеждает), `.cursor/hooks.json` → `.cursor/deny-push.mjs` (разбор строки, fail-closed),
`test/cursor-night-guard.test.js` — **78 кейсов: 52 на отказ, 22 на проход, 4 на сами конфиги**
(`node --test test/cursor-night-guard.test.js`, 15.09 — 78/78). Гард, запрещающий всё, ночь не
переживёт, поэтому проходная половина обязательна. Файл влит в `npm test` глобом
`node --test "test/*.test.js"` — отдельного запуска не требует. Раннера цикла в репо нет и не
будет — одноразовые скрипты не коммитятся.

Три находки ревью закрыты в том же PR, каждая ломала бы ночь или гард:

- **`push` — слово, а не только команда.** Правило матчило `push` в любом месте строки после
  `git`, поэтому `git commit -m "feat(push): …"` уезжал в отказ — в PPL-трекере это тип
  тренировки, ночь встала бы на первом дне толчка. Теперь текст в кавычках срезается до
  разбора, строка режется на команды по `&&`/`;`/`|`, а `git stash push` отделён от `git push`
  и `git send-pack`.
- **`failClosed` в `hooks.json` не был выставлен.** Внутри скрипта fail-closed стоял, но по
  контракту Cursor `beforeShellExecution` **по умолчанию fail-open**: краш, таймаут или
  пропавший файл пропускали команду при разрешённом `Shell(git)`. Гард закрывался ровно в тот
  момент, когда нужен. Проверено докой Cursor, закреплено кейсом на сам конфиг.
- **Короткий `-n` — тот же `--no-verify`.** `git commit -an -m x` обходил pre-commit. Правило
  теперь ловит кластерные короткие флаги у `commit`, не задевая `git log -n 5`.

Дальше — **два круга находок на сам фикс** (PR #327), и оба про одно: каждое исключение,
введённое ради ложного срабатывания, само оказывалось дыркой в правиле пуша.

1. Негативный `stash` в регэкспе прощал **слово**, а не команду: `git push origin refs/stash`
   и `git push origin $(git stash list)` уходили в allow.
2. Замена «снять форму `stash push`» прощала уже что угодно перед пушем:
   `git -c x.y=stash push` снова allow.
3. Разбор по токенам закрыл эти две, и следующий круг нашёл ещё три формы: спейсовые опции со
   значением вне списка (`git --attr-source HEAD push`), кавычки на самих токенах
   (`git "push"`, `git "-C" . push`) и алиас (`git -c alias.x=push x`).

4. Последний круг выдал две находки, требующие **противоположного**: одна — что `git -c "x=y
stash" push` обязан быть отказом (значит содержимое кавычек это код), вторая — что
   `git commit -m "git push"` обязан проходить (значит содержимое кавычек это данные).

**Вывод, ради которого это стоит читать.** Спор из п. 4 строковыми заменами неразрешим: снимаешь
кавычки — ломаешь сообщения коммитов, оставляешь — пропускаешь пуш в кавычках. Разрешает его
только разбор, помнящий про кавычки **на каждом токене**, — `tokenize()` отдаёт
`{ text, quoted }`, и дальше:

- **подкоманда** — первый не-флаг после `git`, опции со значением (`-C`, `-c`, `--git-dir`,
  `--config-env`, `--attr-source`…) съедают свой аргумент целиком, поэтому квотированное
  `-c "x=y stash"` не выдаёт себя за подкоманду;
- **голое слово `push`/`send-pack`** — только среди НЕквотированных токенов, поэтому сообщение
  коммита не срабатывает. Срабатывает всегда, кроме «все найденные подкоманды — `stash`». Этот
  проход и снимает зависимость от полноты списка опций: незнакомая опция уводит в отказ;
- **алиас** — `alias.<имя>=…push…` в значении `-c`, где бы оно ни стояло;
- **регэкспы остальных правил** (`--no-verify`, `-n`, `reset --hard`, `node -e`) судят строку,
  собранную только из неквотированных токенов.

Инлайн-скрипт: `-[a-z]*[ep][a-z]*` держит любой кластер с `e`/`p` (`-e`, `-pe`, `-epi`), длинные
`--test` / `--wait` / `--experimental-vm-modules` не задевает.

5. Шестой круг ушёл за пределы строки: `${X:-push}` и `$PUSH` (подстановка шелла), конфиг из
   окружения (`FOO=push git --config-env=alias.x=FOO x`, `GIT_CONFIG_KEY_*`), пламбинг
   `git http-push`, квотированные флаги `node "-e"`.

**Политика, которой это закрыто:** пламбинг и квотированные флаги — обычные пробелы, дописаны
(`(?:^|-)push$` вместо точного `push`, правило node — по токенам). Подстановку же и конфиг из
окружения хук вычислить не может **по устройству**: значение `$PUSH` живёт в среде, а не в строке.
Такие формы теперь отказ по умолчанию — но только там, где решается судьба команды: подстановка
на месте подкоманды или в значении конфиг-опции, присваивание вплотную перед `git`, любое
`GIT_CONFIG*` и `--config-env`. Аргумент подстановкой остаётся рабочим, иначе ночь потеряет
`npx prettier --write $(git diff --name-only)` — обязательную строку наряда.

Седьмой круг добрал то же двумя формами: brace-expansion (`git {push,fetch} origin` — фигурные
скобки остались в токене после смены разделителей, а проверялись только `$` и backtick) и
ложный отказ на `CI=1 npx prettier --write $(git diff --name-only)` — присваивание считалось
опасным по факту наличия git где-то дальше в строке, хотя git там внутри аргумента. Теперь
`{}` в одном ряду с `$`, а присваивания судятся по сплошному префиксу перед самим `git`, как в
шелле. Оба закрыты PR #329.

**Настоящая граница, и её надо принять решением, а не гардом.** Хук разбирает строку; всё, что
вычисляется вне строки — среда, алиасы, интерпретатор, — вне его власти. Ревьюер прав по каждой
форме и будет прав по следующей. Контроль, который закрывает класс целиком, один: **у ночного
прогона не должно быть права публиковать** — remote без права записи, снятый credential helper
или пуш только с машины LEAD. Гард после этого остаётся тем, чем и был: защитой от случайности и
внятным сообщением в лог, а не рубежом обороны. Пока такого решения нет, ночь идёт под утреннюю
приёмку и с этим ограничением в наряде.

**Решение Gio 2026-09-15: remote без права записи.** Ночной worktree ходит на origin через
fine-grained токен только на чтение — заводит его Gio в GitHub, ассистент учётные данные не
трогает и в файлы репо не пишет. Выбрано именно это из трёх: гарантия на стороне сервера, а не на
чистоте окружения (Windows Credential Manager глобален, снятый helper подменяется), и не на
дисциплине LEAD. Практика: у ночной копии свой `origin` через read-only URL, пуш падает
отказом сервера, ветки утром забирает LEAD из общего object store. `deny-push.mjs` остаётся
первым слоем и внятным сообщением в лог. Форма записи — не `remote set-url` (он общий на все
worktree), а worktree-скоуп; разбор и проверка — ниже, § «Процедура ночи», шаг 3.

**Цена петли: шесть кругов на одном правиле, каждый — 8-10 минут вердикта Bugbot.** Дёшево это
делается только в обратном порядке: сперва вердикт ревьюера, потом `--auto`. И второе: когда
ревьюер третий раз приходит в то же место, чинить надо не форму, а модель разбора.

**Порядок, который это вскрыло:** авто-мёрж срабатывает по обязательным чекам
(`test`+`e2e`+`drift`) и **не ждёт Bugbot** — обе находки приехали через минуты после мёржа.
На ветках, где ревьюер и есть смысл захода, `--auto` ставить после его вердикта, не до.

**Остаточный риск, гардом не закрываемый** (находка Security Reviewer, принята как граница
модели): разрешённые `Shell(node)`/`Shell(npm)`/`Shell(npx)` — интерпретаторы, а хук видит
только внешнюю строку. Инлайн `node -e` закрыт правилом, но чужой `npm run <script>` дотянется
до git в обход разбора. Это защита от случайной публикации, а не песочница; ночь остаётся
работой под утреннюю приёмку LEAD, а не автономным агентом.

**Живой прогон (13.09) вскрыл fail-open в контракте, а не в разборе строки.** Гард отвечал
`allow`/`deny` верно, но резал **весь** Shell в Cursor, включая `git status`, — три причины
разом: (1) ответ шёл camelCase `userMessage`, контракт Cursor — snake_case `user_message`,
строгая схема на части сборок считала ответ битым; (2) Cursor на Windows префиксит stdin
BOM (`U+FEFF`), `JSON.parse` падал, catch уходил в `deny`; (3) таймаут 10с не укладывался на
холодном старте node. При `failClosed: true` каждая из трёх сама по себе даёт то же самое —
отказ всему Shell, не только push. Фикс — PR #352 (`cursor/hook-bom-stdin`): `verdict()`
пишет оба ключа, BOM срезается до парсинга, таймаут 30с, добавлен `CURSOR_HOOK_OFF=1` —
дневной обход для LEAD. Тесты 78/78, drift/test/e2e на PR зелёные.

#### Процедура ночи

Порядок от нарезки worktree до чтения вердикта утром. Собран 15.09 и прогнан всухую —
**ни одного пуша**; шаг 3 ждёт токен Gio, остальные проверены командами в этой же сессии.

1. **Машина не должна засыпать.** `powercfg /change standby-timeout-ac 0` — сон обрывает лог
   на середине карточки, и утром это читается как «агент застрял».
2. **Нарезка: свой worktree на карточку, от свежего `origin/main`.** `git fetch origin`, затем
   на каждую карточку `git worktree add <путь> -b cursor/<карточка>-<2-3-слова> origin/main`,
   в новой копии `npm install` (**без `--prefix`** — он ставит корень зависимостью и пишет
   чужую версию в lock) и `npm run preflight` до нуля FAIL. Свежий worktree штатно даёт два
   FAIL — `node_modules` и `core.hooksPath`, указывающий в чужой чекаут; второй чинит
   `node scripts/fix-hooks-path.mjs`, без него pre-push упадёт ложным SAST-блоком.
   **Больше четырёх карточек за ночь не резать** — мерка HYG-6: `strict` на `main`
   сериализует мёржи, пятая ветка станет осадком, а не выпуском.
3. **Read-only remote — ШАГ ЖДЁТ ТОКЕНА.** Токен заводит Gio (GitHub fine-grained, `Contents:
Read`, область — только этот репозиторий); ассистент учётные данные не трогает.
   Форма из решения 15.09 — `git -C <worktree> remote set-url origin …` — **этот шаг сломает**:
   `remote.origin.url` лежит в общем `.git/config` (проверено `git config --show-origin --get
remote.origin.url` → `C:/PROJECTS/athlete-pro/.git/config`), один на все worktree, и
   переписать его значит отобрать пуш у корневого чекаута и у LEAD. Рабочая форма —
   worktree-скоуп; расширение в репо уже включено (`extensions.worktreeConfig=true`):

   ```bash
   git config --worktree remote.origin.url <read-only URL>  # .git/worktrees/<имя>/config.worktree
   git config --show-origin --get remote.origin.url         # обязан показать config.worktree
   ```

   Проверено всухую подстановкой заведомо нерабочего URL и откатом
   (`git config --worktree --unset remote.origin.url`): общий конфиг не изменился, корневой
   чекаут остался на своём remote. **Вопрос, который решается только с живым токеном:**
   Windows Credential Manager ключуется парой хост+пользователь и для `github.com` уже держит
   токен с правом записи — read-only URL обязан нести отдельное имя пользователя
   (`https://<имя>@github.com/…`), иначе helper подставит старый токен и remote снова станет
   пишущим. Приёмка шага: с машины Gio `git fetch` в ночной копии проходит, а пуш падает
   отказом сервера. До этой проверки ночь не запускать.

4. **Наряд — файлом, по одному на карточку.** Скелет целиком из скилла `agent-brief`
   (ЦЕЛЬ / ГДЕ СТОП / ФАЙЛЫ / ГЕЙТ / НЕ ТРОГАТЬ + ритуал). Ночные строки поверх шаблона:
   - **`Co-Authored-By: Cursor …` в каждом коммите.** В headless правила читаются слабее, а
     без трейлера работа уезжает в `unsigned` у `npm run scorecard` и pre-push роняет
     `scripts/check-branch-prefix.mjs` — имя `cursor/*` обязано совпасть с трейлером.
   - **`npx prettier --write <только свои файлы>`**, никогда `.`; `prettier --check .` в гейт
     не входит — красен по всему репо исторически (#318: +495/−113 на точечном фиксе).
   - **Версию и `package-lock.json` ночь не трогает** — бамп делает LEAD при мёрже, иначе
     четыре ветки дадут четыре конфликта по одному файлу.
   - **ГЕЙТ с якорем:** `npm run brief:gate -- "<цепочка>"` на свежей базе, в наряд
     `# verified @ <sha>`. Без якоря наряд не выдавать.
   - **ФИНИШ без пуша:** `git add -A && git commit` — и стоп. Пуш и PR делает LEAD утром.
5. **Запуск — `cursor-agent` headless, `-p --force`, из каталога worktree**, один вызов на
   карточку. **Не** Background Agents: облачный агент пушит ветки в публичный репо без
   приёмки. Права и гард приезжают из репо сами — `.cursor/cli.json` и `.cursor/hooks.json`
   → `node .cursor/deny-push.mjs` (`failClosed: true`, таймаут 30 с). Лог вызова — в свой файл.
6. **Гейт после карточки, тем же вызовом:** цепочка из наряда (`npm test` + то, что тянет
   правка: `npm run build:sw` для `js/`/`css/`, `npm run docs:budget` для доков).
7. **Вердикт — `summary.txt`, и он лежит ВНЕ worktree.** Утром читается он, не диффы: карточка,
   ветка, SHA последнего коммита, вывод гейта построчно, что не сделано и почему. Внутри копии
   его нельзя — ФИНИШ делает `git add -A`, и файл уедет в ветку.
8. **Утро, LEAD.** По каждой копии: `git log --oneline origin/main..HEAD`, гейт своими руками
   (чужое «готово» — гипотеза), затем пуш с машины LEAD и PR. Ветки лежат в общем object
   store — забирать их из ночных worktree не нужно, достаточно имени.

**Что осталось до закрытия карточки:** токен Gio, шаг 3 на живом токене (fetch проходит, пуш
падает отказом сервера) и первая ночь на одной карточке — прежде чем резать четыре.

### AGENT-10 — SCA-гард красит отказ инструмента как находку

**ЦЕЛЬ.** `pre-push` **и CI** отличают «уязвимости найдены» от «audit не смог отработать» и во
втором случае пропускают, как `pre-push` уже пропускает в airgap.

**Мест два, а не одно — и второе дороже.** Кроме хука тот же `npm audit` стоит шагом
«Security audit (high+)» в `.github/workflows/ci.yml:88` (`npm run security:audit`) внутри job
`test`, а `test` — required-чек. Пока эндпоинт отвечает 400, красным становится **любой** PR:
19.09 так упали разом `claude/changelog-deps-batch` и `claude/router-priority-stability`,
при зелёных `drift` и `e2e`. Очередь встаёт целиком, и по логу это читается как «тесты
упали», хотя юнит-сюита в тот же прогон зелёная.

**Найдено 19.09,** на пуше правки одного `CHANGELOG.md`: хук отбил push сообщением
«High or Critical vulnerabilities found in production dependencies». Уязвимостей не было —
`npm audit` отвечал 400 «Invalid package tree» от самого реестра.

Корень в [.githooks/pre-push:129](.githooks/pre-push:129): ненулевой exit `npm audit`
трактуется как находка, а он ненулевой и при отказе эндпоинта. Airgap-ветка выше (строки
116-123) ловит только мёртвую сеть по `ping registry.npmjs.org` — а здесь ping проходит,
падает именно audit. Дыра ровно между двумя состояниями, и текст отказа уводит в сторону:
разбор начинается с поиска уязвимости, которой нет.

**Что отказ был внешним — проверено, а не предположено:** пустой проект вне репозитория
(`npm init -y` + `npm install lodash`) даёт ту же 400. Наше дерево валидно: `npm ls --omit=dev`
exit 0, 333 пакета с `resolved`/`integrity`, override `qs` разрешается в 6.16.0.

**Как чинить.** Разделить два случая по выводу `npm audit --json` или по коду ошибки:
находка — это непустой `vulnerabilities` с `high`/`critical`, всё остальное (400, 5xx, таймаут,
мусор вместо JSON) — «SCA не отработал», предупреждение и `exit 0`, как в airgap. Своей
переменной обхода у SCA нет и заводить её не надо: гард обязан оставаться fail-closed по
находкам и fail-open по недоступности, иначе очередной сбой npm снова остановит все пуши.

**Мина при проверке:** тест на гард обязан ломаться на обоих концах — подсунуть и настоящую
high-уязвимость (должен блокировать), и ответ 400 (должен пропускать). Проверка только на
втором зазеленеет на хуке, который вообще ничего не блокирует.

### HYG-6 — разобрать очередь черновиков Cursor — ЗАКРЫТА

Закрыта 2026-09-12: очередь **14 → 0**. Пять депендаботов и три PR ушли 10.09, остальные
четырнадцать — четырьмя мёржами: **#336** (1.27.96, пять «влить как есть»), **#337**
(1.27.97, три), **#339** (1.27.98, четыре переделки), **#340** (1.27.99, ребейз #314
целиком). Что именно чинилось — `CHANGELOG.md`, разбор каждого вердикта — в описаниях PR.

**Почему пачками, а не по одному.** На `main` стоит `strict`: каждый мёрж делает остальные
PR устаревшими по базе, и ребейз начинается заново. Конфликт у всех был один и тот же —
пять механических файлов (`CHANGELOG.md`, `js/version.js`, `package.json`,
`package-lock.json`, `sw.js`), то есть четырнадцать циклов CI ради содержимого, которое
не пересекается. Пачка ставит механический слой один раз; код веток берётся коммитами с
сохранением автора и номера исходного PR.

**Три урока, купленных здесь:**

- **Гард, написанный на регекспе по исходнику, обязан вырезать комментарии.** Первая версия
  проверки `window.RestTimer` зеленела на `// window.RestTimer = RestTimer;` — правило
  выдавало себя за гард. Ловится только прогоном на поломку: сними фикс, убедись, что
  тест краснеет, верни. С тех пор так проверен каждый новый гард пачки C.
- **Форматный конфликт разрешать по функциям, не по ханкам.** `js/analytics.view.js`:
  обе ветки несли одинаковый прогон prettier по всему файлу, и построчное «беру свою
  сторону» затащило бы старый `deleteById + save` поверх уже влитого `applyCalendarTypeTap`.
  Дифф зелёный, баг вернулся.
- **Флаг-опция оставляет дефолт багованным.** Черновик #317 прятал чистку `localStorage`
  за `{ local: true }`; забыть флаг на новом пути удаления ничего не стоит. Разведено
  именами: `clearAll()` — IDB-only для тестов, `deleteAllUserData()` — пользовательское.

**#298 закрыт решением Gio** как перекрытый VOICE-2: `main` уже возит настоящую волну на
`AnalyserNode` (`js/intel.voice-wave.js`), PR строил конкурирующую на `@keyframes`.
Правка `test/air-guard.test.js` там была вынужденной, а не контрабандой. Ветка жива в
`refs/archive/cursor/launch-9-voice-hud` — если волна не пойдёт на живом iPhone, её
подход на ключевых кадрах готов как запасной.

Заведено попутно: `_obQuickStart` в `js/onboarding.js` затирает уже выбранный пол
(PR #341). До HYG-6 это было незаметно — пол в формуле Navy всегда был мужской.

### AI-1 — движок Anthropic обещан в UI, но мёртв на бэкенде

**ЗАКРЫТО 13.09 (1.27.114).** Кнопка `#engine-btn-anthropic` (`js/ai-settings.view.js`)
теперь несёт `disabled`, когда `fabKeyState(serverStatus, hasLocalKey, 'anthropic')` даёт
`'missing'` — ни серверного ключа, ни BYOK. Проверка стоит в двух местах и обязана
совпадать: в `renderEngineAndKey` (первый рендер, до ответа `/api/ai-status`) и в
`patchAiStatus` (после пинга — там же живёт переключение `.disabled`, значит ввод своего
ключа в поле снимает блокировку без перезагрузки листа). Airgap/ai-off не задеты:
`fabKeyState` для них возвращает `'airgap'`, не `'missing'`, кнопка остаётся кликабельной.
Гард — `test/ai-engine-lockout.test.js`, baseline проверен на поломку (3 из 3 красные без
фикса). Ключ по-прежнему не заведён на Vercel — решение не отменено, просто путь к 500
больше не начинается тапом по кнопке.

**ЦЕЛЬ (исходная, для памяти).** Пользователь не может выбрать движок, который гарантированно ответит 500.

Опорный факт (живой запрос к проду 2026-09-10, сборка `4ee38d5` подтверждена по deployments):
`POST /api/coach` с `engine: "anthropic"` отдаёт `500 NO_API_KEY` — `ANTHROPIC_API_KEY` не
заведён в окружении Vercel. При этом движок был виден и переключаем без проверки ключа —
`js/ai-settings.view.js:113` рисует индикатор `ai-status-anthropic`,
`js/ai-settings.store.js:12` знает префикс `sk-ant-`. Дефолтный путь зелёный —
`DEFAULT_AI_ENGINE = 'gemini'` (`js/shared/ai-engine.js:12`), поток дошёл до `[DONE]` за 1.8 с.

**Решение Gio 2026-09-10: ключа Anthropic сейчас нет, дефолт остаётся Gemini.** Значит до
раздачи ссылки закрывается со стороны UI — убрать выбор либо пометить движок недоступным,
а не заводить ключ. Это не регрессия от #303: путь не исполнялся и до него.

Хвост, который карточка не закрывает: рантайм `messages.stream()` и `stream.on('text')`
(`lib/aiOrchestrator.js:112-119`) на SDK 0.124.0 **не исполнялся ни разу** — в CI мок
(`test/coach-sse.test.js` подменяет оркестратор), на проде нет ключа. Прыжок 0.51 → 0.124
доказан только на уровне загрузки модуля: запрос дошёл до `_anthropicClient` и вернул
осмысленный отказ, значит ESM-резолв нового мажора живой. Проверять — локальным прогоном
оркестратора с ключом из окружения, не ключом в проде.

### HYG-5 — прополка рабочих копий — ЗАКРЫТА

Закрыта 2026-09-15. Реестр копий: 10 → 1 (только корень; `ap-agent8` и `ap-ai-1` сняты со своими
мёржами). Локальные ветки 18 → 12. Постановка к моменту прополки протухла целиком: ни 4190
удалений, ни `agitated-wescoff` не осталось — их унёс разбор SALV 13.09, а WIP сторожа жив веткой
`wip/watchdog-perms`.

**Чем это стоило заняться — не тем, что было в постановке.** Три «невлитые» копии (`ap-a1`,
`ap-a2`, `gifted-hertz`) оказались влитыми: `git cherry origin/main` отдавал `-`, SHA разошлись на
rebase, а `git status` показывал дифф просто потому, что база отстала. Одна из них при «доведении»
откатила бы влитый `A2` из `js/app.js` — тот же класс, что `--theirs` на ребейзе.

**Находка, которой в постановке не было.** Семь каталогов в `.claude/worktrees` git не знал вовсе:
`.git/worktrees` пуст, `git worktree list` слеп. Отсюда и разрыв в счёте (карточка видела девять
копий, реестр — две). Ловится только `ls` каталога против `worktree list`; из такой скорлупы любая
git-команда молча работает с корневым чекаутом. Шесть были пусты, седьмая — слепок репо от 03.08
на 79 МБ с `node_modules` и без `.git`.

### HYG-7 — прополка локального состояния: stash и имена веток — разобраны, ждут Gio

**ЦЕЛЬ.** `git stash list` и список веток читаются как работа, а не как осадок.

Хвост HYG-5 и AGENT-8, вынесен отдельно решением Gio 15.09.

**Часть stash закрыта 15.09.** Все 12 разобраны по одному — `git diff <база>^1 <база> -- <файл>`
против текущего `origin/main`, по сигнатуре функции/класса, не по названию. Одиннадцать оказались
мёртвым весом: содержимое уже влито (иногда под другим именем — `weekly-report` тесты из
`launch-9-f7-f8` дублировали то, что позже закрыло `GATE-WEEKLY` через `coach-weekly-fallback.test.js`),
либо решение прямо противоположное уже принято и исполнено (`DRUM-TOUCH-1`: стэш убирал
`canCompleteSet`, гард к этому моменту уже снят живым коммитом), либо это чужой мусор
(`stash@{5}` — `sw.js` от постороннего проекта `unit-calc`, залетевший в root-checkout). `КОРЕНЬ-1`
дропнут по своей же подписи в сообщении — ценное там унесено в `salvage-db-backup-wip` ещё 13.09.

Один кусок реально не влит: AI-settings панель debug-логов (`intel-settings-debug` /
`renderIntelLogs`, ~15 строк в `js/ai-settings.view.js`) из `stash@{3}` (`launch-9-f7-f8`).
Сохранена веткой `claude/hyg7-salvage-ai-settings-debug-logs` (= сырой стэш-коммит, не ребейзнута
на текущий main — остальное содержимое стэша уже дублирует влитое, доставать нужен только этот
кусок). Решение — Gio: доводить как карточку или закрыть.

`git stash list` пуст.

**Имена веток разобраны 18.09** (`git cherry`, `git diff origin/main...<ветка>`, grep по `origin/main`;
база `7ddd622b`). Гард AGENT-8 назначает префикс по первому подписанному коммиту.

| Ветка                                | Возраст           | Не влито (`+`)             | Владелец                                               | Вердикт                                                                                                                                                                                                                                                                                        |
| ------------------------------------ | ----------------- | -------------------------- | ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `air-4-sweep-guard`                  | 54 дн., на origin | 3 из 4                     | Gio, трейлера нет; подписан только `f1cbf67d` (Claude) | Мёртвый вес: AIR-4 влита как `69318fb9` (1.25.8), гард на blur живёт в `test/air-guard.test.js`; ветка несёт лишь дубль `air-tier-guard.test.js`. Не трогать — порог 30 дн, решает Gio                                                                                                         |
| `fix/panda-core-stabilization`       | 32 дн.            | 6 из 6                     | Gio, трейлера нет                                      | Живая, не влита: `handoff-lifecycle` (скрипты) на main отсутствует, правки `panda-mood.js`/`RestTimer` отстали на 30+ коммитов. Порог 30 дн — вердикт Gio: карточка или закрыть                                                                                                                |
| `wip/dashboard-placeholder-contrast` | 6 дн.             | 1 реальный + 2 стэш-мусора | `e897f600` подписан Claude Opus 5                      | Живая: `.is-placeholder` и правка `--c-text-3` на main не влиты. **Переименована в `claude/dashboard-placeholder-contrast`** (локально, на origin не было). Стэш-коммиты `ca8054dd`/`e074f52c` тащат чужой каталог `Anthropic-Cybersecurity-Skills-main` — при доводке брать только `e897f600` |
| `wip/set-completion-guard-test`      | 6 дн.             | 1                          | Gio, трейлера нет                                      | Мёртвый вес: гард `canCompleteSet` снят 05.08, тест проверял снятое. Имя не менять; сносить — Gio                                                                                                                                                                                              |
| `wip/watchdog-perms`                 | 6 дн.             | 2                          | Gio, трейлера нет                                      | Мёртвый вес: слепок индекса на 1.27.23, сторож `main-watchdog.yml` с тех пор ушёл вперёд (`860d7bca`). Имя не менять; сносить — Gio                                                                                                                                                            |

**Решает Gio (один список):**

1. `air-4-sweep-guard` — снести (на origin тоже: `push --delete`).
2. `fix/panda-core-stabilization` — карточка на ребейз или закрыть.
3. `wip/set-completion-guard-test`, `wip/watchdog-perms` — снести.
4. `claude/dashboard-placeholder-contrast` — довести как карточку (контраст WCAG AA) или закрыть.
5. `claude/hyg7-salvage-ai-settings-debug-logs` — `renderIntelLogs` на `origin/main` не влит (grep пуст), ветка остаётся сырым стэш-коммитом: карточка или закрыть.

Ветки без трейлера не переименованы: префикс угадывать нельзя. Для ветки открытого PR переименование
не годится (теряется PR) — там `PREFIX_OK=1`.

---

## Порядок между линиями

`AGENT-1` и `AGENT-2` закрыты — обе прошли **до** того, как первая карточка `LAUNCH-*` ушла
в работу к Cursor, ровно за тем и заводились: пока свод правил велел писать сырые hex и звать
несуществующую функцию, исполнитель производил бы код, валящий гейт, и виноват был бы не он.

`AGENT-3` закрыта 26.08 шаблоном наряда. `AGENT-4` закрыта 08.09 замером: после `AGENT-1`
новых `unsigned` нет. `AGENT-7` закрыта 08.09: гейт наряда якорится SHA через
`brief:gate`. `LAUNCH-*` не блокирует.

Две половины `LAUNCH-3` разведены по файлам, а не по важности: `3A` шла параллельно `5A`,
`3B` ждала мёржа `PR #255`. Все четыре закрыты — `3B` в PR #269 (1.27.77), пересечение по
`js/locale.store.js` больше никого не держит.

Дальше порядок такой: **`LAUNCH-6` закрыта** (#271 + ключ на Vercel, проверено 30.08).
**`LAUNCH-5B` влита** (#272, `9601e51`). **`LAUNCH-9` пункты 3–4 закрыты** (#273, `440c302`).
**`LAUNCH-4` закрыта** (#274, `3722ba2`). **`LAUNCH-7` закрыта** (#275, `21ae48d`).
**`LAUNCH-8` закрыта** (#276, `a9b0d5b`). **`LAUNCH-9` F-10 закрыта** (1.27.82).
**`LAUNCH-10` — этим PR.**
Пункт 6 `LAUNCH-9` (F-11) закрыт (1.27.84). Пункт 7 (F-7/F-8) закрыт (1.27.87).

**`5B` и `9` больше не параллелить:** 5B уже в main, `onboarding.js` свободен.

## Находки аудита

Два захода 2026-08-25. Первый — замеры без кликов; второй — интерактивный, он же поправил
первый.

**Инструмент второго захода.** Панель браузера внутри приложения не отображалась, а скрытая
панель не композитит кадры: `requestAnimationFrame` не тикает, `document.visibilityState`
навсегда `hidden`, скриншот падает по таймауту. Кликать в таком превью можно, но верить
результату нельзя — замёрзшая анимация выдаёт себя за зависший интерфейс. Заход выполнен
через Playwright 1.62.1 поверх того же `npm run dev` (:3001): chromium, профиль Pixel 7
(412×915, touch), `locale: ru-RU`, отдельный чистый `userDataDir` на каждый прогон — то есть
не «почищенное хранилище», а браузер, который этот сайт видит впервые. На каждом шаге
писались консоль, `pageerror`, сеть и внешние запросы.

### Первый запуск

**F-1 подтверждена кликами.** При `navigator.languages = ["ru-RU","ru"]` первый экран —
`What's your goal?`. Русский текст в онбординге написан и лежит мёртвым грузом за дефолтом
`'en'` в `js/locale.store.js`: там `let _lang = 'en'`, а `initLocale()` читает
`DB.Settings.get('lang', 'en')` — у новичка настройки нет, значит язык всегда английский, и
локаль устройства не спрашивается вообще.

Поправка к постановке `LAUNCH-5` остаётся в силе и после второго захода: онбординг уже
двуязычный, поэтому карточка не про перевод, а про выбор языка — дефолт от локали устройства
плюс явный переключатель на первом экране. Переключатель в приложении есть, но лежит в
профиле, до которого новичок доходит последним, и на дом он всё равно не действует (F-15).

**F-3 усилена.** `<html lang>` остаётся `en` не только у новичка, но и после того, как человек
явно нажал RU и выбор сохранился (`lang=ru` в `settings`). Это не «неверный дефолт», а
атрибут, который вообще никогда не следует за настройкой.

**Чего в первом заходе не увидели, а стоило:** за весь путь новичка — ни одного запроса на
внешний хост (лог `outbound` пуст), а `bodyMetrics` в IndexedDB лежат зашифрованными
(`_encrypted`/`_iv`). Для `LAUNCH-3` это факты в пользу продукта, их надо описать в документе.

### Онбординг

**F-6 · прогресс не переживает закрытие вкладки (высокая).** Профиль, оставленный на шестом
шаге «You're set», при следующем открытии стартует с первого: цель, опыт, пол, дата рождения,
рост и вес потеряны. Причина — `_data` живёт в памяти модуля, единственная запись в хранилище
происходит в `_obFinish` (`js/onboarding.js`), то есть на самой последней кнопке. Телефонный
звонок на четвёртом шаге стирает всё.

**F-7 · пол проставлен за пользователя (средняя).** `_data.sex = 'm'` в инициализаторе
(`js/onboarding.js:47`), а гейт шага — `!!_data.sex && !!_data.dob`: дату рождения выбрать
обязаны, пол — нет, он уже «выбран». На экране «Male» подсвечен до первого касания. Женщина,
не заметившая преселект, считается мужчиной в DOTS и в силовых тирах — а шаг ровно этим себя
и объясняет: «For accurate strength tier comparisons».

**F-8 · приватность проходится без выбора (средняя).** На шаге «Privacy First» кнопка активна
при нулевом выборе — `_navButtons(ru, true)`. Замерено после прохода без касания карточек:
`privacy.mode = airgap`, `privacy.aiEnabled = false`. Дефолт консервативный, и это правильно;
плоха не подстановка, а то, что человек не знает о сделанном за него выборе — и напрямую
получает F-12.

**F-9 · «Fast Skip» выдумывает профиль (высокая).** Кнопка на первом же экране проставляет
пол `m`, дату рождения `1995-01-01`, вес 80, рост 180, приватность `airgap` и завершает
онбординг — без экрана подтверждения и без пометки «это заглушки». Дальше приложение
показывает эти числа как данные пользователя: профиль честно рисует «80 kg / 180 cm / BMI
24.7». Самый быстрый путь новичка — и он же самый лживый.

**Мелочь того же экрана.** Заблокированная «Continue» отличается от рабочей только
`opacity: 0.4`; тап по ней не даёт ни подсказки, ни причины отказа — проверено тапом на
каждом шаге, состояние экрана не меняется.

### Оверлеи и модалки

**F-5 разделилась надвое: пальцем — нет, клавиатурой — да.** Тапом уйти нельзя:
`document.elementFromPoint` в центре всех четырёх кнопок таб-бара возвращает
`div#onboarding-overlay`, настоящий тап по «Train» оставляет экран на месте и оверлей
открытым. В этой части находка опровергнута, а исходный замер врал ровно потому, что мерил
`z-index` и `pointer-events` самой панели, не считая слой `z-index: 9000` сверху: перекрытие
проверяется попаданием, а не стилями.

Клавиатурой — воспроизводится полностью. Онбординг не модален: у оверлея нет ни `role`, ни
`aria-modal`, `#nav` не `inert` и без `aria-hidden`. Обход табом идёт прямо сквозь него —
аватар, четыре вкладки, кнопки онбординга, звук, FAB; фокус на «Train» плюс Enter уводят
приложение на `s-train`, а оверлей онбординга остаётся открытым поверх чужого экрана. То есть
для пальца это не дыра, а для клавиатуры, свитч-доступа и скринридера — она самая, и приводит
она в состояние, которого в продукте нет нигде больше.

**F-10 · системный «назад» не закрывает полноэкранный оверлей — закрыта (1.27.82).**
Было: Athlete Room (`z-index: 4000`) открыт поверх тренировки, аппаратный Back переключал
экран под ним (`s-train` → `s-home`), оверлей оставался открытым, Escape не делал ничего,
единственный выход — стрелка внутри комнаты. Стало: реестр оверлеев в `Nav` кладёт запись
в history, Back и Escape снимают верхний слой, перекрытие судится попаданием
(`elementFromPoint`), а не CSS. Реестр общий — следующий оверлей подключается двумя строками.

**F-11 · безымянные контролы (средняя).** На экране тренировки 50 из 106 видимых контролов
без доступного имени, включая `set-check` — ту самую галочку, которой закрывают подход;
дальше `drag-handle` и `ex-header-actions`. На профиле безымянных три: два степпера и глаз
показа ключа. Иконки динамического острова несут только `title` — для скринридера это имя,
но на тач-экране подписи не даёт никому.

### ИИ-тренер глазами новичка

**F-12 · входа в ИИ у новичка нет (высокая).** После онбординга `#claude-fab-container` имеет
`display: none` на домашнем экране и на экране тренировки — так решено в `js/claude.view.js`
(на `s-home` FAB прячется, «пока жив маскот»). Но маскот его не заменяет: тап по панде в
центре пустого экрана переключает звук (`dash:mascotSound`), экран не меняется, тренер не
открывается. FAB появляется только на Stats и Profile. Проверено при явно выбранном облаке:
`privacy.mode = cloud`, `privacy.aiEnabled = true` — то есть даже человек, который сознательно
включил ИИ, на двух главных экранах его не находит. Первый экран при этом обещает: «We'll
calibrate the AI coach».

**F-13 · молчаливый 503.** `/api/ai-status` отвечает `503`, ошибка уходит только в консоль.
В интерфейсе на профиле — секция «Claude Key · Get key ↗ · NO KEY», то есть модель по
умолчанию ждёт ключ от самого пользователя. Смежно с `LAUNCH-6`: там решается, ключ серверный
или пользовательский, здесь — что человек видит, пока ключа нет.

**F-14 · английский тост на пути выхода.** Закрытие FAB даёт «Assistant hidden. Enable in
Profile.» без ветки языка.

### Повторный вход

Работает: онбординг не повторяется, всплывает «Session restored», незакрытая тренировка
восстанавливается вместе с состоянием подходов — после перезагрузки на месте `1/3` и
записанный подход.

**F-15 · дом не говорит по-русски (высокая).** Переключатель EN/RU на профиле работает и
сохраняется: профиль и «Тренировки» становятся русскими, `lang=ru` лежит в `settings` и
переживает перезагрузку. Домашний экран остаётся английским: «Ready to crush it? · Your
training log is empty. Time to fix that. · Start First Workout». Корень — `js/dashboard.js`
не содержит ни одного вызова `isRu()`, строки прибиты в разметке. Это первый экран после
онбординга и он же экран каждого возврата.

В консоли на каждом входе — отладочный `Workout.load() called`. **Закрыто (1.27.95):** логи
сняты, а рядом нашлась утечка — `load()` вешал новый `setInterval` на 8 с на каждый заход.

### Офлайн

Работает. Второй заход с отрезанной сетью: приложение поднимается из кеша service worker
(`athlete-pro-v121-470857a6`, контроллер активен), Home, Train и Profile рендерятся, состояние
тренировки на месте. Сеть действительно была отрезана — запрос дев-сервера `/__build` упал с
`net::ERR_FAILED`.

### Поправки к первому заходу

**F-2 неверна в следствии.** Асимметрия словарей реальна — 207 ключей в `en` против 213 в
`ru`, и шесть `metrics.*` есть только в русском. Но их никто не читает: вне
`js/locale.store.js` в `js/` нет ни одного обращения к ним — ни строкой, ни склейкой. Хвост
`t()` действительно печатает сам ключ при отсутствии значения, только до этой ветки дело не
доходит: записи мёртвые. Последствие «посторонний увидит `metrics.sex_hint`» — не наступает.

**F-4 неверна.** `button.island-action-btn.skip` несёт `title="Skip Exercise"`
(`js/shared/dynamic-island.js:125`) и живёт в динамическом острове во время тренировки, а не
на первом экране. На первом экране безымянных кнопок ноль. Настоящая проблема именных
атрибутов — F-11, и она в другом месте.

### Мелочь, карточкой не заведённая

Блок `metrics.*` в словарях мёртв целиком, а не только шестью ru-ключами: обращений к нему в
`js/` нет вообще. `LAUNCH-5A` сносит шесть ради паритета, остаток — кандидат в общую прополку,
не в трек. Отладочный `Workout.load() called` на каждом входе — **закрыт (1.27.95)** вместе с
утечкой таймера в `load()`, гард `test/workout-load.test.js`; остальная линия NOISE остаётся
замороженной в `docs/_archive/`.

### Не пройдено

Создание и правка плана (`EDIT PLAN`), завершение тренировки целиком, выгрузка и загрузка
данных (это `LAUNCH-8`), живой ответ ИИ (нет ключа), WebKit (`LAUNCH-7`).
