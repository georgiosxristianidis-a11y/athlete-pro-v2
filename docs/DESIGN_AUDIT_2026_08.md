# Аудит Дизайн Системы (Obsidian) — Сравнение и План

## Вводные данные
- **Базовый аудит (Claude):** Июль 2026.
- **Текущий аудит (Antigravity):** 5 Августа 2026.
- **Цель:** Верификация исправлений, оценка остаточного дрифта, формирование плана.

## 📊 Сравнение данных (Было / Стало)

| ⚠️ Проблема | Описание (Claude Audit) | Текущий статус | Детали |
| --- | --- | --- | --- |
| **C1** | Неопределенный токен `--c-surface-deep` | 🟢 **Исправлено** | Токен полностью удален из CSS, fallback-проблема решена. |
| **C2** | Мертвый токен шрифта `var(--font)` | 🔴 **Не исправлено** | В `athlete-room.css` всё ещё 7 вызовов `var(--font)`, откатывающихся на системный дефолт. |
| **C3** | Документация (Спецификация vs Реальность) | 🟡 **Частично** | Устаревший `DESIGN.md` отправлен в `docs/_archive/DESIGN-vantablack-DEPRECATED.md`, но новый `DESIGN-SYSTEM.md` пуст. |
| **H1** | Рассинхрон основного цвета | 🟢 **Исправлено** | В `base.css` оставлен один `Primary` (`--c-accent: #00e676`), конфликт со Steel устранен. |
| **H2** | Конфликт брендовых и PPL цветов | 🟢 **Исправлено** | `var(--c-legs)` явно отделён по значению (`#8b5cf6`), а не ссылается на `Secondary`. Структурная независимость достигнута. |
| **H3** | Шкала border-radius игнорируется | 🟡 **Частично** | Добавлен `--r-xxl: 24px`. Крупные хардкод-значения переведены на токены, но мелкие (`2px-5px`) остались жестко заданы. |
| **M1** | Дублирование CSS блоков | 🟡 **Частично** | Дубликат блока `.toast` удален, но токены `--glow-*` всё ещё определены дважды в `base.css` (стр. 110 и 345). |
| **M2** | Злоупотребление `!important` | 🔴 **Не исправлено** | Осталось около 20 `!important` (из них 4 в одном классе `.stepper-val`). Каскад всё ещё ломается. |
| **M3** | Ошибка анимации `.btn-ghost` | 🟢 **Исправлено** | Свойство исправлено: `transition: transform var(--t-fast), opacity var(--t-fast);`. |
| **M4** | 5 способов задания типографики | 🔴 **Не исправлено** | Зоопарк путей к шрифтам сохранён (наличие `var(--font)`, хардкод). |

## 📈 Прогресс
Фундаментальная структурная работа проведена: основные цвета распутаны, критические сломанные поверхности устранены, анимации починены. Главная победа — очистка от устаревшего "Vantablack" документа и стабилизация ядра Obsidian (`base.css`).

## 🛑 Ошибки (Остаточный техдолг)
1. **Типографика сломана** в `athlete-room.css`.
2. **Документация пуста**: новый `DESIGN-SYSTEM.md` не заполнен, что блокирует новых агентов от понимания системы.
3. **Мусорный код**: `--glow-*` дублируются, `!important` продолжает замусоривать компонентную логику.

## 📋 План действий (Read-Only Planning Policy)

**Phase 1: Surgical Fixes (Критика и Гигиена)**
1. Точечная замена (surgical edit) `var(--font)` на `var(--font-main)` во всех 7 местах в `athlete-room.css`.
2. Удаление дубликатов `--glow-*` из `base.css` (строки 345-348).
3. Перенос оставшихся `2px-5px` радиусов на токены `--r-xs` и `--r-sm`.

**Phase 2: Single Source of Truth (Документация)**
1. Заполнение `docs/DESIGN-SYSTEM.md` актуальными токенами Obsidian.

**Phase 3: Component Integrity (Чистка специфичности)**
1. Рефакторинг `workout.css` и `base.css` для удаления `!important` через повышение специфичности (например, вложенные селекторы).

*Аудит завершен. Ожидаю утверждения плана для перехода к фазе Execution.*
