---
phase: 02-performance-reliability
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - sw.js
  - .planning/phases/02-performance-reliability/lighthouse-report.json
autonomous: false
requirements: [PERF-5]

must_haves:
  truths:
    - "Service worker cache is bumped to force browser to pick up all Phase 2 changes"
    - "Lighthouse mobile performance score is measured and recorded"
    - "Lighthouse mobile performance score is >= 90"
  artifacts:
    - path: "sw.js"
      provides: "Updated CACHE_NAME to athlete-pro-v5"
      contains: "athlete-pro-v5"
    - path: ".planning/phases/02-performance-reliability/lighthouse-report.json"
      provides: "Lighthouse JSON output with performance score"
  key_links:
    - from: "sw.js"
      to: "browser cache"
      via: "CACHE_NAME bump triggers old cache pruning on activate"
      pattern: "athlete-pro-v5"
---

<objective>
Bump the service worker cache version to pick up all Phase 2 file changes, then measure Lighthouse mobile performance score to confirm >= 90.

Purpose: Cache version bump ensures deployed changes (removed Firebase scripts, modified db-firebase.js, dashboard.js, shell.js, app.js) are served to users. Lighthouse measurement gates Phase 2 completion.
Output: Updated sw.js with v5 cache, Lighthouse JSON report in phase directory.
</objective>

<execution_context>
@C:/Users/Zephyrus/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zephyrus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-performance-reliability/02-RESEARCH.md
@.planning/phases/02-performance-reliability/02-01-SUMMARY.md
@.planning/phases/02-performance-reliability/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump sw.js cache version to v5</name>
  <files>sw.js</files>
  <read_first>sw.js</read_first>
  <action>
In `sw.js` line 6, change:
```js
const CACHE_NAME = 'athlete-pro-v4';
```
To:
```js
const CACHE_NAME = 'athlete-pro-v5';
```

This single change causes the service worker to:
1. Create a new cache (`athlete-pro-v5`) on install
2. Delete the old cache (`athlete-pro-v4`) on activate
3. Re-fetch all ASSETS into the new cache

This ensures browsers pick up: modified index.html (no Firebase scripts), modified db-firebase.js (dynamic SDK loader), modified dashboard.js (coalesced DB calls), modified app.js (no Analytics import), modified shell.js (lazy Analytics import).

Do NOT change anything else in sw.js. The ASSETS list is already complete (verified by PERF-4 / Plan 01).

After the change, also update `.planning/phases/02-performance-reliability/02-VALIDATION.md` frontmatter: set `nyquist_compliant: true` (all Wave 0 tests created, all plans have automated verify commands).
  </action>
  <verify>
    <automated>cd C:/PROJECTS/athlete-pro && grep -q "athlete-pro-v5" sw.js && ! grep -q "athlete-pro-v4" sw.js && node --test test/pwa.test.js</automated>
  </verify>
  <acceptance_criteria>
    - sw.js contains exactly `const CACHE_NAME = 'athlete-pro-v5';`
    - sw.js does NOT contain `athlete-pro-v4`
    - `node --test test/pwa.test.js` passes
    - 02-VALIDATION.md frontmatter has `nyquist_compliant: true`
  </acceptance_criteria>
  <done>Service worker cache bumped to v5. Old cached files will be replaced on next visit. VALIDATION.md nyquist_compliant updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run Lighthouse and verify performance score >= 90</name>
  <files>.planning/phases/02-performance-reliability/lighthouse-report.json</files>
  <action>
CHECKPOINT: Human verifies Lighthouse score and browser behavior.

All Phase 2 performance optimizations are complete:
- PERF-1: Dashboard DB coalescing (6 transactions -> 2)
- PERF-2: Analytics module lazy-loaded on first visit
- PERF-3: Firebase CDN scripts removed, dynamic injection on demand
- PERF-4: SW ASSETS verified complete
- SW cache bumped to v5

Steps for human verification:

1. Start the dev server: `node server.js`
2. Run Lighthouse CLI (install first if needed: `npm install -g lighthouse`):
   ```
   lighthouse http://localhost:3000 --output=json --output=html --output-path=.planning/phases/02-performance-reliability/lighthouse-report --only-categories=performance --chrome-flags="--headless --no-sandbox"
   ```
3. Check the performance score:
   ```
   node -e "const r=require('./.planning/phases/02-performance-reliability/lighthouse-report.json'); console.log('Performance:', Math.round(r.categories.performance.score*100))"
   ```
4. Score must be >= 90 on mobile profile (Lighthouse default).

5. Additionally verify in browser:
   - Open http://localhost:3000 in Chrome
   - Open DevTools > Network tab
   - Reload — confirm NO requests to `gstatic.com/firebasejs`
   - Navigate to Stats tab — confirm analytics.view.js loads on first click
   - Check Application > Service Workers — confirm athlete-pro-v5 is active

Resume signal: Type "approved" with the Lighthouse score, or describe issues found.
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification of Lighthouse score"</automated>
  </verify>
  <acceptance_criteria>
    - Lighthouse mobile performance score >= 90
    - No Firebase CDN requests visible in DevTools Network tab on page load
    - analytics.view.js loads only when Stats tab is first clicked
    - Service Worker shows athlete-pro-v5 as active in Application tab
  </acceptance_criteria>
  <done>Lighthouse mobile performance score >= 90 confirmed by human. Phase 2 gate passed.</done>
</task>

</tasks>

<verification>
- `grep "athlete-pro-v5" sw.js` confirms cache bump
- `! grep -q "athlete-pro-v4" sw.js` confirms old version removed
- `node --test test/pwa.test.js && node --test test/smoke.test.js` all pass
- `node --test test/perf.test.js` all pass (all PERF assertions green)
- Lighthouse mobile performance score >= 90 (human verified)
- No Firebase CDN requests on page load (human verified)
- Analytics module loads lazily on first Stats navigation (human verified)
</verification>

<success_criteria>
1. sw.js CACHE_NAME is 'athlete-pro-v5' and athlete-pro-v4 is absent
2. Lighthouse mobile performance score >= 90
3. All tests pass (smoke, pwa, perf)
4. Human confirms: no Firebase requests at boot, analytics lazy-loads, SW v5 active
</success_criteria>

<output>
After completion, create `.planning/phases/02-performance-reliability/02-03-SUMMARY.md`
</output>
