---
plan: "06"
phase: 1
wave: 5
depends_on: ["05"]
files_modified:
  - sw.js
  - js/workout.store.js
  - js/workout.view.js
  - js/analytics.store.js
  - js/analytics.view.js
  - js/claude.store.js
  - js/claude.view.js
  - js/dashboard.js
  - js/profile.js
  - js/timer.js
  - js/rest-timer.js
  - js/shell.js
autonomous: true
requirements: [ARCH-2, ARCH-3]
---

# Plan 06: Service Worker Update + JSDoc Completion Pass

## Objective
Update `sw.js` to reflect the new file structure (bump cache to v3, add new files, remove old monolith paths) and perform a final JSDoc annotation pass across all files that were not yet fully annotated. This completes the ARCH-2 and ARCH-3 requirements.

## Tasks

### Task 06-06-01: Update sw.js ASSETS list and cache name

<read_first>
- sw.js — full file (82 lines). Current `CACHE_NAME` is `athlete-pro-v2`. Current ASSETS list includes old monolith paths like `/js/workout.js`, `/js/analytics.js`, `/js/claude.js`.
</read_first>

<action>
In `sw.js`, make the following changes:

1. Change the cache name:
```js
const CACHE_NAME = 'athlete-pro-v3';
```

2. Replace the ASSETS array with the updated file list. Remove old monolith paths and add all new split files:

```js
const ASSETS = [
  '/index.html',
  '/manifest.json',
  '/js/app.js',
  '/js/shell.js',
  '/js/db.js',
  '/js/db-firebase.js',
  '/js/supabase-check.js',
  '/js/timer.js',
  '/js/rest-timer.js',
  '/js/dashboard.js',
  '/js/workout.store.js',
  '/js/workout.view.js',
  '/js/analytics.store.js',
  '/js/analytics.view.js',
  '/js/claude.store.js',
  '/js/claude.view.js',
  '/js/profile.js',
  '/js/body-stats.js',
  '/js/plate-calc.js',
  '/css/dashboard.css',
  '/css/workout.css',
  '/css/analytics.css',
  '/css/profile.css',
  '/css/claude.css',
  '/css/body-stats.css',
  '/icons/favicon.png',
  '/icons/icon-192.png',
  '/icons/icon-512.png',
  '/icons/apple-touch-icon.png',
];
```

3. Do NOT change the install, activate, or fetch handlers — they remain identical.

Note: The old paths `/js/workout.js`, `/js/analytics.js`, `/js/claude.js` are REMOVED because those files no longer exist. The new paths `/js/workout.store.js`, `/js/workout.view.js`, etc. are ADDED.

Also note: `db-firebase.js`, `supabase-check.js`, and `plate-calc.js` were previously MISSING from the ASSETS list (flagged by PERF-4). They are now included. This is an incidental fix that benefits Phase 2.
</action>

<acceptance_criteria>
- [ ] `grep -c "athlete-pro-v3" sw.js` returns `1`
- [ ] `grep -c "athlete-pro-v2" sw.js` returns `0`
- [ ] `grep -c "app.js" sw.js` returns `1`
- [ ] `grep -c "shell.js" sw.js` returns `1`
- [ ] `grep -c "workout.store.js" sw.js` returns `1`
- [ ] `grep -c "workout.view.js" sw.js` returns `1`
- [ ] `grep -c "analytics.store.js" sw.js` returns `1`
- [ ] `grep -c "analytics.view.js" sw.js` returns `1`
- [ ] `grep -c "claude.store.js" sw.js` returns `1`
- [ ] `grep -c "claude.view.js" sw.js` returns `1`
- [ ] `grep -c "rest-timer.js" sw.js` returns `1`
- [ ] `grep -c "supabase-check.js" sw.js` returns `1`
- [ ] `grep -c "'/js/workout.js'" sw.js` returns `0` (old monolith removed)
- [ ] `grep -c "'/js/analytics.js'" sw.js` returns `0` (old monolith removed)
- [ ] `grep -c "'/js/claude.js'" sw.js` returns `0` (old monolith removed)
</acceptance_criteria>

### Task 06-06-02: JSDoc completion pass — dashboard.js

<read_first>
- js/dashboard.js — full file (339 lines). Check if any JSDoc annotations already exist.
</read_first>

<action>
Add JSDoc annotations to all public functions in `js/dashboard.js`:

1. Add `// @ts-check` as the first line.

2. Annotate the `load()` function:
```js
/**
 * Load and render the dashboard home screen.
 * Fetches recent workouts, volume stats, streak data, and renders all sections.
 * @returns {Promise<void>}
 */
async function load() { ... }
```

3. Annotate any other public or private helper functions in the file with `@param` and `@returns`.
</action>

<acceptance_criteria>
- [ ] `grep -c "// @ts-check" js/dashboard.js` returns `1`
- [ ] `grep -c "@returns" js/dashboard.js` returns at least `1`
</acceptance_criteria>

### Task 06-06-03: JSDoc completion pass — profile.js

<read_first>
- js/profile.js — full file (380 lines). Check existing annotations.
</read_first>

<action>
Add JSDoc annotations to all public functions in `js/profile.js`:

1. Add `// @ts-check` as the first line.

2. Annotate public functions:
```js
/**
 * Load and render the profile screen.
 * @returns {Promise<void>}
 */
function load() { ... }

/**
 * Save body metrics (weight, height).
 * @returns {Promise<void>}
 */
async function saveMetrics() { ... }

/**
 * Save body measurements.
 * @returns {Promise<void>}
 */
async function saveMeasurements() { ... }

/**
 * Adjust default rest time.
 * @param {number} delta — seconds to add/subtract
 * @returns {Promise<void>}
 */
async function adjustRest(delta) { ... }

/**
 * Set the preferred weight unit.
 * @param {'kg'|'lbs'} unit
 * @returns {Promise<void>}
 */
async function setUnit(unit) { ... }

/**
 * Toggle haptic feedback preference.
 * @returns {Promise<void>}
 */
async function toggleHaptic() { ... }

/**
 * Export all data as JSON download.
 * @returns {Promise<void>}
 */
async function exportData() { ... }

/**
 * Import data from a JSON file.
 * @returns {void}
 */
function importData() { ... }

/**
 * Clear all application data after confirmation.
 * @returns {Promise<void>}
 */
async function clearAllData() { ... }
```
</action>

<acceptance_criteria>
- [ ] `grep -c "// @ts-check" js/profile.js` returns `1`
- [ ] `grep -c "@param\|@returns" js/profile.js` returns at least `10`
</acceptance_criteria>

### Task 06-06-04: JSDoc completion pass — timer.js and shell.js

<read_first>
- js/timer.js — 121 lines
- js/shell.js — newly created in Plan 02
</read_first>

<action>
**js/timer.js:**
1. Add `// @ts-check` as the first line.
2. Annotate public functions:
```js
/**
 * Start the main workout timer.
 * @returns {void}
 */
function start() { ... }

/**
 * Pause the timer.
 * @returns {void}
 */
function pause() { ... }

/**
 * Resume the paused timer.
 * @returns {void}
 */
function resume() { ... }

/**
 * Reset the timer to zero.
 * @returns {void}
 */
function reset() { ... }

/**
 * Get current elapsed seconds.
 * @returns {number}
 */
function seconds() { ... }

/**
 * Format seconds as MM:SS string.
 * @param {number} [s] — seconds (defaults to current timer value)
 * @returns {string}
 */
function fmt(s) { ... }

/**
 * Restore timer state from localStorage.
 * @returns {void}
 */
function restore() { ... }
```

**js/shell.js:**
1. Add `// @ts-check` as the first line (if not already present).
2. Annotate functions:
```js
/**
 * Navigate to a screen by ID.
 * @param {string} id — screen element ID (e.g., 's-home', 's-train')
 * @returns {void}
 */
function go(id) { ... }

/**
 * Get the current active screen ID.
 * @returns {string}
 */
function current() { ... }

/**
 * Show a toast notification.
 * @param {string} msg — message text
 * @param {'success'|'error'|'info'} [type='info'] — toast type
 * @param {number} [duration=2800] — display duration in ms
 * @returns {void}
 */
function show(msg, type = 'info', duration = 2800) { ... }
```
</action>

<acceptance_criteria>
- [ ] `grep -c "// @ts-check" js/timer.js` returns `1`
- [ ] `grep -c "@param\|@returns" js/timer.js` returns at least `7`
- [ ] `grep -c "// @ts-check" js/shell.js` returns `1`
- [ ] `grep -c "@param\|@returns" js/shell.js` returns at least `4`
</acceptance_criteria>

### Task 06-06-05: Final verification — all files have exports and JSDoc

<read_first>
- All js/*.js files
- sw.js
</read_first>

<action>
Run a comprehensive verification pass:

1. Verify every `js/*.js` file (except app.js which is the entry point consumer) has at least one `export` statement:
```bash
for f in js/db.js js/timer.js js/shell.js js/dashboard.js js/workout.store.js js/workout.view.js js/analytics.store.js js/analytics.view.js js/claude.store.js js/claude.view.js js/profile.js js/rest-timer.js js/body-stats.js js/plate-calc.js js/db-firebase.js js/supabase-check.js; do
  echo "$f: $(grep -c 'export' $f) exports"
done
```

2. Verify no `'use strict'` remains in any `js/*.js` file (modules are strict by default):
```bash
grep -rl "'use strict'" js/*.js
```
Expected: empty output (no matches).

3. Verify all store files have zero DOM references:
```bash
grep -n "document\.\|innerHTML\|addEventListener\|getElementById" js/workout.store.js js/analytics.store.js js/claude.store.js
```
Expected: zero matches.

4. Verify JSDoc coverage — each file has `@param` or `@returns`:
```bash
for f in js/db.js js/timer.js js/shell.js js/dashboard.js js/workout.store.js js/workout.view.js js/analytics.store.js js/analytics.view.js js/claude.store.js js/claude.view.js js/profile.js js/rest-timer.js; do
  echo "$f: $(grep -c '@param\|@returns' $f) annotations"
done
```

5. Verify old monolith files are deleted:
```bash
test ! -f js/workout.js && test ! -f js/analytics.js && test ! -f js/claude.js && echo "OLD FILES DELETED OK"
```

If any check fails, fix the issue before marking this task complete.
</action>

<acceptance_criteria>
- [ ] All `js/*.js` files have at least one `export` statement (except app.js which only has imports)
- [ ] `grep -rl "'use strict'" js/*.js` returns empty output
- [ ] `grep -c "document\.\|innerHTML\|addEventListener" js/workout.store.js js/analytics.store.js js/claude.store.js` returns `0` for each
- [ ] Every store/view/module file has at least 4 JSDoc annotations
- [ ] `js/workout.js`, `js/analytics.js`, `js/claude.js` do not exist
</acceptance_criteria>

## Verification

### Must-haves (goal-backward)
- [ ] `sw.js` has `CACHE_NAME = 'athlete-pro-v3'` and updated ASSETS list with all new file paths
- [ ] All public functions across all JS files have JSDoc `@param`/`@returns` annotations
- [ ] No `'use strict'` directives remain in ES Module files
- [ ] All store files have zero DOM references
- [ ] Old monolith files (`workout.js`, `analytics.js`, `claude.js`) are deleted

### Smoke test
1. Start the server: `node server.js`
2. Open the app in an incognito/private browser window (fresh cache)
3. Check DevTools > Application > Service Workers — verify `athlete-pro-v3` cache is installed
4. Navigate through all 5 screens — no errors
5. Go offline (DevTools > Network > Offline) — all screens should still load from cache
6. Hover over function calls in VS Code — JSDoc type hints should display
