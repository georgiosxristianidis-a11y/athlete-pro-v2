---
phase: 03-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/base.css
  - index.html
  - css/workout.css
  - css/claude.css
  - css/analytics.css
  - css/body-stats.css
  - css/dashboard.css
  - css/profile.css
  - sw.js
autonomous: true
requirements: [DESIGN-1, DESIGN-2, DESIGN-3]

must_haves:
  truths:
    - "css/base.css exists and contains design tokens + shared component styles"
    - "All screen CSS files import base.css (or base.css is loaded before them in HTML)"
    - "--c-text-3 is #9ca3af, meeting WCAG AA 4.5:1 contrast on dark backgrounds"
    - "All input/textarea elements show visible focus ring when navigated via keyboard"
    - "No duplicate component definitions across screen CSS files"
  artifacts:
    - path: "css/base.css"
      provides: "Design tokens, shared component styles, focus-visible styles"
      min_lines: 80
    - path: "index.html"
      provides: "Updated --c-text-3 token, base.css link"
      contains: "--c-text-3: #9ca3af"
    - path: "sw.js"
      provides: "base.css in ASSETS precache"
      contains: "/css/base.css"
  key_links:
    - from: "index.html"
      to: "css/base.css"
      via: "link rel=stylesheet loaded before screen CSS files"
      pattern: "base\\.css"
    - from: "css/workout.css"
      to: "css/base.css"
      via: "shared classes removed from workout.css (now in base.css)"
      pattern: "modal-overlay|btn-icon-sm"
---

<objective>
Create css/base.css as the design system foundation: extract shared component styles (buttons, modals, stat-chips) from screen CSS files, fix --c-text-3 contrast to WCAG AA, and add :focus-visible indicators to all inputs/textareas.

Purpose: Eliminate duplicated component definitions across 6 CSS files, establish a single source of truth for shared styles, and fix two accessibility violations (contrast + keyboard focus).

Output: css/base.css with shared components; updated screen CSS files with duplicates removed; updated index.html with contrast fix and base.css link; updated sw.js precache.
</objective>

<execution_context>
@C:/Users/Zephyrus/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zephyrus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create css/base.css with shared components, focus-visible, and fix --c-text-3 contrast</name>
  <files>css/base.css, index.html</files>
  <read_first>
    - index.html (lines 28-102 for inline :root tokens and reset)
    - css/workout.css (lines 473-544 for modal-overlay, modal-sheet, modal-handle, modal-header, modal-title, btn-icon-sm definitions)
    - css/workout.css (lines 722-728 for btn-primary)
    - css/claude.css (lines 90-136 for duplicate modal-handle, btn-icon-sm)
    - css/analytics.css (lines 80-96 for btn-icon-nav)
    - css/dashboard.css (lines 103-123 for streak-dot, stat-chip patterns)
    - css/body-stats.css (lines 295-313 for modal-overlay.bs-overlay overrides)
    - css/body-stats.css (lines 337, 382 for outline:none)
    - css/workout.css (lines 379, 603, 1065 for outline:none)
    - css/claude.css (lines 26, 452 for outline:none)
    - css/profile.css (line 105 for outline:none)
  </read_first>
  <action>
1. Create `css/base.css` with the following structure and content:

**Section 1 — Design Tokens** (`:root` block):
Move the full `:root` token set from index.html inline `<style>` into base.css. This includes all `--c-*`, `--sp-*`, `--r-*`, `--sh-*`, `--t-*`, `--safe-*`, `--nav-h` custom properties. Change `--c-text-3` from `#6b7280` to `#9ca3af` in this block.

Keep a minimal `:root` in index.html inline style with ONLY these critical-path tokens (needed before base.css loads):
```css
:root {
  --c-bg: #0a0a0f;
  --c-text-1: #e8eaed;
  --nav-h: 68px;
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);
}
```
Remove all other tokens from the inline `:root` in index.html (lines 58-102). The rest of the body/html/reset rules stay inline.

2. Also update `--c-text-3` from `#6b7280` to `#9ca3af` in the inline `:root` remnant if it references --c-text-3 (it should not after the move, but verify).

**Section 2 — Shared Component: Buttons**:
Extract into base.css:
```css
.btn-primary {
  background: var(--c-accent);
  color: #000;
}
.btn-primary:active {
  background: var(--c-accent-d);
  transform: scale(0.98);
}
.btn-icon-sm {
  width: 32px;
  height: 32px;
  border-radius: 8px;
  background: var(--c-surface);
  border: 1px solid var(--c-border);
  color: var(--c-text-2);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
}
.btn-icon-sm:active {
  background: var(--c-surface-h);
}
.btn-icon-nav {
  width: 36px;
  height: 36px;
  border-radius: 10px;
  background: var(--c-surface);
  border: 1px solid var(--c-border);
  color: var(--c-text-2);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  flex-shrink: 0;
  -webkit-tap-highlight-color: transparent;
}
.btn-icon-nav:active {
  background: var(--c-surface-h);
}
```

**Section 3 — Shared Component: Modals**:
Extract into base.css from workout.css (the canonical version):
```css
.modal-overlay { /* from workout.css:473-483 */ }
@keyframes overlay-in { /* from workout.css:484-491 */ }
.modal-sheet { /* from workout.css:492-501 */ }
@keyframes sheet-in { /* from workout.css:502-509 */ }
.modal-handle { /* from workout.css:510-516 */ }
.modal-header { /* from workout.css:517-522 */ }
.modal-title { /* from workout.css:523-528 */ }
```
Copy exact values from workout.css for these classes.

**Section 4 — Focus-Visible Styles**:
Add global focus-visible rules:
```css
input:focus-visible,
textarea:focus-visible,
select:focus-visible {
  outline: none;
  box-shadow: 0 0 0 2px var(--c-bg), 0 0 0 4px var(--c-accent);
}
button:focus-visible {
  outline: none;
  box-shadow: 0 0 0 2px var(--c-bg), 0 0 0 4px var(--c-accent);
}
```
This double-ring pattern (2px bg gap + 2px accent ring) ensures visibility on dark backgrounds without layout shift.

3. In `index.html`, add a `<link>` to load base.css BEFORE dashboard.css (the first CSS file loaded):
```html
<link rel="stylesheet" href="css/base.css" />
<link rel="stylesheet" href="css/dashboard.css" />
```
base.css is loaded synchronously (critical — contains tokens), not deferred like other CSS files.

4. In `sw.js`, add `'/css/base.css'` to the ASSETS array, right before `'/css/dashboard.css'`.
  </action>
  <verify>
    <automated>grep -c "c-text-3.*#9ca3af" css/base.css && grep -c "focus-visible" css/base.css && grep -c "btn-icon-sm" css/base.css && grep -c "modal-overlay" css/base.css && grep -c "base.css" index.html && grep -c "base.css" sw.js</automated>
  </verify>
  <acceptance_criteria>
    - css/base.css exists and contains `:root` block with `--c-text-3: #9ca3af`
    - css/base.css contains `.btn-primary`, `.btn-icon-sm`, `.btn-icon-nav` class definitions
    - css/base.css contains `.modal-overlay`, `.modal-sheet`, `.modal-handle`, `.modal-header`, `.modal-title` class definitions
    - css/base.css contains `input:focus-visible` and `button:focus-visible` rules with `box-shadow: 0 0 0 2px var(--c-bg), 0 0 0 4px var(--c-accent)`
    - index.html contains `<link rel="stylesheet" href="css/base.css" />`
    - index.html inline `:root` has only --c-bg, --c-text-1, --nav-h, --safe-top, --safe-bottom (5 properties max)
    - sw.js ASSETS array contains `'/css/base.css'`
  </acceptance_criteria>
  <done>css/base.css contains design tokens (with #9ca3af contrast fix), shared button/modal components, and focus-visible styles. index.html loads base.css before screen CSS. sw.js precaches base.css.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicated components from screen CSS files and replace outline:none with focus-visible</name>
  <files>css/workout.css, css/claude.css, css/analytics.css, css/body-stats.css, css/profile.css, css/dashboard.css</files>
  <read_first>
    - css/base.css (the file just created in Task 1 — verify exact class names defined there)
    - css/workout.css (full file — identify all classes now in base.css: .modal-overlay through .btn-icon-sm:active at lines 473-544, .btn-primary at lines 722-728, outline:none at lines 379, 603, 1065)
    - css/claude.css (full file — .modal-handle at lines 90-96, .btn-icon-sm at lines 121-136, outline:none at lines 26, 452)
    - css/analytics.css (.btn-icon-nav at lines 80-96)
    - css/body-stats.css (.modal-overlay.bs-overlay at lines 295-313 — these are OVERRIDES of the base, keep them; outline:none at lines 337, 382)
    - css/profile.css (outline:none at line 105)
  </read_first>
  <action>
**Remove duplicated component definitions** that are now in base.css:

1. **css/workout.css** — Remove these blocks (they now live in base.css):
   - `.modal-overlay` (lines 473-483) and `@keyframes overlay-in` (lines 484-491)
   - `.modal-sheet` (lines 492-501) and `@keyframes sheet-in` (lines 502-509)
   - `.modal-handle` (lines 510-516)
   - `.modal-header` (lines 517-522)
   - `.modal-title` (lines 523-528)
   - `.btn-icon-sm` and `.btn-icon-sm:active` (lines 529-544)
   - `.btn-primary` and `.btn-primary:active` (lines 722-728)
   - Remove `outline: none` at lines 379, 603, 1065 — base.css :focus-visible handles this globally

2. **css/claude.css** — Remove:
   - `.modal-handle` (lines 90-96) — duplicate of base.css version
   - `.btn-icon-sm` and `.btn-icon-sm:active` (lines 121-136) — duplicate of base.css version
   - Note: claude.css had btn-icon-sm at 34px vs workout.css 32px. If claude needs the larger size, add a `.claude-sheet .btn-icon-sm { width: 34px; height: 34px; border-radius: 10px; }` override in claude.css. Otherwise use the 32px base.
   - Remove `outline: none` at lines 26, 452

3. **css/analytics.css** — Remove:
   - `.btn-icon-nav` and `.btn-icon-nav:active` (lines 80-96) — now in base.css

4. **css/body-stats.css** — Keep:
   - `.modal-overlay.bs-overlay` overrides (lines 295-313) — these are variant overrides, NOT duplicates. They override the base modal-overlay behavior for body-stats specific animation.
   - Remove `outline: none` at lines 337, 382

5. **css/profile.css** — Remove:
   - `outline: none` at line 105

6. **css/dashboard.css** — No component duplicates to remove. Stat-chip stays here (it's only defined in dashboard.css).

**For each `outline: none` removal:** Do NOT add a replacement — the global `:focus-visible` rule in base.css handles focus indicators. Simply delete the `outline: none;` line. The selectors containing these declarations may have other properties — only remove the `outline: none;` property, not the entire rule.
  </action>
  <verify>
    <automated>echo "=== outline:none remaining ===" && grep -rn "outline: none" css/ ; echo "=== btn-icon-sm in workout.css ===" && grep -c "\.btn-icon-sm" css/workout.css ; echo "=== modal-overlay in workout.css ===" && grep -c "\.modal-overlay" css/workout.css ; echo "=== btn-icon-sm in claude.css ===" && grep -c "\.btn-icon-sm" css/claude.css ; echo "=== btn-icon-nav in analytics.css ===" && grep -c "\.btn-icon-nav" css/analytics.css ; echo "=== modal-handle in claude.css ===" && grep -c "\.modal-handle" css/claude.css ; echo "Done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "outline: none" css/` returns zero matches (all 8 instances removed)
    - `grep -c "\.btn-icon-sm" css/workout.css` returns 0 (moved to base.css)
    - `grep -c "\.modal-overlay" css/workout.css` returns 0 (moved to base.css)
    - `grep -c "\.btn-icon-sm" css/claude.css` returns 0 or 1 (0 if no size override needed, 1 if .claude-sheet .btn-icon-sm override added)
    - `grep -c "\.btn-icon-nav" css/analytics.css` returns 0 (moved to base.css)
    - `grep -c "\.modal-handle" css/claude.css` returns 0 (moved to base.css)
    - `grep -c "\.modal-overlay" css/body-stats.css` returns at least 1 (bs-overlay variant overrides preserved)
    - All CSS files still parse correctly (no syntax errors from partial removals)
  </acceptance_criteria>
  <done>All duplicated component definitions removed from screen CSS files. All 8 outline:none instances removed. Screen-specific overrides (body-stats modal variant, potential claude btn-icon-sm size override) preserved.</done>
</task>

</tasks>

<verification>
1. `css/base.css` exists with `:root` tokens, shared components, and focus-visible rules
2. `--c-text-3` is `#9ca3af` everywhere (base.css), not `#6b7280`
3. No `outline: none` remains in any CSS file under `css/`
4. No duplicate `.btn-icon-sm`, `.btn-icon-nav`, `.modal-overlay`, `.modal-sheet`, `.modal-handle` definitions across CSS files (only in base.css + overrides)
5. `index.html` loads `base.css` before other CSS files
6. `sw.js` precaches `base.css`
7. App loads without visual regression — all buttons, modals, inputs render correctly
</verification>

<success_criteria>
- css/base.css exists and is loaded first, containing all design tokens and shared components
- --c-text-3 is #9ca3af (WCAG AA compliant)
- All inputs/textareas/buttons have :focus-visible box-shadow indicator
- Zero outline:none in CSS files
- Zero duplicated component definitions across screen CSS files
</success_criteria>

<output>
After completion, create `.planning/phases/03-design-system/03-01-SUMMARY.md`
</output>
