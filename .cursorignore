# Системный мусор и сборка
node_modules/
dist/
build/
.vercel/
.cache/
coverage/
.git/

# Бинарники (текстом не читаются)
*.jpg
*.jpeg
*.png
*.gif
*.ico

# ВНИМАНИЕ: *.svg НЕ игнорируется — иконки в проекте только SVG,
# исполнитель обязан видеть иконочный слой (AGENT-2, 2026-08-25).

# Пожиратели токенов
*.log

# Изоляция контекста: черновики, не боевые правила
.planning/
.workbox/

# ВНИМАНИЕ: sw.js НЕ игнорируется — ASSETS/CACHE_NAME собирает `npm run build:sw`
# под тестом test/sw-cache-name.test.js. Слепой исполнитель роняет гейт, не видя причины.

# Секреты
.env*
.vscode/
.idea/
.DS_Store
Thumbs.db
