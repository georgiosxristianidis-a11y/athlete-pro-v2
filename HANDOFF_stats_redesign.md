# HANDOFF — Elite Stats Redesign (Concept 1)

> Ветка: `claude/silly-antonelli-d0a2cb` (FF-поверх serene-ishizaka). Обновлено 2026-07-05.

## STATS-1 — РЕАЛИЗОВАНО 2026-07-05 (silly-antonelli, SW v92)

- Бар из 3 чисел + прогресс-линия схлопнуты в `.live-hero`: герой-тоннаж 38px/800 tabular-nums + unit kg, чип `0/7 ex` (шёпот, справа), рельс `#live-rail` — сегмент на подход, done = `--c-accent` + glow.
- Count-up: `Spring.animate` (stiffness 170 / damping 28 — овердемп, без овершута), from = текущее число на экране (переживает re-render и прерывание анимации). `#live-sets-done` / `#workout-progress-fill` удалены; рельс перестраивается при live-добавлении сетов (W-1).
- Правки: `js/workout.view/render.js` (разметка), `js/workout.view/handlers.js` (`_updateLiveStats`), `css/workout.css` (старые .live-bar/.workout-progress-* удалены), `css/base.css:934` (.live-val → .live-hero-val в digit-списке).
- Верифицировано в превью: рельс == State (19/19), count-up монотонный 0→1059→…→1818, скрин ок. Гейт 229/229 + eslint 0 + stylelint 0 (38 старых warn).
- Замечено (pre-existing, НЕ STATS-1): при апдейте стейта извне (другая вкладка) set-check чекбоксы в DOM не перерисовываются — стата обновляется, строки нет.
- Остаток: полевой чек на телефоне + вердикт Gio по подаче (размер героя / цвет рельса).
> Гейт: `npm test` (229) + `npm run lint` (0 err). Скриншот в превью виснет из-за canvas-фона — усыплять rAF/canvas/fab перед снимком, мерить через preview_eval (DOM-замеры точнее скринов).

## Сделано в этой сессии (2 коммита, не запушены, main не тронут)

- **e9c7060** — UI-CENTER: однострочный заголовок упражнения. handle·icon·имя·PPL-badge·кнопки·chevron на одной оси; счётчик X/Y перенесён из заголовка в шапку сетов (`.set-header-row`, правая колонка над чек-боксами). JS-хук `card.querySelector('.exercise-meta')` цел.
- **0967e0d** — стат-бар = профильный паттерн `.pp-metrics-strip`: значения 21px/800 (было 24), оптический центр (line-height 1.2), divider 1×32, прогресс-линия 3px + плавающее свечение, зазор ⚡↔⋮ 4px. Профиль и workout согласованы (правил `css/profile.css` .pp-m-val тоже).

## Смежное (инфо от Gio, ДРУГАЯ ветка)

- BLUR-SCROLL + CORE-TAP-SAFETY закрыты: коммиты `1e098fa`, `c8ad2a3`, тег `checkpoint-2026-07-05-blur-scroll-core-air`. Не запушено. → обе 🔴 выпадают из очереди HANDOFF_next_cards.md (реконсиляция — LEAD при мёрже).

## Карточка STATS-1 — «Один герой + живой рельс»

Согласовано с Gio: плоский бар из 3 равных чисел = «просто» + дублирует Dynamic Island (верх, показывает 1/3 + таймер) и прогресс-линию. Элит = иерархия + консолидация + движение.

- **ЦЕЛЬ:**
  1. **Герой — Volume/тоннаж** (сейчас `#live-tonnage`, kg): вынести как одно крупное число с анимацией **count-up** при закрытии сета (эмоц. крючок силовой). Spring из `js/shared/spring.js`.
  2. **Прогресс-линия → сегментированный рельс**: `.workout-progress-track` (`render.js:381-385`, css `workout.css:137`) разбить на насечки — по подходу (или по упражнению), зелёная заливка по факту. Рельс становится статом «sets» → отдельный `sets`-стат из бара убрать.
  3. **«Exercises» → крошечный чип** `3/7` (шёпот, не равный третий стат).
  - Итого: одно смелое число + живой рельс + чип. Бар+линия схлопываются в один элемент.
- **ГДЕ:** разметка бара — `js/workout.view/render.js:363-385`. Живое обновление — `_updateLiveStats()` в `js/workout.view/handlers.js:656` (пишет `#live-tonnage`/`#live-sets-done`/`#live-ex-done` + `#workout-progress-fill` scaleX). Стили — `css/workout.css` (.live-bar блок ~128, .workout-progress-* ~137).
- **ГДЕ СТОП:** скриншот до/после (усыпить canvas); count-up не дёргается; рельс совпадает с реальным прогрессом; гейт 229 + lint 0.
- **НЕ ТРОГАТЬ:** профильный `.pp-metrics-strip` (он остаётся классическим ридаутом на профиле — это workout-специфичный редизайн); Dynamic Island (Концепт 2 «свернуть в Island» — отдельный смелый шаг, не смешивать); формулы `_updateLiveStats` (тоннаж/sets/ex считаются верно — менять только рендер/подачу).
- **АЛЬТЕРНАТИВЫ (если Gio передумает):** №2 свернуть всё в Dynamic Island (архитектурно); №3 кольца Apple-Fitness (риск — громче минимализма AIR).

## Следующий шаг

Прототипировать STATS-1 живьём в реальном компоненте, показать скрин, итерировать с Gio.
